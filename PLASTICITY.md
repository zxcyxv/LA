# 가소성 규칙으로서의 구조 — 헤비안, STDP, 그리고 빠진 항

> 이 구조의 상태 갱신이 시냅스 가소성 규칙과 **어디까지 같고 어디서 갈라지는지**를
> 유도한다. 결론: `ψ` 가 순서 무관 헤비안과 순서 민감 STDP 사이의 **정확한 보간
> 파라미터**이고, 빠진 것은 **post-synaptic 항** 하나이며, 그것이 곧 델타 규칙이다.

---

## 1. 상태 갱신은 흔적 기반 헤비안 규칙이다

```
S_t = Λ S_{t-1} + k_t v_t†     →     S_t = Σ_{n≤t} e^{-α(t-n)} Λ_phase^{t-n} k_n v_n†
```

| 항 | 시냅스 대응 |
|---|---|
| `k_n` | 시냅스전 활동 |
| `v_n` | 시냅스후 활동 |
| `k_n v_n†` | **헤비안 외적** |
| `e^{-α(t-n)}` | **적격 흔적(eligibility trace)** 의 지수 감쇠 |
| `S` | 시냅스 가중치 행렬 |

이는 STDP 의 표준 trace 기반 구현과 같은 식이며, 선형 어텐션을 외적 fast-weight
memory 로 보는 해석(Schlag et al. 2021)과도 일치한다. 대화내역2 §2 가 이 구조를
"fast-weight 연산자"로 규정했다.

위상이 더하는 것: **흔적이 복소라 회전한다.** 따라서 기여의 **부호가 lag 에 따라
바뀐다.**

---

## 2. 학습된 가소성 창 — 실측

selective copy 100% 체크포인트(`ckpt_model1_R4.pt`)에서 lag 별 평균 어텐션 가중
`ā(Δ)` 를 재면:

```
 Δ    ā(Δ)
 0   +0.043   +
 1   -0.212   ────────────────
 2   -0.128   ─────────
 3   -0.181   ─────────────
 4   -0.400   ──────────────────────────────  ← 억제 최대
 5   -0.179   ─────────────
 6   -0.036   ──
 7   -0.169   ────────────
 8   -0.075   ─────
 9   +0.043   +
10   +0.151   ++++++++++
11   +0.161   +++++++++++
12   +0.127   ++++++++
13   +0.191   +++++++++++++  ← 강화 최대
14   +0.006   +
15   -0.094   ──────
16   -0.011   ─
17   -0.041   ───
18   -0.180   ─────────────
19   -0.152   ──────────
```

**부호 반전 3회.** 단순 지수 감쇠가 아니라 **감쇠 진동형 시간창**이다.
해마 세타 위상에 따라 LTP/LTD 가 갈리는 **위상 의존 가소성**과 같은 형태다.

---

## 3. STDP 와의 정확한 대응 — 대칭/반대칭 분해

### 3.1 무엇이 STDP 를 STDP 로 만드는가

고전 STDP 창의 정의적 특징은 **시간 순서에 따른 부호 반전**이다.

```
W(Δt) = +A₊ exp(-Δt/τ₊)   (Δt > 0, pre→post : LTP)
W(Δt) = -A₋ exp(+Δt/τ₋)   (Δt < 0, post→pre : LTD)
```

즉 **홀함수 성분(반대칭)** 이 본질이고, `W(0) = 0` (lag 0 에서 가소성 없음) 이다.

### 3.2 우리 커널의 분해

```
Re[ q̄_t k_n ] = γ [ cos ψ · g(z_t,z_n)  +  sin ψ · ω(z_t,z_n) ]
                        └─ 대칭 ─┘           └─ 반대칭 ─┘
```

- `g(z_t,z_n) = Re⟨z_t,z_n⟩` — `t↔n` 교환에 **불변** → 순서 맹목, 순수 헤비안
- `ω(z_t,z_n) = Im⟨z_t,z_n⟩` — 교환하면 **부호 반전** → 순서 민감, **STDP형**

`ω` 는 심플렉틱 형식이고 교대형식이므로 **`ω(z,z) = 0`** — 이것이 곧 STDP 의
**`W(0) = 0`** 에 대응한다.

(부수 귀결: `a_tt ∝ cos ψ` 인 이유가 이것이다. EXCITABILITY.md §8 에서 기하로
유도한 사실과 여기서 가소성으로 유도한 사실이 같은 것이다.)

### 3.3 회전을 포함해도 성립한다

전개하면

```
a_tn = cos ψ · Re[e^{-iθΔ} w]  +  sin ψ · Im[e^{-iθΔ} w],     w = z̄_t z_n,  Δ = t-n
```

`t↔n` 교환은 `Δ → -Δ` 이고 `w → w̄` 인데, 둘이 합쳐져 결과적으로 **`ψ → -ψ`** 와 같다.
따라서 `cos` 항은 살아남고 `sin` 항만 뒤집힌다. **`θΔ` 회전이 반대칭성을 깨지 않는다.**

### 3.4 수치 검증

인과 마스크를 걷은 커널 `K_tn` 을 대칭/반대칭으로 분해하면:

| `ψ` | 대칭 ‖S‖ | 반대칭 ‖A‖ | 반대칭 비중 |
|---|---:|---:|---:|
| 0 | 3.384 | **0.000** | **0.0%** |
| 랜덤 | 0.938 | 0.474 | 33.5% |
| π/2 | **0.000** | 0.671 | **100.0%** |

**`ψ` 가 헤비안과 STDP 사이의 정확한 다이얼이다.**

| `ψ` | 커널 | 가소성 |
|---|---|---|
| 0 | 순수 `g` | 순서 무관 헤비안 (이때 `q=k`) |
| π/2 | 순수 `ω` | **STDP형 반대칭** |

### 3.5 유니터리일 때만 정확하다

위 검증은 `α = 0` 에서 한 것이다. 감쇠가 있으면 `e^{∓αΔ}` 가 교환 시 대칭이 아니라
**정확한 반대칭이 깨진다**. 즉 **STDP 창 모양을 정확히 가지려면 유니터리여야 한다.**

---

## 4. 학습된 모델은 어느 쪽을 썼나

| | 평균 기여 | 비중 |
|---|---:|---:|
| 대칭 `g` (헤비안) | 0.1131 | **80.1%** |
| 반대칭 `ω` (STDP형) | 0.0281 | 19.9% |

에너지로는 대칭이 지배한다. **그러나 채널 성향은 반대다.**

- `|sin ψ| > |cos ψ|` 인 채널: **39 / 64**
- `|cos ψ|` 평균 0.522 vs `|sin ψ|` 평균 **0.712**

그리고 결정적으로, selective copy 를 실제로 푸는 **주소 채널 두 개가 강한 반대칭형**이다.

| 채널 | cos ψ | sin ψ | |
|---|---:|---:|---|
| 20 | +0.298 | **−0.955** | 반대칭 우세 |
| 21 | +0.408 | **−0.913** | 반대칭 우세 |

즉 다수 채널이 대칭 성분으로 배경 확산을 하고, **일하는 채널만 반대칭**이다.
80:20 은 "반대칭이 안 중요하다"가 아니라 "배경이 많다"는 뜻이다.

---

## 5. 갈라지는 지점 두 개

### 5.1 스파이크가 없다

EXCITABILITY.md §6 에서 확인했듯 문턱·전부아니면전무 이벤트가 없다.
STDP 는 스파이크 타이밍을 전제하므로 그 이름을 쓰려면 발화를 먼저 만들어야 한다.

정확한 이름은 **"진동하는 적격 흔적을 가진 rate-based 가소성"**, 또는
**"위상 의존 가소성"** 이다. STDP 는 그 특수한 경우다.

### 5.2 `Δ ≥ 0` 만 관측한다 — **이 절은 틀렸다. §5.3 참조**

~~인과 마스크가 LTD 절반을 지운다~~ 는 서술은 **부호가 `Δ` 의 부호에 묶여 있다는
전제**였고, 그 전제가 틀렸다. 실제로는 인과 창 안에서 부호가 계속 뒤집힌다.

`Δ = t − n` 에서 `n` 은 쓴 위치, `t` 는 읽는 위치이므로 `Δ ≥ 0` 인 것은 맞다.
다만 그것이 **부호를 제약하지 않는다.**

### 5.3 정정 — 토큰 순서는 부호를 정하지 않는다

**실측.** 학습된 모델에서 고정 lag 별 개별 기여의 부호 분포:

| Δ | 양(+, LTP) | 음(−, LTD) |
|---:|---:|---:|
| 1 | 44.2% | 55.8% |
| 3 | 44.5% | 55.5% |
| 5 | 44.8% | 55.2% |
| 10 | 44.9% | 55.1% |
| 20 | 45.2% | 54.8% |

**모든 lag 에서 양·음이 거의 반반이다.** 주소 채널 21 단독으로 봐도 73:27 로 둘 다 나온다.
**인과 창 안에서 LTP 와 LTD 가 온전히 공존한다.**

#### 역할과 부호의 분리

| | 무엇이 정하는가 |
|---|---|
| **역할** (누가 pre, 누가 post) | **토큰 순서** — `n` 이 쓰고(pre), `t` 가 읽음(post) |
| **부호** (LTP 인가 LTD 인가) | **위상** — `cos(arg z_n − arg z_t − ψ_j − (Φ_t − Φ_n))` |

고전 STDP 는 **둘 다 타이밍이 정한다**. 우리는 **역할만 시간이 정하고 부호는 위상이 정한다.**

#### 위상 성분의 분해 — 시간 성분은 생 lag 가 아니다

총 위상은

```
Φ = (arg z_n − arg z_t)  −  ψ_j  −  (Φ_t − Φ_n)
    └── 쌍의 내용 ──┘      채널상수   └─ 누적 ─┘
```

세 번째 항이 **생 시간이 아니다.** `Φ_t − Φ_n = Σ_{m=n+1}^t ω_m`, `ω_m = θ + Δθ(h_m)`
이므로 **내용 가중 거리**다.

원형분산 측정 (0=완전 결정, 1=균등):

| ch | 유효 θ | 내용 `A` | **누적 `ΔΦ`** | 생 lag `θΔ` | 총 `Φ` |
|---|---:|---:|---:|---:|---:|
| **21** (주소) | −0.006 | 0.320 | **0.866** | **0.001** | 0.853 |
| **20** (주소) | −0.976 | 0.438 | 0.947 | 0.940 | 0.957 |
| 36 (무판별) | −0.201 | 0.056 | 0.537 | 0.748 | 0.590 |

**채널 21 이 결정적이다.** 생 lag 성분의 분산이 **0.0013** — 기본 회전 `θ = −0.006` 이라
36토큰 창에서 시간에 따른 위상 변화가 사실상 없다. 그런데 누적 `ΔΦ` 의 분산은 **0.866**.
**위상차 전체가 게이트에서 나오고 생 시간에서 오는 것이 0 이다.**

`ΔΦ` 가 무엇을 추적하는지 보면:

| ch | `ΔΦ` ~ 생 lag 상관 | `ΔΦ` ~ 데이터 토큰 개수 상관 |
|---|---:|---:|
| **21** | −0.184 | **−0.337** |
| 20 | −0.044 | −0.035 |
| 36 | **−0.998** | −0.789 |

채널 36(주소 기능 없음)은 생 lag 와 **−0.998** 로 거의 완전 상관 — 순수한 시계다.
채널 21 은 **데이터 개수와의 상관이 생 lag 의 두 배**다.

> **LTP 인지 LTD 인지가 "언제 왔나"가 아니라 "사이에 무엇이 몇 개 있었나"로 결정된다.**

#### 채널 유형이 분화한다

| 유형 | 예 | `ΔΦ` 의 정체 | 가소성 해석 |
|---|---|---|---|
| **시계형** | 36 | 생 lag (상관 −0.998) | 고전 STDP 에 가까움 — 타이밍이 부호를 정함 |
| **카운터형** | 21 | 데이터 개수 | **내용이 부호를 정함** — 고전 STDP 아님 |
| **고주파형** | 20 | 앨리어싱 (주기 6.4토큰) | 둘 다 흔들림 |

같은 모델 안에 세 종류가 공존하며, 태스크를 실제로 푸는 것은 카운터형과 고주파형이다.

**고전 STDP = 게이트가 꺼진 채널.** `Δθ ≡ 0` 이면 `ΔΦ = Δθ` 로 퇴화해 시간 시계가 된다.
게이트가 켜지면 시간 시계가 내용 시계로 바뀌고, 가소성 부호도 내용이 정한다.


---

## 6. 진짜로 빠진 것 — post-synaptic 항

### 6.1 확인

```
ΔS_t = k_t ⊗ v_t,     k_t = f(h_t),   v_t = h_t
```

**둘 다 같은 `h_t` 의 함수다.** 상태를 100배로 흔들어도 쓰기 항이 **정확히 0만큼**
변한다(실측 `0.0e+00`). 되먹임 경로가 없다.

생물학적 헤비안 규칙은 `Δw ∝ x^pre · y^post` 이고 `y^post` 는 **그 시냅스가 붙은
뉴런의 출력**, 즉 가중치 자신에 의존하는 양이다. 우리 것은 **입력 ⊗ 입력**이지
**입력 ⊗ 출력**이 아니다.

엄밀히는 헤비안도 아니고 **입력 자기상관 누적**이다.

### 6.2 예외 하나

깊이 축에는 있다. `h^{(r+1)} = h^{(r)} + f^{(r)}/R` 이므로 `r+1` 의 쓰기는 `r` 의
읽기 출력에 의존한다.

| 축 | post 존재? |
|---|---|
| 시퀀스 축 `t` (스캔 내부) | **없음** |
| 깊이 축 `r` (재귀 사이) | 있음 |

EXCITABILITY.md 의 FHN 분석에서 "두 축이 각각 절반씩 가지고 있다"고 한 것과
같은 구조다.

### 6.3 델타 규칙이 곧 post 항이다

```
ΔS_t = k_t ⊗ ( v_t − S_{t-1} k_t )
                    └─ 키 k_t 에 현재 저장된 값 = 모델 자신의 출력 ─┘
```

같은 교란 실험에서 오차항은 **375 만큼 변한다** — 상태에 의존한다.
Widrow-Hoff `Δw ∝ (목표 − 출력) × 입력` 의 정확한 형태다.

---

## 6.4 정정 — 델타 규칙은 STDP 의 LTD 가 아니다

§6.3 에서 "델타 규칙이 곧 post 항"이라고 썼다. **부정확하다.**

trace 기반 STDP 의 표준형은:

```
pre 흔적:  x_j(t) = Σ_{n≤t} μ₊^{t-n} · (pre 활동)
post 흔적: y_j(t) = Σ_{n≤t} μ₋^{t-n} · (post 활동)

post 발화 시:  Δw += A₊ · x_pre     ← LTP
pre  발화 시:  Δw −= A₋ · y_post    ← LTD, **post 흔적에 비례**
```

**LTD 는 "현재 저장값과의 오차"가 아니라 "post 가 최근 얼마나 발화했는가"에 비례한다.**
델타의 `v − S†k` 는 오차 보정(Widrow-Hoff)이지 흔적이 아니다. 다른 규칙이다.

### "post 먼저" 의 세 가지 구현

우리 쓰기 `ΔS_t[j,:] = k_{t,j} v_t†` 는 **pre 발화 이벤트**다. 여기에 LTD 를 붙이는 방식:

**(a) post 흔적으로 기존 가중치를 억제**
```
S_t[j,:] = (λ_j − A₋ y_j(t-1)) S_{t-1}[j,:] + k_{t,j} v_t†
```
채널별 스칼라 곱 → 대각 유지, 교환법칙 성립, 병렬 스캔 그대로.
결과적으로 **입력 의존 감쇠**이며 Mamba 의 selectivity 와 같은 형태다.

**(b) post 흔적으로 새 쓰기를 억제**
```
ΔS_t[j,:] = k_{t,j} (v_t − η y_j(t-1))†
```
`y` 가 **입력에서만 계산되면**(예: 쿼리 흔적 `y_j = Σ μ^{t-n}|q_{n,j}|²`)
쓰기 항이 입력만의 함수라 **재귀식이 현재와 동일한 대각 형태**다.
`cumsum` 으로 병렬화되고 위상 코드·라그랑지안 구조가 그대로 보존된다.

의미: **"최근에 많이 조회된 채널에는 새로 덜 쓴다"** — 사용 중인 기억의 보호.

**(c) 오차 보정 (델타 규칙)**
```
ΔS_t = k_t (v_t − S_{t-1} k_t)†
```
`S` 에 대한 랭크-1 전이 → 대각성이 깨지고 chunked WY 가 필요하다.

### 비교

| | post 신호 | 대각 유지 | 병렬 | `‖S‖` 묶임 | 문헌 |
|---|---|---|---|---|---|
| (a) 흔적 → 기존 억제 | post 흔적 | ○ | ○ | 부분 | Mamba selectivity |
| **(b) 흔적 → 쓰기 억제** | **post 흔적** | **○** | **○ (cumsum)** | **✗** | **확인 못 함** |
| (c) 오차 보정 | 현재 출력 | ✗ | 청크 필요 | ○ | DeltaNet |

**(b) 가 STDP 의 LTD 형태에 가장 충실하면서 우리 병렬 구조를 전혀 깨지 않는다.**

**단, (b) 는 쓰기 크기를 조절할 뿐 기존 내용을 지우지 않으므로 `‖S‖` 를 묶지 못한다.**
용량 문제에는 여전히 (c) 또는 감쇠가 필요하다. (b) 와 (c) 는 대체재가 아니라
**다른 일을 하는 서로 다른 항**이다.

- **(b)** = STDP 충실도 + 사용 중 기억 보호. **대각성 유지 = 공짜**
- **(c)** = 용량 재사용. **대각성을 대가로 지불**

### KV 캐시의 진화

| | 저장소 | 과거 항목의 운명 |
|---|---|---|
| softmax attention | append-only, `O(T)` | **절대 안 변함** — `k_n,v_n` 이 동결 |
| 선형 어텐션 (현재) | 고정 크기 | 전부 **균일하게** 회전·감쇠 |
| **델타** | 고정 크기 | **내용 주소로 선택적 재기록** |

self-attention 의 KV 는 고정이고 우리 것은 진화한다. 다만 현재는 "균일하게 흐려지는"
진화이고, 델타는 "관련된 것만 골라 고쳐 쓰는" 진화다. 후자만이 용량을 재사용한다.

수치 확인 (유사 키 `k1·k2 = 0.588` 로 덮어쓴 뒤 `k1` 읽기):

| | 오차(직후) | 오차(덮어쓴 뒤) | `‖S‖` |
|---|---:|---:|---:|
| 누적 (현재) | 0.0000 | 1.824 | 5.499 |
| 델타 | 0.0000 | 2.114 | **4.611** |

**둘 다 오염된다.** 델타가 사주는 것은 간섭 제거가 아니라 **노름 안정성과 용량 재사용**
이다. MQAR 설계 시 이 구분을 유지해야 한다.

그리고 (a)(b)(c) 모두 **인과 재귀**이므로 자기회귀가 유지된다. 양방향으로 갈 필요가 없다.

---

## 6.5 LTD 는 델타를 대신할 수 없다 — 방향성이 핵심

**질문.** LTD 는 인과에 어긋나는 연결을 억제하는 역할을 하는데, 그것으로 델타를
대신할 수 있는가?

**답: 못 한다.** 하는 일이 다르다.

### 실험

`p=64`, 키-값 쌍 `N=300` (즉 `N ≫ p`, 용량 초과 영역). `‖V‖` 평균 = 5.65 이므로
그보다 큰 오차는 사실상 복원 실패다.

| 규칙 | `‖S‖` | 최근 20개 오차 | 초기 20개 오차 |
|---|---:|---:|---:|
| 순수 헤비안 (현재) | 99.11 | 12.72 | 13.05 |
| LTD → 쓰기 억제 (b) | **25.50** | 5.45 | 4.78 |
| LTD → 기존 억제 (a) = 감쇠 | 55.50 | 6.96 | 8.28 |
| **델타 (c)** | 45.38 | **2.60** | 7.74 |

**LTD(b) 는 노름을 가장 잘 묶지만 복원을 전혀 못 한다.** 쓰기를 줄여 노름이 안 자라는
것은 "아무것도 제대로 안 쓴 것"과 구별되지 않는다.

**델타만 최근 항목을 실제로 복원한다**(2.60, 유일하게 `‖V‖` 의 절반 이하).
초기 항목은 7.74 로 버렸다 — **의도된 거래다. 오래된 것을 치우고 최근 것을 정확히 저장.**

### 왜 그런가

| | 방향성 | `‖S‖` 묶음 | **오류 정정** |
|---|---|---|---|
| LTD (a) 기존 억제 | 채널별 **균일** | ○ | ✗ |
| LTD (b) 쓰기 억제 | 없음 | ○ | ✗ |
| **델타** | **키 방향 선택적** | ○ | **○** |

델타의 고유 능력은 **키 `k` 방향으로만 골라 지우는 것**이다. LTD 는 균일하게 줄이거나
쓰기를 약하게 할 뿐이라 **기억을 고치지 못하고 흐리기만 한다.**

그리고 이는 피할 수 없다:

> **방향 선택적 지우기 ⟺ `(I − βkk†)` 랭크-1 보정 ⟺ 비대각 전이 ⟺ 청크 스캔**

스칼라·대각 연산으로는 원리적으로 방향성을 만들 수 없다.

### "인과에 어긋나는 연결 억제" 는 이미 있다

LTD 의 기능적 역할 — 예측하지 못하는 연결을 잘라내는 것 — 은 우리 모델에 **이미
구현되어 있다.** 다만 가소성이 아니라 **읽기 가중의 부호**로.

`a_tn < 0` 이면 토큰 `n` 의 값이 `t` 의 출력에서 빠진다(§5.3 의 44:56).
**억제성 연결은 표현된다.** 그러나 **가소성으로서의 억제는 없다** — 쓰기는 항상
`k ⊗ v` 로 더하기만 하고 부호는 읽을 때 위상이 정한다.

| LTD 의 두 의미 | 우리 모델 |
|---|---|
| **억제성 연결** (음의 가중치) | **있음** — 음의 어텐션 가중 |
| **기존 연결의 약화** (쓴 것을 지움) | **없음** — 쓰기는 누적만 |

**후자가 용량 문제를 푸는 쪽이고, 그것이 델타 또는 감쇠다.**

### 남은 선택지

| # | 방향 | 얻는 것 | 비용 |
|---|---|---|---|
| ① | **델타 + 유니터리** | 용량을 실제로 해결 | 청크 스캔. DeltaNet 계열과 정면 경쟁 |
| ② | LTD(b) + 감쇠 유지 | 대각성 유지, STDP 충실도 | 성능 이득 기대 어려움(위 실험) |
| ③ | 현 구조 + 메커니즘 분석 심화 | 해석가능성 논문 | 벤치마크 포기 |

**①이 아니면 성능으로 이길 길은 현재 보이지 않는다.**

---

## 7. 다섯 갈래가 한 항으로 모인다

이번 세션에서 따로 나온 문제들이 전부 `−S_{t-1}k_t` 하나의 결핍이다.

| 증상 | 어디서 나왔나 | 델타가 해결하는가 |
|---|---|---|
| 유니터리가 짐 (2.825 vs 2.002) — 노름 성장 | CAPACITY.md §4 | **예** — 치환이라 노름이 랭크에 묶임 |
| `τ` 초기화가 0.68 bpb 결정, 학습 안 됨 | CAPACITY.md §4.2 | **예** — 감쇠 자체가 불필요 |
| `T > p` 에서 SNR `1/√t` 붕괴 | `T` 스윕 | **예** — 본래 주장 |
| pre 만 있고 post 없음 | §6 | **부분** — 오차항은 post 이나 STDP 의 LTD 는 아니다(§6.4) |
| 정확한 반대칭(STDP형)에 유니터리 필요 | §3.5 | **예** — 유니터리가 살아남 |

그리고 **유니터리와 델타가 서로를 필요로 한다.**

- 델타가 노름을 묶어주므로 감쇠를 넣을 이유가 사라진다
- 프레임 변환 `S̃_t = Λ^{-t}S_t` 가 정확하려면 `|λ| = 1` 이어야 한다
  (`|λ|<1` 이면 `Λ^{-t}` 가 `e^{+αt}` 로 폭발)

프레임 변환 후:

```
S̃_t = (I − β k̃_t k̃_t†) S̃_{t-1} + β k̃_t v_t†,     k̃ = Λ^{-t} k_t
```

`Λ` 가 유니터리 대각이므로 `Λ^{-t}(I − βkk†)Λ^t = I − βk̃k̃†` 로 **형태가 보존**된다.
회전이 키에 완전히 흡수되고 남은 전이는 항등이다. 이후 DeltaNet 의 표준 chunked
알고리즘이 수정 없이 적용된다.

**주의:** 이것이 "청크가 불필요"를 뜻하지 않는다. `(I − βk̃k̃†)` 들끼리는 여전히
교환하지 않는다. 얻는 것은 **새 알고리즘이 필요 없다**는 것이다. 그리고 우리 현재
병렬 경로가 이미 `O(T²)` 이므로 chunked (`O(T·C·d)`) 는 오히려 싸다.

---

## 8. 다음 단계

**델타 규칙. 단 승부처를 MQAR 로 옮긴다.**

8MB 바이트 LM 에서 `T=128, p=128` 로는 이길 수 없다. 용량 경계에 겨우 닿는 설정이고,
격차의 상당 부분이 MLP 경로(82.7%)에서 오는데 델타는 그것을 안 건드린다.

델타의 주장은 **"`T ≫ p` 에서 질적으로 다르다"** 이므로 그것이 드러나는 곳에서 재야 한다.
**MQAR** (multi-query associative recall) 이 정확히 그 태스크이고, Mamba·GLA·DeltaNet
논문이 모두 쓰므로 비교 가능한 숫자가 이미 있다.

### 8.1 계획

**1단계 — 순차 델타 구현.** `‖S‖` 가 실제로 묶이는가, `α=0` 에서 학습이 도는가.

**2단계 — MQAR 스윕.** `p=128` 고정, 키-값 쌍 `N` 을 32 → 512.

| 조건 | 예측 |
|---|---|
| 감쇠 누적 (현재) | `N` 증가 시 무너짐 |
| 유니터리 누적 | 더 빨리 무너짐 |
| **유니터리 + 델타** | **`N > p` 까지 버팀** |
| 감쇠 + 델타 (GDN 유사) | 비교군 — 유니터리가 실제로 이기는지 확인 |

마지막 줄이 중요하다. "델타가 좋다"는 이미 알려진 것이므로, 우리 차별점인
**감쇠 없는 순수 유니터리**가 실제로 값을 하는지 반드시 같이 재야 한다.

**3단계 — LM 복귀.** 2단계가 되면 `T` 를 512~1024 로 늘려 다시 본다.

### 8.2 안 할 것

- **`τ` 초기 범위 스윕** — 델타가 되면 감쇠가 사라지므로 무의미. 델타 실패 시에만
- **멀티헤드, `SiLU+W_O`** — 증분 개선이고 이야기를 안 바꿈
- **SNN 자기지도 학습** — STDP 기반 비지도 학습은 역사적으로 스케일하지 않았고
  (SOTA SNN 은 surrogate gradient BPTT 를 쓴다), 벤치마크 게임을 떠나게 된다.
  **프레임만 취하고 학습법은 취하지 않는다.**

### 8.3 반증 조건

**`N > p` 에서도 델타가 안 버티면 이 아키텍처 방향은 끝이다.**

그 경우 남는 가치는 메커니즘 분석 — 위상 코드, `ψ` 의 계량/심플렉틱 분해와
헤비안↔STDP 보간, `M(S)` 흥분성, 깊이↔위상 교환 — 이고, 해석가능성 논문으로
정리하는 것이 맞다. 그 분석들은 Mamba-3 같은 실제 모델에 그대로 적용되며,
같은 위상 코드가 나온다는 것을 이미 확인했다(RESULTS.md §7).

### 8.4 유보

델타 + 회전 조합은 **누구든 곧 할 조합**이다. Mamba-3 가 GDN 을 주 경쟁자로
놓았으므로 "Mamba-3 + 델타"는 명백한 다음 수다. 우리 자리는 "먼저 했다"가 아니라
**감쇠 없는 순수 유니터리 + 델타**, 그리고 `ψ` 의 기하 구조다.
그 차별점을 실험 설계에 처음부터 박아넣어야 한다(§8.1 의 비교군).

---

## 9. 요약 — 무엇을 주장할 수 있나

**주장 가능**

> 이 구조의 어텐션 커널은 계량 성분과 심플렉틱 성분의 `ψ`-혼합이며, 심플렉틱
> 성분은 토큰 순서 교환에 대해 반대칭이다. 즉 `ψ` 는 **순서 무관 헤비안 가소성과
> 순서 민감 STDP형 가소성 사이의 보간 파라미터**다. 학습된 모델에서 내용 주소지정을
> 담당하는 채널들은 반대칭 성분이 지배적이었다(`|sin ψ| ≈ 0.93`).

`ψ` 가 측정 가능한 단일 스칼라라 검증이 쉽고, 벤치마크를 걸지 않아도 방어된다.

**주장 불가**

- "이 모델은 STDP 를 구현한다" — 스파이크가 없다
- "시간 순서 반대칭을 구현한다" — 반대칭 구조는 온전하나(§5.3),
  **부호를 정하는 것은 시간이 아니라 내용**이므로 고전 STDP 와는 다르다
- "생물학적 가소성 규칙이다" — post 항이 없다 (델타를 넣기 전까지)
