# 복소 선형어텐션의 위상 주소지정 — 실험 기록

작성 2026-08-09. 대화내역1·2 및 초기구상.txt에서 출발한 아키텍처 탐색의 전체 기록.
성공뿐 아니라 **틀렸던 것, 실패한 것, 선점당한 것**도 남긴다.

---

## 0. 한 장 요약

| 결과 | 상태 |
|---|---|
| 순수 복소 선형어텐션(Model 0)이 MLP 없이 3차 비선형성을 내재 | 검증됨 |
| Model 0은 LTI 전이라 내용 주소지정이 약함 → selective copy 92.8% 천장 | 확정 |
| 입력 의존 **위상**(Model 1)이 그 천장을 뚫음 → 100% | 확정 |
| 최적 위상 코드를 닫힌 형태로 유도(`위상차 = c·N`)하고 실측 확인 | 확정 |
| 깊이 ↔ 위상은 교환 가능한 자원 | 확정 (두 아키텍처에서 재현) |
| 바이트 LM에서 파라미터 통제 후에도 Model 1 우위 | 확정 (단일 시드) |
| **Mamba-3(ICLR 2026)가 같은 위상 코드를 학습함** | 확정 |
| FHN 흥분계 네 요소가 모두 존재하고 작동 | 확정 ([EXCITABILITY.md](EXCITABILITY.md)) |
| 핵심 재귀식과 "RoPE trick"은 Mamba-3에 선점됨 | **선점 확인** |

핵심 문장:

> 데이터 의존 회전 SSM이 *무엇을* 학습하는지에 대한 닫힌 형태의 답을 유도했고,
> 우리 최소 모델과 Mamba-3 양쪽에서 그 코드가 실제로 구현됨을 채널 단위로 확인했다.

---

## 1. Model 0 — 최소 복소 선형어텐션

### 1.1 설계

```
z_t = W_C h_t                        (C^p)
q_t = e^{+iψ/2} ⊙ z_t
k_t = γ ⊙ e^{-iψ/2} ⊙ z_t            γ = sqrt(1-|λ|²)
S_t = Λ S_{t-1} + k_t h_tᵀ           Λ = diag(λ), λ_j = e^{-α_j + iθ_j}
f_t = p^{-1/2} · Re[q_t† S_t]
h^(r+1) = h^(r) + f^(r)/R            weight-tied, R회
```

학습 파라미터는 `{W_C, α, θ, ψ}` 넷뿐(블록 16,576개). R을 늘려도 증가하지 않는다.
MLP·softmax·norm·gate·bias·위치임베딩 없음. 위치 정보는 `λ^{t-n}`이 전담.

원 구상(초기구상.txt) 대비 변경 두 가지:
1. 시간 스케일 `1/t` 제거
2. 유니터리 전이 `e^{-iθ}` → 감쇠 전이 `e^{-α+iθ}`

`1/t`를 쓰면 이득이 시퀀스 위치에 의존한다. 감쇠로 바꾸면 **시간불변**이 된다
(같은 패턴을 t=2에 두든 t=40에 두든 출력이 비트 단위로 동일함을 확인).

### 1.2 검증 (`check_model0.py`)

| 항목 | 결과 |
|---|---|
| parallel ≡ recurrent ≡ step | 3.1e-15 |
| T=1에서 3차 동차성 `f(c·h) = c³f(h)` | 정확 (c=0.5/2/3에서 오차 <1e-14) |
| 감쇠비 `a(Δ+1)/a(Δ) = \|λ\|` | 0.900000 (이론값 0.9) |
| 시간불변성 | 0.000e+00 |
| 역전파 | 네 파라미터 전부 유한 |

### 1.3 초기화 임계 스케일 (중요)

norm이 없고 자기항이 3차라 초기화에 임계점이 있다. `W_C` 배율별 순전파 1회
디리클레 에너지 증폭률:

| 배율 | 0.25 | 0.5 | 0.71 | 1.0 | 1.5 |
|---|---|---|---|---|---|
| E_D 비 | ×1.03 | ×1.18 | ×1.56 | ×5.1 | **×694,495** |
| max\|a_tt\| | 0.06 | 0.30 | 0.94 | 6.6 | 1656 |

기본값을 `w_scale=0.5`로 고정. 이 취약성은 나중에 LM 실험에서 다시 나타난다(§5.2).

### 1.4 진단량 정정

문서(대화내역2 §14)의 `ρ = ‖HAH‖₂ ≶ 1` 기준은 출력이 `O = AV`일 때의 것이다.
Model 0은 residual Euler(`h + f/R`)이므로 실제 임계 연산자는 `I + A/R`이다.
초기값에서 `ρ=0.37`인데 `ρ_R=1.06`으로 부호가 반대로 나온다. `euler_gain()`을 따로 둠.

---

## 2. selective copy — Model 0

### 2.1 태스크

```
[_, _, D3, _, _, _, D7, _, ..., D1, _, _, M, M, M, M]
        랜덤 위치 데이터 4개                마커 4개
i번째 마커에서 i번째 데이터 토큰 출력
```

`l_noise=32, n_mem=4, n_data=16` → `T=36, V=18`, chance = 6.25%.
Mamba 논문 Table 1의 표준 시험. 두 능력을 분리 요구:
(1) 내용 기반 쓰기, (2) 내용 기반 읽기(시간 거리가 매 샘플 랜덤).

### 2.2 결과

| 설정 | tok | seq | 학습 후 ρ_R | E_D 궤적 |
|---|---:|---:|---|---|
| Model 0 R=1 | **0.366** | **0.008** | 24.1 | 1,558 → 158,989 |
| Model 0 R=2 | 0.916 | 0.779 | 7.7 → 53.9 | 967 → 6,900,000 |
| Model 0 R=4 | 0.928 | 0.777 | 1.85 → 1.22 | 931 → 587 **감소** |
| Model 0 R=8 | 0.915 | 0.725 | 1.70 → 1.21 | 1,132 → 666 **감소** |
| Model 0 fixed 위치 (LTI 통제군) | 1.000 | 1.000 | 2.34 → 2.12 | 1,514 → 11,102 |
| softmax attention 기준선 | 1.000 | 1.000 | — | — |

**읽는 법**
- 고정 위치에서 100%가 나오므로, 격차의 원인은 모델의 약함이 아니라 **랜덤 위치
  주소지정**으로 특정된다.
- R=1은 실패하지만 **content-aware 쓰기는 학습했다** (blank의 `‖z‖`를 6.1로 눌러
  데이터 26.9의 1/4). 실패 원인은 정확히 **읽기 주소지정**이다.
- R≥2는 반대로 blank를 억제하지 **않는다** (`‖z‖` blank 21.7 > data 17.2).
  blank가 순서 카운터를 나르는 매체가 되기 때문.
- R=1,2는 이득을 24~54까지 올려 폭주하며 풀고, R=4,8만 `ρ_R≈1.2`에서 E_D가
  **감소**하는 소산 영역에 머문다. `ρ_R>1`인데 E_D 감소는 모순이 아니다 —
  ρ_R은 최악 모드 이득이므로 **대부분 소산 + 특정 모드만 증폭**을 뜻한다.
  대화내역2 §6~7의 "구조화된 보강간섭"이 CE만 최소화한 결과로 나타난 것.

### 2.3 메커니즘 분해 — 측정 방법을 한 번 틀렸다

처음엔 개별 `A^(r)` 행에서 마커가 정답 데이터에 몰리는지 봤다. **안 몰렸다**
(질량 0.074, 균등 0.031, argmax 적중 15%).

**측정이 틀렸다.** residual Euler에서 실제 토큰 혼합은 개별 `A`가 아니라 합성 연산자다:

```
M = Π_r (I + A^(r)/R)
```

`M`으로 다시 재니 대각선이 나왔다 (마커 i가 데이터 4개에 배분한 질량, chance 0.25):

| | D0 | D1 | D2 | D3 |
|---|---|---|---|---|
| marker 0 | **0.553** | 0.235 | 0.086 | 0.125 |
| marker 1 | 0.222 | **0.444** | 0.245 | 0.089 |
| marker 2 | 0.106 | 0.232 | **0.431** | 0.231 |
| marker 3 | 0.111 | 0.091 | 0.238 | **0.560** |

부드러운(soft) 주소지정이고, 인접 항목 누출 22~24%가 92.8% 천장의 원인.

순서 주소가 어디서 오는가: **재귀 깊이가 만든다.** blank 위치의 `h`에서
"앞에 나온 데이터 개수"를 선형 프로브로 복원하면 r=0에서 모든 blank가 동일(정보 0),
r=1에서 R²=0.58. (무작위 초기화 기준선 0.39와 차이가 크진 않아 이 증거만으로는 약하다.)

---

## 3. Model 1 — 구동 해밀토니안

### 3.1 유도

초기구상.txt의 QM 해석은 전이를 `Û(θ) = exp(−iθĤ)`로 쓴다. 물리에서 계에 입력이
들어온다는 것은 감쇠 조절이 아니라 **해밀토니안에 항이 붙는 것**이다:

```
Ĥ_t = Ĥ_0 + V̂(x_t)   →   U_t = exp(−i(θ + Δθ(x_t)))
```

`|U_t| = e^{-α}`는 그대로이므로 **입력이 조절하는 것은 감쇠가 아니라 위상 속도**다.
Mamba/GLA/RWKV 계열(선택적 망각)과 달리 정보가 지워지지 않는다.

누적 위상 `Φ_t = Σ_{m≤t} ω_m`을 쓰면

```
Π_{m=n+1}^{t} U_m = exp(−α(t−n)) · exp(−i(Φ_t − Φ_n))
```

**시간 거리 `(t−n)θ` 자리에 내용 가중 거리 `Φ_t − Φ_n`이 들어온다.**
`Δθ`가 데이터 토큰엔 크고 빈칸엔 0이면, 이 값은 **사이의 데이터 토큰 개수를 센 값**이다.

### 3.2 selective copy의 닫힌 형태 해

`Δθ(blank)=0`, `Δθ(data)=c`, `Δθ(marker)=c_M`이라 두면, 마커 i와 정답 데이터
`D_{i+1}` 사이의 누적 위상차는

```
Φ_{L+i} − Φ_n = c(3−i) + c_M(i+1)
```

**`c_M = c`로 두면 = 4c.** `i`가 사라진다. 데이터 위치가 어떻게 랜덤하게 흩어져도
정답까지의 위상차가 상수다. 공진 조건 `φ_q − φ_k = 4c` 하나로 모든 i, 모든 배치에서
동시에 만족된다. **재귀도 MLP도 없이 단일 레이어로 정확해가 존재한다.**

일반화: `n_mem = N`이면 **`c·N`**. 그리고 순서들이 별칭 없이 구분되려면 **`c·N < 2π`**.
이 용량 한계는 문헌에 언급된 적이 없다.

수치 확인: 데이터 위치를 `[1,4,9,17]`/`[0,11,12,23]`/`[5,6,7,8]`로 바꿔도 위상차가
전부 `4.00c (mod 2π)`, 코사인 커널값도 전부 −0.500.

### 3.3 구현

```
Δθ_t = s ⊙ sigmoid(W_θ h_t + b)      s, b ∈ R^p,  W_θ ∈ R^{p×d}
Φ_t  = cumsum(θ + Δθ_t) mod 2π       (float64 누적 후 wrap)
S_t  = diag(e^{−α − i(θ+Δθ_t)}) S_{t-1} + k_t h_tᵀ
```

게이트를 `softplus`가 아니라 **`sigmoid`**로 둔 이유: 유도가 요구하는 해는
"빈칸 정확히 0, 데이터 정확히 c"라는 이산 게이트다. softplus는 위로 무제한이고
0을 내려면 로짓이 −∞여야 한다. `s·sigmoid`는 `[0,s]`로 묶여 게이트 해석이 정확해진다.

통제: **`s=0`이면 Model 0과 수치적으로 동일(1.1e-15).** 게이트 바이어스를 −2로
초기화해 학습 시작 시점에도 `Δθ≈0`이므로, 성능 차이를 초기화가 아닌 **게이트 학습**
탓으로 돌릴 수 있다.

부수 효과: `|e^{−iΦ}| = 1`이라 `Φ`를 `q,k`에 흡수시키면 남는 커널이 실수 감쇠뿐이라
전 과정이 실수 연산이 된다. 실측 **Model 0보다 2배 빠름**.
(단 이 트릭은 Mamba-3의 "RoPE trick"으로 이미 알려져 있다 — §6 참조.)

### 3.4 결과

| | Model 0 | **Model 1** |
|---|---|---|
| R=1 | 0.366 / 0.008 | **1.000 / 1.000** |
| R=4 | 0.928 / 0.777 | **1.000 / 1.000** |

R=1은 2,000스텝에 loss 정확히 0. 파라미터는 +8,320개(+17%).

### 3.5 학습된 위상 코드

주소 채널은 **64개 중 2개**. 각분리가 정확히 π (오답을 정반대 위상으로 밀어
코사인 커널에서 부호를 뒤집는다).

| 채널 | θ | Δθ(blank) | **ω(blank)** | Δθ(data) | ω(data) | 집중도 R |
|---|---|---|---|---|---|---|
| 21 | 0.045 | 0.0005 | **0.045 ≈ 0** | 2.702 | 2.747 = c | 0.947 |
| 20 | 4.885 | 1.383 | **6.268 ≈ 2π ≡ 0** | 0.010 | 4.895 = c | 0.993 |

**같은 해를 두 가지 파라미터화로 찾았다.** 채널 21은 게이트를 닫아서, 채널 20은
게이트를 끝까지 열어 정확히 2π까지 채워서 `ω(blank) ≡ 0`을 만든다.
§3.2에서 "2π 되감김"을 제약으로만 적었는데, 모델은 그것을 **해를 하나 더 만드는
자유도**로 썼다.

정량적으로도 맞물린다: 잔차 `|ω(blank) mod 2π|`가 채널 20은 0.0148, 21은 0.0454인데
집중도가 각각 0.993, 0.947. **잔차가 작을수록 주소가 정확하다.**

**내 예측이 부정확했던 점**: `Δθ`가 0이어야 한다고 썼지만 실제 조건은
`ω = θ + Δθ`가 mod 2π로 0이다. 유도할 때 `θ`를 빠뜨렸다.

### 3.6 깊이 ↔ 위상 자원 교환

| | R=1 | R=4 |
|---|---|---|
| 정확도 | 1.000 / 1.000 | 1.000 / 1.000 |
| 주소 채널 수 | **2** | **1** |
| 단일 커널 분리도 (Cohen's d) | **8.00** | 3.08 |
| 합성 어텐션 질량 (정답) | 0.68–0.73 | **0.73–0.79** |
| ρ_R | **16.3** | 1.53 → 1.24 |
| E_D | 1,661 → 104,477 | 1,299 → 1,155 |

R=1은 한 번에 끝내야 해서 채널 2개 + 극단적으로 날카로운 커널을 쓰고 이득이
16배로 폭주한다. R=4는 채널 1개 + 무딘 커널인데 4번 누적하니 최종 주소지정이
오히려 더 정확하고 동역학은 얌전하다.

> 위상 주소가 생기자 R=4는 남는 깊이를 정확도를 쥐어짜는 데 쓰지 않고 **폭주를 피하는 데** 썼다.

---

## 4. 시험 B — 가변 `n_mem` (깊이↔위상의 반대 방향)

§3.2의 유도는 공진 오프셋이 `c·N`이다. R=1의 `φ_q`는 상수이므로 **단 하나의 N에만**
맞출 수 있다. 예측: **가변 N은 R=1을 깨고 R≥2가 복구한다.**

| | R=1 | R=4 |
|---|---|---|
| 고정 N=6 | 1.000 / 1.000 | 1.000 / 1.000 |
| 가변 N∈[2,6] | **0.696 / 0.309** | **0.988 / 0.955** |

예측 적중. 이로써 **깊이↔위상 상보성이 양방향 확인**됐다 —
위상은 깊이가 하던 순서 주소지정을 대체하고(§3.4), 깊이는 위상이 못 하는
N 적응을 담당한다.

---

## 5. 바이트 단위 언어모델링

selective copy는 순서 위상 주소지정이 **정확히 최적해**인 태스크다(§3.2에서 닫힌
형태로 유도할 수 있었던 것이 증거). 따라서 거기서 이겼다는 것은 "메커니즘이 실재한다"의
증거이지 "쓸모 있다"의 증거가 아니다. **유도된 해가 없는** 태스크에서 재봤다.

코퍼스: Python 표준 라이브러리 소스 8MB, 바이트 단위(V=256).
설정: `d=128, R=4, T=128`, 6,000스텝, 동일 옵티마이저.

### 5.1 결과

| 모델 | 총 파라미터 | 최저 val bpb |
|---|---:|---:|
| **Model 1** | 57,664 | **3.0755** |
| Model 0 (p=96, **파라미터 통제군**) | 57,632 | 3.2016 |
| Model 0 (p=64, 원본) | 49,344 | 3.1733 |
| softmax attention 기준선 | 49,344 | 3.4802 |

**파라미터를 맞춘 Model 0이 Model 1을 못 따라잡는다.** 오히려 p=64보다 살짝 나쁘다
(시드 노이즈 안쪽이라 사실상 동률). 격차 0.098~0.126 bpb는 파라미터가 아니라
**메커니즘**에서 온다.

시드 노이즈 추정 (부분 데이터): model0 @1000 = 3.5369/3.5676/3.5534 (σ=0.015),
model1 @2000 = 3.3318/3.2903/3.2768 (σ=0.029). 격차는 그 3~6배.

### 5.2 실패 기록 두 개

**① 검증 분할을 잘못 잡았다.** 파일을 경로순 정렬해 뒤쪽 500KB를 잘랐더니 val이
`pydoc_data/topics.py` 한 파일 — 코드가 아니라 산문이었다. 고유 바이트 121 대 170,
비ASCII 5배. 블록 인터리브 분할로 수정.

**② 그 OOD 분할에서 Model 1만 발산했다.**

```
step   train      val bpb
 500   2.7063      3.8744
1000   2.4788      4.6e10      ← train은 계속 내려가는데 val만
1500   2.1119      9.5e12
```

같은 val에서 attn 3.61, Model 0 3.93. 분할을 고치니 정상 학습됐지만,
`max logit`이 Model 0은 782, Model 1은 477까지 자란다(attn은 15에서 안정).
**정규화 없는 설계 전반의 특성**이며 §1.3의 3차항 임계 스케일이 실제 데이터에서
드러난 것. Model 1 고유 문제는 아니고 오히려 Model 1 쪽이 덜 심하다.

---

## 6. Mamba-3와의 관계 — 선행연구 확인

`arXiv:2603.15569` (2026-03-16, ICLR 2026), Lahoti·Li·Chen·Wang·Bick·Kolter·Dao·Gu.

### 6.1 선점된 것 (정직하게)

Proposition 2, 식 (9):
```
h_t = e^{Δ_t A_t} · R_t · h_{t-1} + Δ_t B_t x_t      R_t = Block{R(Δ_t θ_t[i])}
```
**우리 Model 1의 전이식과 같다.**

Proposition 3, 식 (10) — 논문이 **"RoPE trick"**이라 부르는 것:
```
h_t = e^{Δ_t A_t} h_{t-1} + (Π R_iᵀ) Δ_t B_t x_t,   y_t = [(Π R_iᵀ) C_t]ᵀ h_t
```
누적 회전을 B(key)와 C(query)에 흡수. **§3.3에서 내가 차별점이라 주장한
`cumsum` 인수분해와 동일한 수학이다.** 이 부분은 물러설 게 없다.

동기 구조도 유사하다 — 그쪽은 "실수 고유값으로는 회전 동역학을 표현 못 해 parity
같은 state-tracking을 못 푼다", 우리는 "LTI 전이로는 순서 주소지정을 못 한다".

### 6.2 구조적 차이 (실재함)

| | Mamba-3 | Model 1 |
|---|---|---|
| MLP | **SwiGLU 교대 배치** (§3.4, Llama 식) | 없음 |
| 정규화 | BCNorm/QKNorm + pre-norm | 없음 |
| 바이어스 | B·C 채널별 학습 바이어스 | 없음 |
| 깊이 | 서로 다른 레이어 스택 | **weight-tied 재귀 (Euler 적분)** |
| 사영 | B, C 별도 | **`W_C` 하나 + `±ψ/2`** |
| Value | `B_t x_t` | **`h_t` 자체 (W_V 없음)** |
| 감쇠 | 입력 의존 `Δ_t A_t` | 고정 `α` |
| 위상 증분 | `tanh(angle)·Δ_t·π` — **부호 있음** | `s·sigmoid(·)` — **비음수 단조** |
| 회전 범위 | `rope_fraction=0.5` (상태의 절반만) | 전 채널 |
| 겨냥 능력 | state-tracking (parity, 모듈러 산술) | **순서 내용 주소지정** |

곁가지: 대화내역1은 "맘바가 MLP를 교대 배치한다"고 했고 대화내역2가 "Mamba 원 논문은
별도 MLP 블록이 없다"고 정정했는데 — **Mamba-3에 대해서는 대화내역1이 맞다.**

공식 구현(`state-spaces/mamba@mamba_ssm/modules/mamba3.py`)에서 확인:
- `Angles_Cumsum = cumsum(Angles * DT) mod 2π` (`mamba3_siso_combined.py:315`)
- `angle_inc = tanh(angle_proj) * dt * π` (`mamba3_mimo_rotary_step.py:75`)

**`mod 2π`가 공식 커널의 일급 시민이다** — §3.2에서 유도한 모듈러 구조와
`c·N < 2π` 용량 한계가 Mamba-3에 그대로 적용된다는 근거.

### 6.3 유도 경로가 다르면 방향도 달라진다

구동 해밀토니안 + 순서 카운터 유도는 **모듈러 조건**을 낳는다
(`ω(blank) ≡ 0 mod 2π`, `c·N < 2π`). SSM 이산화 유도에서는 자연스럽게 나오지 않는
질문이다 — 그쪽은 위상을 표현력 자원으로 보지 셈 좌표로 보지 않는다.

그리고 그 시각이 실제로 결과를 낳았다: 채널 20이 게이트를 열어 빈칸 위상을 정확히
2π까지 채운 것을 찾은 건, mod 2π를 보라는 유도가 있었기 때문이다. 그 유도 없이
로그만 봤으면 "빈칸이 제일 크네, 실패했나" 하고 넘겼을 것이다(실제로 처음엔 그랬다).

---

## 7. Mamba-3가 같은 위상 코드를 학습하는가 — 확인

공식 구현은 Triton/CuteDSL 커널이라 그대로 못 돌린다. 파라미터화를 그대로 따라
순수 PyTorch로 재현(`mamba3_ref.py`). 단순화: exponential-Euler(Prop 2/3) 사용,
MIMO 미사용, 청크 커널 대신 시퀀스 루프. 위상 메커니즘 자체는 동일.

### 7.1 L=1 (161k 파라미터)

selective copy **1.0000 / 1.0000**. 각도 채널 8개 **전부가 주소 채널**:

| ch | 정답 집중도 R | 정답 방향 | 오답 R | **각분리** | 점수 |
|---|---|---|---|---|---|
| 3 | 0.9996 | +0.807 | 0.312 | **3.1416** | 1.311 |
| 5 | 0.9993 | −0.832 | 0.311 | **3.1416** | 1.310 |
| 6 | 0.9991 | −0.828 | 0.311 | **3.1416** | 1.310 |
| 7 | 0.9992 | +0.833 | 0.311 | **3.1416** | 1.310 |
| 1 | 0.9999 | −0.886 | 0.309 | **3.1415** | 1.309 |
| 2 | 0.9999 | +0.892 | 0.309 | **3.1415** | 1.308 |
| 0 | 0.9999 | −0.898 | 0.308 | **3.1415** | 1.308 |
| 4 | 0.9972 | +2.128 | 0.070 | 3.1351 | 1.067 |

집중도 0.999는 우리(0.947~0.993)보다 깨끗하고, 각분리 π가 7/8 채널에서 소수점
넷째 자리까지 일치한다.

### 7.2 증분 프로파일 — 동일한 해임을 확정

| ch | inc blank | inc data | inc marker | \|blank\| mod 2π | \|data−marker\| |
|---|---:|---:|---:|---:|---:|
| 0 | −0.0025 | −1.8114 | −1.8331 | **0.0025** | **0.0217** |
| 1 | −0.0022 | −1.8113 | −1.8331 | 0.0022 | 0.0217 |
| 2 | +0.0024 | +1.8115 | +1.8332 | 0.0024 | 0.0217 |
| 3 | −0.0027 | +1.8113 | +1.8259 | 0.0027 | 0.0145 |
| **4** | +0.0016 | **−0.9905** | **−1.0342** | 0.0016 | 0.0438 |
| 5 | +0.0027 | −1.8118 | −1.8324 | 0.0027 | 0.0206 |
| 6 | +0.0027 | −1.8095 | −1.8330 | 0.0027 | 0.0235 |
| 7 | −0.0027 | +1.8112 | +1.8331 | 0.0027 | 0.0219 |

§3.2의 유도해는 `Δθ(blank)=0`, `Δθ(data)=Δθ(marker)=c`였다.

- **빈칸 0.002 — 데이터(1.81)보다 세 자릿수 작다.** 사실상 0.
- **데이터 = 마커, 차이 1% 이내.**

**통계적 유사성이 아니라 동일한 해다.** 8채널 전부.

부수 관찰 둘:
- 채널 4만 `c ≈ 0.99`로 나머지(1.81)의 **절반** → **다중 해상도 시계**가 실제로 나타남
  (문서 §7에서 예측했던 것).
- 부호가 ± 쌍으로 갈림(−1.81 5개, +1.81 3개) → `tanh` 증분(부호 있음)의 서명.
  우리 `sigmoid`(비음수)는 한쪽 부호만 나왔다. **귀납 편향 차이가 관측됨.**

### 7.3 L=4 (637k) — 깊이↔위상 교환의 독립 재현

selective copy **1.0000 / 1.0000**. 주소 채널이 **32개 중 6개**로 줄었고
**전부 layer 0**에 있다(집중도 0.935~0.983, 각분리 π).
layer 1 이후의 위상 채널은 각분리 ≈ 0(0.0007~0.0047)으로 주소 정보를 안 나른다.

| | Mamba-3 L=1 | Mamba-3 L=4 |
|---|---|---|
| 주소 채널 | **8 / 8 (100%)** | **6 / 32 (19%)** |
| 최고 집중도 | 0.9999 | 0.9828 |
| 최고 점수 | 1.311 | 1.263 |

§3.6에서 Model 1에서 발견한 **"깊이가 있으면 위상 자원을 덜 쓴다"가
다른 아키텍처에서 독립적으로 재현됐다.**

---

## 8. 종합 판단

### 8.1 남는 기여

| | 상태 |
|---|---|
| 복소 데이터 의존 회전 전이 | 선점 (Mamba-3) |
| 누적 회전의 Q/K 흡수 병렬화 | 선점 ("RoPE trick") |
| **최적 위상 코드의 닫힌 형태 유도 + 채널 단위 검증** | **열림** |
| **MLP·norm 없이 위상만으로 내용 주소지정** | **열림** |
| **깊이 ↔ 위상 자원 교환** | **열림** (두 아키텍처에서 재현) |
| **위상 코드 용량 한계 `c·N < 2π`** | **열림** |
| 흩어진 분야의 연결 (R&F SNN / 데이터 의존 RoPE / 위상-언어) | 약하지만 위를 뒷받침 |

`Linear Attention Is All You Need` 류의 벤치마크 논문으로 갈 자리는 없다.
핵심 재귀식이 ICLR 2026 논문의 명명된 기법이기 때문. 하지만 **해석가능성 논문**으로는
성립한다:

> 데이터 의존 회전 SSM이 *무엇을* 학습하는지는 아무도 보지 않았다.
> 우리는 최적 위상 코드를 닫힌 형태로 유도하고, 학습된 망이 그것을 채널 단위로
> 실제로 구현함을 Model 1과 Mamba-3 양쪽에서 보인다.
> 그리고 재귀 깊이와 위상 자유도가 같은 계산을 두고 교환 가능한 자원임을 보인다.

이 프레이밍에서는 Mamba-3가 경쟁자가 아니라 **분석 대상**이 된다.

### 8.2 막전위 동역학 해석 — 흥분성은 실재한다

> **주의.** 이 절은 한 번 정반대로 썼다가 정정했다. 어떻게 틀렸는지와 재현 방법은
> **[EXCITABILITY.md](EXCITABILITY.md)** 에 따로 정리했다. 분석하는 쪽이 같은 함정에
> 빠지기 매우 쉬운 지점이다.

블록 출력은 항등적으로 두 항으로 갈린다.

```
S_t = Λ S_{t-1} + k_t h_tᵀ
f_t = p^{-1/2} Re[q_t† S_t] = M(S_{t-1}) h_t  +  a_tt h_t
                              └─ 선형 ─┘        └─ 3차 ─┘
```

`q_t` 가 `h_t` 에 선형이고 `S_{t-1}` 은 과거만의 함수이므로 첫 항은 **`h_t` 에 정확히
선형**이며, 계수 행렬이 대화내역2 §3 의 `λ(S_{t-1})` 이다.

**FHN 네 요소가 모두 존재한다.**

| # | FHN | 이 구조 | 실측 |
|---|---|---|---|
| 1 | `−u³/3` 포화 | `a_tt h_t` | `a_tt(data) = −0.61` (포화 부호) |
| 2 | `+u` 불안정화 | `M(S_{t-1}) h_t` | `max Re λ(M) > 0`, **96~100%** |
| 3 | 느린 회복변수 | `S_t` 가 다음 이득을 조절 | 구조적 |
| 4 | 시간척도 분리 | `τ = 1/α` | 2.4 ~ 2286 |

`M` 은 autograd 야코비안과 4e-16 이내로 일치한다 (`analyze_excitability.py --verify`).

```
     토큰    max Re λ(M)      >0 비율
    blank         0.2155       96.88%
     data         0.0894       96.88%
   marker         0.1272      100.00%
```

②가 **상수가 아니라 기억의 함수**라는 점에서 FHN 보다 풍부하다 — FHN 의 `+u` 는
고정 계수지만 여기서는 이득 자체를 `S` 가 조절한다.

**3차항의 부호는 대화내역1 과 반대다.** FHN 에서 폭발은 1차항이 만들고 3차항이
**멈춘다**. 대화내역2 §6 의 지적대로이며, 학습된 모델은 FHN 이 맞는 부호를 골랐다.
게다가 부호가 내용 의존이다 — `a_tt` 가 blank +0.160, data **−0.609**, marker −0.135
로, 데이터 토큰이 억제성 채널(`cos ψ_j < 0`)을 선택적으로 켠다.

**아직 관측되지 않은 것:** 문턱 통과, 전부아니면전무, 유한 피크와 반전, 명시적 리셋.
흥분 *기질* 이 있다는 것과 스파이크가 *일어난다* 는 것은 다르다. 다만 selective copy
는 이산 발화를 요구하지 않는 태스크이므로(부드러운 위상 정합만으로 100%), "구조는
있고 태스크가 안 시켰다"로 읽는 편이 정합적이다. 반증 형태: **출력 대역폭을 좁히거나
상태 전달에 노이즈를 넣어 인공 "누설 케이블"을 만들면** 전부아니면전무 코드가 나와야
한다.

### 8.3 신뢰 수준 — 정직하게

- LM은 **단일 시드**다. 시드 노이즈는 0.015~0.03으로 추정했고 격차는 3~6배지만,
  완전한 시드 반복은 못 했다.
- `d=128`, 6,000스텝, 8MB — **장난감 규모**다.
- LM의 softmax attention 기준선이 **약하다**(MLP 없는 tied 단일헤드).
  "선형 어텐션이 트랜스포머보다 낫다"로 읽으면 안 된다.
- Mamba-3 재현은 exponential-Euler 단순화 버전이다. trapezoidal 3항 재귀와 MIMO는 없다.

### 8.4 다음에 할 것

1. **진짜 기준선** — Transformer(+MLP), 공식 Mamba-3, GLA를 파라미터/FLOP 맞춰서
2. **시드 3개 이상**, 한 자릿수 이상 스케일업
3. **MQAR** (multi-query associative recall) — 표준 벤치마크이고 우리 메커니즘이
   정면으로 겨냥하는 태스크
4. **`c·N < 2π` 용량 한계 검증** — 공식 Mamba-3에 그대로 적용 가능한 예측
5. **신규성 확인** — resonate-and-fire SNN 계열(PRF, C-SiLIF, dendritic R&F),
   복소/회전 SSM, PRISM(위상-언어, ICML 2026)

---

## 부록: 파일

| 파일 | 내용 |
|---|---|
| `model0.py` | Model 0 블록·모델·진단량 (`dirichlet_energy`, `spectral_gain`, `euler_gain`, `self_gain`) |
| `model1.py` | Model 1 (구동 해밀토니안). `driven=False` 또는 `s=0`이면 Model 0으로 환원 |
| `check_model0.py` / `check_model1.py` | 등가성·3차 동차성·감쇠·γ 스케일·4c 불변량 검증 |
| `selective_copy.py` | 태스크 (고정/랜덤 위치, 가변 `n_mem`) |
| `train_selective_copy.py` | 학습 + softmax attention 기준선 |
| `analyze_selective_copy.py` | 합성 연산자 주소지정 분해, 순서 카운터 선형 프로브 |
| `analyze_phase.py` | 위상 불변량 원형 통계 (집중도 R, 각분리, 판별 점수) |
| `analyze_membrane.py` | 확산/반응 분해, 토큰 종류별 이득, 무의미 vs 유의미 입력 |
| `analyze_excitability.py` | 기억 의존 선형 이득 `M(S)` 의 스펙트럼 (autograd 검증 포함) |
| `charlm.py` | 바이트 LM (블록 인터리브 분할) |
| `mamba3_ref.py` | Mamba-3 SISO 순수 PyTorch 재현 |
| `train_mamba3.py` | Mamba-3 학습 + 위상 코드 측정 + 증분 프로파일 |
