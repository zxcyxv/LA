# 2026-08-11 — 망각 곡선과 R&F 재프레이밍

하루치 작업 기록. 전부 **학습 불필요**하고 재현된다.

## 문서

| 파일 | 내용 |
|---|---|
| **[CORRECTION_mamba3.md](CORRECTION_mamba3.md)** | **`mamba3_ref.py` 에 연산자 우선순위 버그 — `A` 가 상수가 되어 감쇠가 죽어 있었다. Mamba-3 와의 모든 대조가 무효.** 오늘 가장 중요한 것 |
| [FORGETTING.md](FORGETTING.md) | 망각 곡선. `MISALIGNMENT §10` 의 "계산도 측정도 하지 않았다" 를 닫는다. **§12~§17 에서 §5·§6 이 뒤집혔으니 머리말의 경고를 먼저 읽을 것** |
| [RESONANCE.md](RESONANCE.md) | 이 구조는 STDP 가 아니라 **resonate-and-fire** 다. "두 뉴런" 이 어디 있는지 물어서 나온 것 |

## 스크립트

```bash
python diag_forgetting.py     # [A]~[F] 신호 포락선, ~3초
python diag_snr.py            # [S1]~[S6] SNR·기억 수명·β 다이얼, ~4분
python diag_tradeoff.py       # [T1]~[T3] 가소성-안정성 거래, ~2분
python diag_resonance.py      # [R1][R2] 결맞음·주파수 응답, ~1분
python check_stdp_under_C.py  # 갈래 C 가 위상 해석을 깨는지, ~1분
```

레포 루트를 `sys.path` 에 자동으로 넣으므로 이 폴더에서 그대로 실행된다.

`drafts/` 는 `diag_forgetting.py` 로 통합되기 전의 습작이다. **`diag_forgetting.py` 가
전부 대체하므로 참고용으로만 남긴다.**

## 이날 확정된 것

| | |
|---|---|
| §6.4 의 느린 저장소 `W` 는 **거듭제곱을 못 준다** — 지수 두 개의 가중합 (항등식) | FORGETTING §2 |
| 레포 기본 초기화가 **이미 거듭제곱**이다 (β = 1.485 ± 0.019) | FORGETTING §4 |
| **유효 기억 수명** 이라는 단일 지표가 생겼다 — 현재 기본값 **80 토큰** | FORGETTING §12 |
| 잡음은 `Δ` 에 무관하다(지수 −0.007) → **SNR 이 신호의 β 를 물려받는다** | FORGETTING §13 |
| **`r_max` 넓히기는 무효**(80/80/80). 긴 채널의 *개수* 가 안 늘기 때문 | FORGETTING §14 |
| **β 다이얼로 수명 80 → 500 (6.25배).** 파라미터·상태 0 개 | FORGETTING §16 |
| 그리고 그것은 **거래**다 — 최근 −26%, 과거 +57%, 총합 −18% | FORGETTING §15.1 |
| **장거리 예산에 천장이 있다 — `∫SNR dlogΔ (Δ>32) ≈ 4.8`.** 현재는 절반(2.44)만 씀 | FORGETTING §16.1 |
| **기억 수명: 우리 192 토큰 vs Mamba-3 32~48. 파라미터 1/23** (둘 다 학습 후) | CORRECTION §4.2.1 |
| 단 곡선 모양이 **정반대** — Mamba-3 는 최근 강세(7.60), 우리는 장거리 강세 | CORRECTION §4.2.1 |
| 외삽: 버그 수정 후 Mamba-3 가 128 에서 0.062 → **0.707**. 우리(0.973)가 여전히 앞섬 | CORRECTION §4.1.1 |
| 전이식이 **R&F 방정식 그 자체**다. "두 뉴런" 은 없고 **한 채널의 두 시각**이다 | RESONANCE §2 |
| **보강간섭이 스파이킹을 일으킨다** — 막전위 이득 2.0배 → 스파이크 이득 **8.1배** | RESONANCE §4 |
| **회전 주기(2.45) < 절대 불응기(3)** — 공진 대역이 막혀 있다 | RESONANCE §5 |
| 갈래 C 의 `gate_write` 는 위상 해석을 **안 깬다** (부호 보존 100%) | RESONANCE §6 |

## 이날 뒤집힌 것 (스스로)

| | |
|---|---|
| **`mamba3_ref.py` 의 `A` 계산** | **버그.** 연산자 우선순위로 상수화 + 부호 뒤집힘. `EXTRAPOLATION §6-3`, `RESULTS §7`, `CAPACITY §2·§5` **전부 재측정 필요** |
| `MISALIGNMENT §5.2` "병렬 뱅크 ≠ 캐스케이드, 차이는 승격 경로" | **반증** — 밀도가 맞으면 병렬 뱅크도 거듭제곱 |
| `FORGETTING §5·§11-2` "`r_max` 를 넓히면 기억이 길어진다" | **반증** — 수명 80/80/80 |
| `FORGETTING §6` 처방 "`logtau + γ·τ^{−1}` 로 β 복원" | **철회** — 장기기억엔 β 가 **낮아야** 좋다 |
| `EXTRAPOLATION §1` "`logtau` 는 반증(미미)" | **재판정 필요** — 수명을 2.5~6배 늘린다 |

## 다음

`FORGETTING.md §17` 참조. 요약하면 — **신호 쪽 다이얼은 다 돌렸고 천장을 봤다.
남은 방향은 잡음이고, 그것을 건드리는 것은 갈래 C 의 쓰기 게이트뿐이다.**
그리고 그 실험은 `MISALIGNMENT §9.9.4` 의 직접 사영 게이트 대조군과 **같은 런으로 둘 다
답한다.**
