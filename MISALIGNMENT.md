# 층위 어긋남 진단 — FHN / STDP / 헤비안과 어디서 갈라지는가

> **이 문서의 용도.** 이 구조가 생물학적 층위와 **어디까지 동형이고 어디서 한 칸씩
> 어긋나는지**를 잰다. 목표는 **막전위 동형 확보에 의한 장기기억 능력 향상**이며,
> 그 기준으로 기존 문서들의 정합성 주장을 다시 채점한다.
>
> **이 문서는 [PLASTICITY.md](PLASTICITY.md) §8 의 "델타 규칙 노선" 을 채택하지 않는다.**
> 그 결론은 "성능으로 이길 길" 을 기준으로 도출된 것이고, 목표가 막전위 동형이면
> 전제부터 다시 봐야 한다. §5 에서 그 이유를 사슬 형태로 제시한다.
>
> 측정 재현: `python diag_misalign.py` (학습 불필요, 1.7초). 표기 `[A]`~`[E]` 는 그 절 번호다.

---

## 0. 한 장 요약

| 발견 | 상태 |
|---|---|
| 어긋남 6개가 **하나의 제약**(병렬 스캔 = 상태 되먹임 금지)에서 나온다 | 해석 |
| FHN 네 요소 중 **② 만 온전**하다. ① 부호 미보장, ③④ 축 어긋남 | 확정 |
| `f(0) = 0` 이 모든 설정에서 항등적 → **안정 정지점(휴지 전위)이 없다** | 확정 `[A]` |
| 따라서 스파이크 부재는 태스크 탓이 아니라 **구조적 정리**다 | 확정 |
| 회복변수 `S` 는 깊이축에서 매번 0 으로 재생성된다 (`ε→∞`, FHN 의 정반대 극한) | 확정 `[E]` |
| 3차 포화항의 부호를 **보장하면 오히려 더 발산**한다 (로지스틱 오버슈트) | 확정 `[C]` |
| 자기항을 지수적으로 적분하면 순전파 에너지 증폭이 사라진다 | 확정 `[C]`, 초기화 시점 |
| **그러나 그 증폭이 학습 실패로 이어지지 않는다** — `w_scale` 재해석 **철회** | **반증** §8 |
| 개입 1 의 selective copy 열세(0.965)는 런 미완 + 태스크 부적합 — **판정 철회** | 철회 §8.2 |
| `ψ` 의 기하 서사(헤비안↔STDP 다이얼)가 기능적인지 **절제 실험이 한 번도 없다** | **미검증** §8.6 |
| **개입 1(`cos ψ<0`)이 LM d=512 의 첫 스텝 발산을 제거한다** — warmup 300 을 파라미터 0개로 대체 | **확정** §8.5 |
| 개입 2(지수 적분): 개입 1 **없이는 위험**(단독 `nan`), 개입 1 **위에서는 무조건 안전하나 측정 불가**(0.006 bpb) | 확정 §8.6 |
| 오일러 오버슈트의 분리선은 `\|a_tt\|/R = 2` 인데 운용점이 **0.268** — 개입 2 는 호출되지 않은 보험 | 확정 §8.6.1 |
| 기준선 발산은 `a_tt>0` 의 **양의 되먹임 + 문턱 부재**. 개입 1 이 음의 되먹임으로 바꾼다 | 확정 §8.6.1 |
| 기존 구조가 학습된 이유: 보호막 넷(`w_scale`·warmup·clip·**학습이 부호를 스스로 고침**) | 확정 §8.6.2 |
| **§4 격하**: ① 의 결함은 영구적 구조 결함이 아니라 **초기화 시점 경쟁 조건**이다 | **정정** §8.6.3 |
| §4 의 진단(발산 경로 = 자기항 부호)이 **(a) 자기항인지 (b) 커널 전체 위상 이동인지** 안 갈렸다 | **미결** §8.6 |
| selective copy 는 두 개입의 판정을 **모두 반대로** 만들었다 | 확정 §8.6 |
| 현재 흔적 커널은 동시성 창이 아니다 — 피크 lag 0, 40토큰에 부호반전 36회 | 확정 `[D]` |
| 흔적(`e^{-αΔ}`)과 시냅스가중(`q†S` 로 읽힘)이 **같은 변수**다 → 공고화가 들어갈 자리 없음 | 확정 |
| 장기기억까지의 사슬 4개 링크가 **전부** 끊겨 있고, 델타는 그중 **0개**를 고친다 | 해석 |
| 금지된 되먹임은 **깊이축 `r` 에서 합법**이다 — 병렬성을 깨지 않고 전부 통과 | **확정** §9 |
| **깊이축의 총 적분 시간은 항상 1** 이다 (`R` 은 해상도). 깊이축 FHN(갈래 A)은 원리적으로 불가 | **확정** §9.1 |
| **갈래 C 로 문턱·전부아니면전무·불응기·문턱하 누적이 모두 관측됐다** (학습 없이) | **확정** §9.3 |
| 완화 반복이 `R=4` 에서도 수렴한다 — 불응 입력억제가 진동을 죽인다 | 확정 §9.3 |
| 단 `h` 에 문턱을 만든 게 아니라 **새 변수 `m`** 에 만들었다. `[A]` 는 여전히 참 | 확정 §9.7 |
| 개입 3·4(깊이축 FHN)는 **폐기**. §6.3 설계는 §9.1 에 의해 무효 | 폐기 §9.7 |

핵심 문장:

> 생물학은 모든 층위를 **한 축**(물리적 시간) 위에서 돌린다. 이 모델은 두 축(`t`, `r`)에
> 나눠 놓았고, 어느 축도 필요한 짝을 **둘 다** 갖지 않는다.

---

## 1. 근본 원인 하나 — 병렬 스캔 제약

문서 전체를 관통하는데 한 번도 명시되지 않은 제약이 있다. `PLASTICITY.md §6.4` 가
(a)(b)(c) 를 "대각 유지 / 병렬" 로 채점하고, `CAPACITY.md §7.1` 이 "리셋 조건이 상태
의존이면 prefix scan 이 붕괴한다" 고 쓰고, 모든 효율적 선형 모델이 게이트를 입력
의존으로만 두는 이유를 설명하는 그 제약이다.

```
병렬 스캔 유지  ⟺  모든 항이 입력만의 함수  ⟺  상태 되먹임 금지
```

그런데 생물학의 해당 항들이 **전부 상태 되먹임**이다.

| 생물학 항 | 무엇에 되먹임하는가 | 제약 하에서 |
|---|---|---|
| post-synaptic 항 | 가중치가 만든 출력 | 금지 |
| 문턱 통과 | 막전위 자신 | 금지 |
| 공고화 승격 | 흔적을 읽어 느린 저장소에 씀 | 금지 |
| 방향 선택적 지우기 (델타) | 키 방향의 현재 저장값 | **청크 알고리즘으로 우회 가능** |

**즉 어긋남들은 개별 설계 실수가 아니라 한 제약이 만든 그림자다.** 그리고 델타 규칙이
선택된 이유가 여기서 드러난다 — 생물학적으로 옳아서가 아니라, **제약을 유지한 채
통과할 수 있는 유일한 문이기 때문**이다.

벤치마크를 우선하면 그 선택이 정합적이다. 목표가 막전위 동형이면 **제약 자체를
재검토해야 한다.** §6 에서 제약을 깨지 않고 되먹임을 합법화하는 길을 제시한다.

---

## 2. 층위 지도 — 어느 축에 무엇이 있는가

모델에는 축이 둘이다.

```
시퀀스축 t :  S_t = Λ S_{t-1} + k_t h_t^T        (스캔 내부)
깊이축   r :  h^(r+1) = h^(r) + f^(r)/R          (weight-tied 오일러 적분)
```

FHN 은 `u` 와 `v` 가 **같은 축**에서 결합되어야 성립한다. 그런데:

| | 막전위 `u` 가 적분되는가 | 회복변수 `v` 가 지연을 갖는가 | post 항이 있는가 |
|---|---|---|---|
| **시퀀스축 `t`** | ✗ — `h_t` 는 입력으로 주어짐 | ○ — `S_{t-1}` | ✗ |
| **깊이축 `r`** | ○ — `h ← h + f/R` | ✗ — `[E]` 매번 0 초기화 | ○ |

**어느 축도 짝을 둘 다 갖지 않는다.** `EXCITABILITY.md §6.2` 는 "두 축이 각각 절반씩
가지고 있다" 고 쓰고 넘어갔는데, 절반씩 갖는 것은 갖는 것이 아니다.

### 2.1 ④ 시간척도 분리는 정의되지 않는다

`EXCITABILITY.md §1` 은 ④ 를 `τ = 1/α` (실측 2.4~2286) 로 채웠다. 그런데 `τ` 는
**토큰 단위**이고 막전위 적분 스텝 `1/R` 은 **깊이 단위**다.

```
ε = (막전위 시간척도) / (회복 시간척도) = (1/R) / (1/α)     ← 분모와 분자가 다른 축
```

**비 자체가 정의되지 않는다.** ④ 는 실측값이 있어서 충족된 것처럼 보이지만, 재는 대상이
FHN 이 요구하는 것과 다르다.

---

## 3. 어긋남 6개 — "하나씩" 어긋나는 패턴

| # | 생물학 | 모델 | 어긋난 방향 |
|---|---|---|---|
| 1 | `Δw ∝ pre × post_`**출력** | `k_t ⊗ h_t` = pre × post_**입력** | 한 **단(stage)** 앞 |
| 2 | STDP 비대칭은 **쓰기 규칙**에 | 비대칭은 **읽기 커널**(`ψ` 의 `sin` 성분)에 | 한 **사상(map)** 옆 |
| 3 | `v` 는 `u` 와 같은 축에서 지연 | `v` 는 `t` 축, `u` 는 `r` 축 | 한 **축** 옆 |
| 4 | 적격흔적 ≠ 시냅스가중 (**2변수**) | `S` 하나가 겸직 | 한 **층** 압축 |
| 5 | 흔적은 상승상 있는 창 (**2차**) | 단일 지수 → lag 0 최대 `[D]` | 한 **미분 차수** 낮음 |
| 6 | 포화는 **구획별** 독립 | `a_tt` 는 전 채널 합 **스칼라 1개** | 한 **차원** 낮음 |

`#1` 과 `#2` 는 `PLASTICITY.md §6`, `§5.3` 이 이미 도달한 지점이다. `#2` 만 한 걸음 더:
**쓰기 규칙 `ΔS_t = k_t ⊗ h_t` 는 순서 민감도가 정확히 0 이다.** 이전에 무엇이 왔든 같은
값을 쓴다. 모든 순서 의존성은 저장된 것을 **읽을 때** 커널에서 생긴다. 따라서 "STDP" 는
가소성 규칙이 아니라 **판독 규칙**에 있다 — 어느 사상에 비대칭이 붙어 있는가의 층위 오류다.

### 3.1 `#3` — 회복변수가 없다

`[E]` 가 확정한다. `S` 는 깊이 스텝마다 0 에서 새로 만들어지므로 `S^{(r)} = F(h^{(r)})`,
즉 **현재 막전위장만의 순간 함수**다. 결과:

- `(u, v)` 위상평면에서 궤적이 **곡선 `v = F(u)` 위에 갇힌다.** 면적이 없으니 고리도 없다.
- 완화진동·불응기·유한 여행 후 복귀가 **원리적으로** 불가능하다.

동역학적으로 이것은 FHN 의 `ε ≪ 1` 이 아니라 **`ε → ∞`, 정반대 극한**이다 (회복변수의
단열 소거). 소거 후 남는 것은 빠른 부분계뿐이고, 그것은 흥분계가 아니라 쌍안정 스위치다.

**결합 방식도 다르다.**

```
FHN :  u̇ = u − u³/3 − v          ← 감산 (additive)
모델 :  f = M(S) h + a_tt h        ← 승산 (multiplicative)
```

`EXCITABILITY.md §1` 은 승산 결합을 "②가 상수가 아니라 기억의 함수라는 점에서 FHN 보다
풍부하다" 고 평가했다. **풍부한 것이 아니라 다른 동역학 부류다.** 감산 결합은 작동점을
`u` 축 위에서 이동시켜 널클라인 교차 구조를 만들고, 문턱은 그 교차에서 나온다. 승산
결합은 `h = 0` 을 항상 평형으로 남기므로 널클라인 기하가 생기지 않는다.

### 3.2 `#4` — 흔적과 시냅스가중이 같은 변수다

생물학은 두 변수다.

| | 시간척도 | 전달에 관여 |
|---|---|---|
| 적격흔적 `e_ij` | 빠름 (초) | **안 함** |
| 시냅스가중 `w_ij` | 느림 (분~년) | **함** |

그 사이의 사상이 **공고화**다. 모델의 `S` 는 `e^{-α(t-n)}` 로 감쇠하면서(흔적) 동시에
`q†S` 로 읽힌다(가중치). **한 변수에 두 역할이 겹쳐 있어 공고화가 들어갈 자리가 없다.**

여기서 `CAPACITY.md §4` 의 두 미스터리가 풀린다.

| 관측 | 흔적 관점의 해석 |
|---|---|
| `τ` 가 학습되지 않고 초기화가 0.68 bpb 를 결정 | 흔적의 `τ` 는 애초에 **기억 수명이 아니다.** 수명은 공고화된 가중치가 정한다. 없는 변수에 수명을 요구하고 있었다 |
| 유니터리가 감쇠보다 0.82 bpb 나쁘다 | **`τ = ∞` 인 흔적은 흔적이 아니다.** 동시성 검출 창을 없애서, 쓸 수도 없는 용량을 산 것 |

`CAPACITY.md §4.1` 은 "부동소수점도 역전파도 유니터리가 유리한데 왜 지는가" 를 미결로
남기고 §7 에서 "고정 용량의 패배" 로 정리했다. 흔적 관점에서는 **예측되는 결과**다.

그리고 이것이 델타+유니터리 노선에 대한 가장 강한 반론이다 — **겸직이 문제인데 겸직을
유지한 채 갱신 규칙만 정교하게 만드는 것**이다.

### 3.3 `#5` — 흔적이 한 미분 차수 낮다

`[D]`:

| | 피크 lag | 부호 반전 (40토큰) | lag0 / 피크 |
|---|---:|---:|---:|
| 현재 `e^{-αΔ}cos(θΔ)` | **0** | **36회** | 1.000 |
| 캐스케이드 (이중 지수) | **18** | **0회** | 0.159 |

```
현재      앞 8개: [1.0, -0.9, 0.77, -0.62, 0.47, -0.32, 0.18, -0.05]
캐스케이드 앞 8개: [1.0, 1.87, 2.61, 3.26, 3.81, 4.28, 4.68, 5.02]   ← 상승상
```

현재 커널은 **동시성 창이 아니라 임펄스 + 진동**이다. 모델은 창 모양을 **회전**으로
흉내내고, 생물학은 **동역학 차수**로 만든다 (PSP/PSC 의 표준 모형이 이중 지수 =
alpha function 이고, 상승상이 곧 동시성 검출 폭이다).

**그리고 이것이 [EXTRAPOLATION.md](EXTRAPOLATION.md) 의 절벽과 같은 원인이다.** 진동으로
창을 만들면 학습 길이 밖에서 앨리어싱이 일어난다 (§6: `θ` 주기 중앙값 2.11 토큰, 안 감기는
채널 1/64). 캐스케이드는 **실극점이라 부호 반전이 0회 → 원리적으로 앨리어싱이 없다.**

> §6-3 이 지목한 처방("안 감기는 채널")과 생물학적 흔적 동역학이 **같은 수정으로 수렴한다.**

### 3.4 `#6` — 포화가 스칼라 하나다

`a_tt h_t` 는 막 전체를 **스칼라 게인 하나**로 포화시킨다. FHN 의 `−u³/3` 은 각 구획마다
독립이다. 즉 현재 모델은 **단일 구획 점뉴런(point neuron)** 이고, 자기 포화항의 자유도는
토큰당 1 이다.

이것이 `CAPACITY.md §2.2` 의 관측 — "SSM 파라미터를 3.6배 쓰고 1.58 bpb 지며, 위상 채널을
늘려도 MLP 가 하는 일을 대신 못 한다" — 의 구조적 이유일 수 있다. **비선형 자유도가
토큰당 1 인데 MLP 는 `d` 다.** 부수 귀결로 `#6` 이 `#4`(부호 뒤집힘)의 원인이기도 하다:
전 채널 합이라 부호가 전역으로 하나뿐이고, 가중치 `|z_{t,j}|²` 가 내용 의존이라 그 하나가
입력에 따라 뒤집힌다.

---

## 4. ① 포화항은 유계성을 보장하지 않는다 — 부호만 고치면 더 나빠진다

> **§8.6.3 에서 이 절의 성격이 격하됐다.** 결함은 영구적 구조 결함이 아니라 **초기화 시점의
> 경쟁 조건**이며, 기존 설계는 보호막 셋(`w_scale=0.5`, warmup 300, `clip=1.0`)과 "학습이
> 부호를 스스로 고친다"로 그 경쟁을 이기고 있었다. 아래를 "기존 런이 잘못됐다"로 읽으면 안 된다.

### 4.1 부호가 내용에 따라 뒤집힌다

`[B]` (시드 6개):

```
psi 자유   : a_tt ∈ [-0.207, +0.267]   → 부호 뒤집힘, 포화 보장 없음
cos psi < 0 : a_tt ∈ [-0.750, -0.214]   → 항상 음수, 포화 보장
```

FHN 의 `−u³/3` 은 **고정 음수 상수**여야 한다 — 그것이 유계성의 근거다. 부호가 뒤집히면
어떤 입력에서 3차항이 **불안정화** 항이 되고, `M` 의 양의 고유방향(96~100%)과 겹치면
유한시간 폭주한다. `RESULTS.md §2.2` 의 `E_D → 6.9e6` 과 `CAPACITY.md §4.4` 의 d=512
발산이 그 증상이다.

`EXCITABILITY.md §7` 은 이 부호 뒤집힘을 "데이터 토큰이 억제성 채널을 선택적으로 켠다"
는 흥미로운 발견으로 기록했다. 옳은 관측이지만 **동시에 ① 의 보장을 무효화한다는 점은
짚지 않았다.**

보장은 제약 하나 거리에 있다. `cos ψ_j < 0` 이면 입력과 무관하게 `a_tt < 0` 이다. 그리고
`ψ ∈ (π/2, 3π/2)` 는 `|sin ψ| ≈ 1` 과 양립하므로 (3사분면) **주소 채널의 반대칭성
(`PLASTICITY.md §4`: ch20 `sin ψ = −0.955`) 을 잃지 않는다.** 파라미터 0개:
`ψ = π/2 + π·sigmoid(ψ_raw)`.

### 4.2 그런데 부호만 고치면 더 발산한다

`[C]` — 순전파 1회의 디리클레 에너지 증폭률:

| w_scale | R | 오일러/`ψ`자유 | 오일러/`cos<0` | **지수/`cos<0`** | max\|a_tt\| |
|---:|---:|---:|---:|---:|---:|
| 0.5 | 1 | ×0.98 | ×0.37 | ×0.46 | 0.12 |
| 1.0 | 1 | ×1.19 | ×0.89 | ×0.39 | 0.49 |
| 1.5 | 1 | ×2.49 | **×9.77** | **×1.78** | 1.10 |
| 2.0 | 1 | ×6.31 | **×39.02** | **×5.61** | 1.96 |
| 0.5 | 4 | ×0.98 | ×0.53 | ×0.54 | 0.12 |
| 1.0 | 4 | ×1.21 | ×0.19 | ×0.22 | 0.60 |
| 1.5 | 4 | ×9.72 | ×0.07 | ×0.11 | 8.72 |
| 2.0 | 4 | ×318,368 | **×8.6e23** | **×0.07** | 606 |

(`max|a_tt|` 는 오일러/`ψ`자유 런에서 잰 값)

**포화 부호를 보장하면 오히려 발산이 가속된다** (w=2.0, R=4 에서 ×3.2e5 → ×8.6e23).
이유는 명확하다:

```
h ← h(1 − c‖h‖²/R)        ← 명시적 오일러 + 3차 감쇠 = 로지스틱 사상
```

큰 `‖h‖` 에서 오버슈트해 음수로 튕긴다. **연속 FHN 에서 유계성을 보장하는 바로 그 항이,
명시적 오일러 이산화에서는 발산을 가속한다.**

자기항만 지수적으로 적분하면 사라진다 (1차까지 오일러와 동일):

```
h ← h · exp(a_tt/R) + (f − a_tt·h)/R
```

`Λ = e^{-α+iθ}` 에 이미 지수적 처리를 쓰고 있으므로 일관성도 회복된다. 자기항만 명시적
오일러로 남아 있던 것이 비대칭이었다.

### 4.3 두 개입은 반드시 함께 가야 한다

`[C]` 의 함의는 이것이다. **하나만 시도하면 각각 실패로 보인다.**

- `cos ψ<0` 만: 더 나빠짐 (×9.77) → "포화 부호는 중요하지 않다" 로 잘못 결론
- 지수 적분만: `a_tt > 0` 인 입력에서 `exp(+a_tt/R)` 가 증폭 → 이득 없음

문서가 이 조합을 못 찾은 이유가 이것일 것이다.

### 4.4 성공하면 정정되어야 할 서술 셋 — **§8 에서 대부분 철회됨**

> **주의.** 아래 표는 `1+2` 가 성공한다는 가정 하의 정정 제안이었다. §8 의 실측에서
> 셋 중 둘이 철회됐다. 남은 하나(`CAPACITY.md §4.4`)만 LM 시험 대기 중이다.
> 이 절을 근거로 다른 문서를 고치면 안 된다.

| 문서 | 현재 서술 | 재해석 |
|---|---|---|
| `model0.py §1.3` / `RESULTS.md §1.3` | `w_scale` 임계 스케일 (1.5 에서 ×694,495) 은 "norm 이 없고 자기항이 3차라" 생기는 고유 취약성 | **이산화 인공물**일 가능성. `[C]` 지수 열은 w=2.0 에서도 ×0.07 |
| `CAPACITY.md §4.4` | 폭 확장에 `lr ∝ 1/√d` + warmup 300 필요 — "정규화를 뺀 대가" | 첫 스텝 발산의 경로가 3차항 오버슈트라면 불필요해질 수 있음 |
| `RESULTS.md §3.6` | "R=1 은 이득 16배로 폭주, R=4 는 남는 깊이를 **폭주를 피하는 데 썼다**" | `1/R` 이 오일러 스텝 크기이므로 **안정성 조건 그 자체**. 학습된 자원 배분 없이도 나온다 |

특히 세 번째는 "깊이 ↔ 위상 교환" 서사의 일부이므로 확인이 필요하다. 교환 자체는
주소 채널 수(2 vs 1)와 Cohen's d(8.00 vs 3.08)로 독립 측정되어 있어 무너지지 않지만,
**`ρ_R`·`E_D` 로 쓴 부분은 수치 적분 효과와 분리되지 않았다.**

---

## 5. 장기기억까지의 사슬 — 네 링크가 전부 끊겨 있다

목표를 사슬로 펼치면:

```
막전위 동형 → 문턱 → 이산 발화 → 공고화 촉발 → 느린 저장소 이전 → 장기기억
```

| 링크 | 필요한 것 | 현재 | 근거 |
|---|---|---|---|
| 문턱 | 원점 밖의 **안정** 정지점 | **없음** | `[A]` |
| 발화 | 지연된 회복변수 | **없음** | `[E]`, §3.1 |
| 촉발 | 발화가 `t` 축 쓰기를 게이트 | **경로 없음** | 흥분성은 `r` 축, 기억은 `t` 축 |
| 이전 | 두 번째 느린 상태 + 승격 | **없음** | §3.2 |

### 5.1 문턱 — `f(0) = 0` 은 항등식이다

`[A]`: 플래그 8개 조합 전부에서 `max|f(0)| = 0.000e+00`. 이유는 구조적이다. value 경로가
`h` (또는 `h W_V`) 이므로 `A` 에 무엇을 넣어도 `f = A·0 = 0` 이다. `use_bias`, `use_silu_wo`
(SiLU(0)=0), `polar`, `read_norm`, `unitary` 전부 마찬가지다.

`CAPACITY.md §3.3` 이 `use_bias` 로 커널에 상수항 `|b|²cos ψ` 를 만들었지만, 그것은 `A` 의
상수항이지 `f` 의 상수항이 아니다. `f` 는 여전히 `h` 에 대해 최저 차수 1 이다.

그리고 기본 설정에서 `f` 는 **정확히 3차 동차**다 (`check_model0.py` 확인). 따라서
`f(h*) = 0` 이면 `f(λh*) = λ³f(h*) = 0` — **평형집합이 원점을 지나는 원뿔이고, 고립된
안정 정지점이 존재할 수 없다.** 게다가 원점은 불안정하다 (`max Re λ(M) > 0`, 96~100%).

> **`EXCITABILITY.md §6` 의 "구조는 있고 태스크가 안 시켰다" 는 성립하지 않는다.**
> 안정 정지점이 없으므로 어떤 태스크를 줘도 스파이크는 나오지 않는다. 태스크 문제가
> 아니라 정리다.

필요한 것은 `f` 자체의 **degree-0 항**이다. FHN 에서 정지점을 원점에서 떼어내는 파라미터
`a` 에 대응하며, 생물학적으로는 **휴지 전위를 정하는 긴장성 전류(tonic current)** 다.
가장 싼 구현은 value 경로 바이어스 `v_t = h_t W_V + b_V` (파라미터 `d` 개). `q, k` 기하와
무관하므로 라그랑지안 구조도 보존된다.

### 5.2 이전 — 병렬 뱅크에는 승격 경로가 없다

현재 `S` 는 채널마다 다른 `τ_j` 를 갖는 **시간척도의 병렬 뱅크**다. 그런데 쓰기는 모든
채널에 `k_t h_t^T` 를 `γ_j` 배로 동시에 넣는다. 즉 **한 기억의 운명이 쓰기 시점에
확정되고 이후 개정되지 않는다.**

생물학은 개정한다 — early-LTP 가 late-LTP 로 갈지는 이후 활동이 정한다. Fusi, Drew &
Abbott (2005, *Cascade models of synaptically stored memories*, Neuron) 의 캐스케이드
모형에서 **가소성-안정성 딜레마를 푸는 것은 척도의 존재가 아니라 척도 간 승격**이고,
그 결과가 지수 망각 → **거듭제곱 망각**이다.

> **병렬 뱅크(현재) ≠ 캐스케이드(생물). 차이는 승격 경로 하나다.**

`CAPACITY.md §7.2` 는 "빠진 것은 저장소가 아니라 공고화 메커니즘" 이라고 옳게 짚었으나,
두 슬롯을 **학습된 가중치 ↔ `S`** 사이에 놓았다. 그러면 추론 중 갱신이 불가능해 test-time
training 이 필요해진다. 올바른 정렬은:

| | 생물 | 올바른 대응 | 현재 |
|---|---|---|---|
| 빠름 | 적격흔적 / 단기 촉진 | `S` — **감쇠가 정상이다** | ○ |
| 느림 | 공고화된 시냅스 | **새 상태 `W`, `τ_W ≫ τ_S`** | **없음** |
| 사상 | 공고화 (발화가 촉발) | 승격 게이트 | **없음** |

**이 정렬은 학습된 가중치를 건드리지 않는다.** 둘 다 추론 중 갱신되는 상태다.

### 5.3 그래서 델타 규칙은 이 사슬을 고치지 않는다

| 링크 | 델타가 고치는가 |
|---|---|
| 문턱 (안정 정지점) | ✗ |
| 발화 (회복변수) | ✗ |
| 촉발 (축 간 경로) | ✗ |
| 이전 (느린 저장소 + 승격) | ✗ |

델타가 하는 일은 **빠른 저장소 안의 간섭·노름 문제**다. `PLASTICITY.md §6.5` 의 실험이
정확히 그것을 보인다 — 델타만 최근 항목을 복원(2.60)하고 초기 항목은 버린다(7.74).
유용하지만 **"오래된 것을 치우고 최근 것을 정확히 저장" 은 장기기억의 반대 방향**이다.
그 문서가 "의도된 거래" 라고 쓴 거래가, 이 목표에서는 지불하면 안 되는 대가다.

---

## 6. 제약을 깨지 않고 되먹임을 합법화하는 길

### 6.1 깊이축이 유일한 합법 축이다

`PLASTICITY.md §6.2` 가 "post 항은 깊이축에 있다" 고 관찰했지만 함의를 끝까지 밀지 않았다.
함의는 이것이다.

> 깊이 `r` 에서는 **모든 토큰의 `h^{(r-1)}` 이 동시에 존재**한다. 따라서 그것으로 계산한
> 무엇이든 `r` 단계의 `t` 축 스캔에게는 **입력**이다. 순환이 없다.

문턱·게이트·승격·발화를 `h^{(r-1)}` 에서 계산해 `r` 단계 스캔의 게이트로 넣으면
**게이트된 선형 스캔이라 병렬성이 그대로다.** `R` 번의 상태 되먹임 라운드를 공짜로 얻는다.

**tied 재귀가 이미 그 장치였는데 아무도 그렇게 쓰지 않았다.** `CAPACITY.md §5.1` 이
"weight-tied 재귀 = 가장 큰 차이" 라고 지목한 그 구조의 진짜 용도일 수 있다.

부수 효과: 델타조차 깊이축에서는 청크 알고리즘 없이 된다 (`k̂†ΛS` 를 `r-1` 단계 읽기값으로
대체하면 대각성 유지). **따라서 델타를 *노선*으로 삼을 이유가 더욱 없다 — 나중에 부산물로
얻을 수 있다.**

### 6.2 개입 후보 — 비용순

| # | 개입 | 파라미터 | 고치는 어긋남 | 확신도 |
|---|---|---:|---|---|
| 1 | `cos ψ < 0` 강제 (`ψ = π/2 + π·σ(·)`) | **0** | `#6` 부호, ① 보장 | 높음 (`[B]`,`[C]`) |
| 2 | 자기항 지수 적분 | **0** | ① 오버슈트, `w_scale` 임계, lr 스케일링 | 높음 (`[C]`) |
| 3 | value 경로 바이어스 `b_V` | `d` | **휴지 전위** = 원점 밖 안정 정지점 | 중간 |
| 4 | `v^{(r)}` 를 별도 상태로 + **감산** 결합 | ~4 + `(B,T,d)` 상태 | `#3` — 깊이축 FHN 완성 | 중간 |
| 5 | 흔적 캐스케이드 (실극점 2개, 비회전 채널) | ~`p` | `#5`, 상승상, 앨리어싱 제거 | 중간 |
| 6 | 느린 상태 `W` + 승격 게이트 | ~`2p` + `(p,d)` 상태 | `#4`, 공고화 | 낮음 (미검증) |
| 7 | 채널 그룹별 포화 (구획화) | 0~ | `#6` 차원 | 낮음 |

**1+2 는 반드시 같이, 그리고 먼저.** 파라미터 0개인데 §4.4 의 서술 세 개를 동시에
재해석하게 만들고, 실패하면 비용도 0이다.

### 6.3 깊이축 FHN (`#4`)

블록이 이미 불안정 선형항(`M`)과 3차 포화항(`a_tt`)을 주므로 FHN 의 `f(u)` 자리에 그대로
들어간다. 필요한 것은 별도의 느린 변수와 **감산** 결합뿐이다.

```
h^(r+1) = h^(r) + ( f(h^(r)) + b_V − v^(r) ) / R          ← u̇ = f(u) + I − v
v^(r+1) = v^(r) + eps·( h^(r) + a − b·v^(r) ) / R          ← v̇ = ε(u + a − bv)
```

`v` 는 `(B,T,d)` 로 토큰마다 독립이다. 즉 **깊이축 = 한 타임스텝 내부의 막전위 완화**,
**토큰축 = 시퀀스 시간** 으로 해석이 갈리고, 이것이 생물학적으로도 정합적이다.

이것이 되면 `EXCITABILITY.md §6` 이 제시한 반증 조건 — "출력 대역폭을 좁히거나 상태
전달에 노이즈를 넣어 인공 누설 케이블을 만들면 전부아니면전무 코드가 나와야 한다" — 을
**처음으로 실제 시험할 수 있다.**

### 6.4 느린 저장소와 승격 (`#4`, `#5`, `#6`)

채널을 (빠름 `j`, 느림 `j`) 쌍으로 묶는다.

```
S_t[j] = λ_j S_{t-1}[j] + γ_j k_{t,j} v_t†                  (기존 — 빠른 흔적)
W_t[j] = μ_j W_{t-1}[j] + η_j · g_t · S_t[j]                (추가 — 느린 저장, |μ_j| > |λ_j|)
f_t    = p^{-1/2} · Re[ q_t†( S_t + c·W_t ) ]
```

- 전이가 채널별 **하삼각 2×2** 이므로 대각성·결합법칙·병렬 스캔이 **전부 보존된다.**
- 닫힌 형태가 `(λ^{Δ+1} − μ^{Δ+1})/(λ − μ)` — `[D]` 의 alpha-function 이다.
  **즉 `#5`(상승상)와 `#6`(승격)이 같은 항이다.**
- 승격 게이트 `g_t` 를 §6.1 대로 `h^{(r-1)}` 의 발화에서 계산하면 병렬성이 유지된다.
  `g_t ≡ 1` 이면 무게이트 캐스케이드(= 순수 alpha 커널)로, 단계적 검증이 가능하다.
- `CAPACITY.md §7.1` 의 입력 계산 가능한 `c_t = Σ‖k‖²‖h‖²` 문턱을 쓰면 segmented scan
  형태가 된다. 그 절이 이미 "지금 구조에서 실현 가능한 유일한 발화 형태" 라고 짚은 것과
  같은 장치이며, **여기서는 그것을 리셋이 아니라 공고화 촉발로 재해석한다.**

**정직한 한계:** `W` 를 더해도 상태는 `O(1)` 이다. `CAPACITY.md §7` 의 정보이론적 한계를
넘지 못한다. 얻는 것은 비트가 아니라 **배분** — 오래된 기억이 간섭에서 보호되어 정밀도를
잃되 사라지지 않는, 지수 망각 → 거듭제곱 망각이다. `CAPACITY.md §8` 이 스스로 정한 축
("용량을 늘린다가 아니라 같은 용량을 더 잘 조직한다")과 정확히 일치한다.

---

## 7. 다음 실험과 반증 조건

### 7.1 순서

**1단계 — `1+2` (파라미터 0개).** selective copy R=1/R=4 재학습 + `w_scale` 스윕.

| 예측 | 반증되면 |
|---|---|
| `w_scale` 임계 스케일이 사라진다 (1.5, 2.0 에서 학습 성공) | ① 가 유계성의 경로가 아니다. §4.4 정정 철회 |
| R=1 의 `ρ_R` 폭주(16.3)가 완화된다 | `RESULTS.md §3.6` 의 자원 배분 해석이 유지된다 |
| selective copy 100% 유지 | 억제성 채널 제약이 주소지정을 해친다 → `#6` 은 구획화(`#7`)로 풀어야 한다 |
| `lr = 2e-3` 에서 d=512 가 발산하지 않는다 | `CAPACITY.md §4.4` 서술 유지 |

과감쇠 위험이 있다 (`[C]` 에서 w=0.5, R=1 이 ×0.98 → ×0.46). 학습으로 확인해야 한다.

**2단계 — `3+4` (깊이축 FHN).** 스파이크 관측이 처음으로 가능해진다.

| 예측 | 반증되면 |
|---|---|
| 원점 밖 안정 정지점이 생긴다 (`f(0) ≠ 0`, 3차 동차성 깨짐) | `b_V` 가 정지점을 만들지 못한다 → degree-0 항의 위치를 다시 봐야 함 |
| 대역폭을 좁히면 전부아니면전무 응답이 나온다 | 흥분성 기질 주장 자체가 무너진다 (`EXCITABILITY.md §6` 의 반증 조건) |
| `(‖h‖, ‖v‖)` 위상평면에 **고리**가 생긴다 | 감산 결합만으로는 부족 — `ε`, `b` 범위 탐색 |

**3단계 — `5+6` (캐스케이드 + 승격).** 여기서 장기기억을 잰다.

| 예측 | 반증되면 |
|---|---|
| 길이 외삽 절벽이 완화된다 (실극점 → 앨리어싱 없음) | `#5` 가 절벽의 원인이 아니다. `EXTRAPOLATION.md §7(a)` 로 복귀 |
| 오래된 항목의 오차가 **거듭제곱**으로 열화 (지수 아님) | 승격 없는 병렬 뱅크와 구별되지 않음 → `#4` 진단 재검토 |
| `g_t` 게이트가 있는 쪽이 `g_t ≡ 1` 보다 낫다 | 공고화 촉발이 불필요. 캐스케이드만 취하면 됨 |

### 7.2 이 문서가 걸고 있는 것

**`1+2` 가 실패하면 §4 전체가 무너진다.** 그 경우 `w_scale` 임계 스케일은 실제로 고유
취약성이고, ① 의 부호 문제는 유계성과 무관한 별개 사안이 된다.

**`[A]` 는 무너지지 않는다.** 항등식이다. 따라서 "현재 구조에 문턱이 없다" 는 결론은
어떤 실험 결과와도 무관하게 유지되며, 목표가 막전위 동형이라면 **degree-0 항은 선택이
아니라 필수**다.

---

## 8. 1단계 실측 — 예측 대체로 반증

`Model1Config.psi_inhib` / `self_exp` 로 구현 (파라미터 0개, 플래그 off 시 기존과 비트 단위
동일 — 실측 0.0e+00). selective copy, `l_noise=32, n_mem=4`, seed 0, `clip=1.0`.
로그: `scratchpad/log_*.txt`, 곡선: `sc_*.json`.

구현 검증 셋:

| 항목 | 실측 |
|---|---|
| 플래그 off 시 기존 모델과 동일 (회귀 없음) | **0.0e+00** |
| `psi_inhib`: `ψ_raw ∈ [-50,50]` 강제 후에도 `max cos ψ` | **0.000000** — 학습으로 벗어날 수 없음 |
| `self_exp` 의 1차 정합성 `\|오일러 − 지수\|` | R=4: 2.02e-2 → R=64: 2.50e-3 → R=1024: **1.58e-4** |

마지막 줄이 중요하다. R=64→1024 에서 16배 R 증가에 오차가 15.8배 줄어 **`O(1/R)`** 이다.
즉 `self_exp` 는 다른 모델이 아니라 **같은 모델의 다른 적분기**다 (R→∞ 극한이 동일).
R=4→64 의 비가 8.1 로 어긋난 것은 R=4 가 아직 점근 영역이 아니기 때문이다.

### 8.1 결과

| 설정 | 기준선 | **1+2** | 개입1 단독 | 개입2 단독 |
|---|---|---|---|---|
| R=1, `w=0.5` | 1.000 @ 400 | 1.000 @ 400 | — | — |
| R=1, `w=1.5` | 1.000 @ **400** | 1.000 @ **2000** | **0.965 (미달)** | 1.000 @ **400** |
| R=4, `w=1.5` | 1.000 @ 800 | 1.000 @ **400** | — | — |

학습 후 동역학 (R=1, `w=1.5`):

| | ρ_R | max\|a_tt\| | mean d_t | step 1 gnorm |
|---|---:|---:|---:|---:|
| 기준선 | 7.93 | 2.79 | +0.48 | 93.1 |
| 1+2 | **25.28** | **21.88** | −5.72 | **5.3** |
| 개입1 단독 | 15.22 | 10.87 | −5.38 | 21.0 |
| 개입2 단독 | 8.89 | 3.12 | +0.53 | 93.8 |

### 8.2 무엇이 틀렸나

**① `w_scale` 재해석 — 철회.** 예측은 "1.5, 2.0 에서 학습 성공" 이었는데 **기준선이 이미
성공한다** (R=1 은 400스텝, R=4 는 800스텝). 즉 `[C]` 의 순전파 에너지 증폭(×9.72)이
**학습 실패로 이어지지 않는다.** `clip=1.0` 이 첫 스텝 폭주를 흡수한다. `model0.py §1.3`
표는 순전파 진단으로서 정확하고, **그것이 학습 실패를 예측하지 않는다는 것이 새 정보다.**

**② ρ_R 완화 — 반증.** 개입 쪽 `ρ_R` 이 25.3 으로 기준선 7.9 보다 **높다.** `max|a_tt|` 도
21.9 대 2.8. 해석: 지수 적분이 큰 음수 `a_tt` 를 안전하게 만들어주니 모델이 그 영역을
쓴다. 실패는 아니지만 "동역학이 얌전해진다" 는 예측은 틀렸다. **`RESULTS.md §3.6` 의
자원 배분 해석을 반박할 근거가 되지 못한다 — 그 서술은 유지된다.**

**③ 개입 1 은 해롭다 — 이 판정은 철회한다. `미결` 이다.**

초판에서 "`cos ψ<0` 단독이 0.9648 로 100% 를 놓쳤으므로 억제성 채널 제약이 주소지정을
깎는다" 고 썼다. **근거가 안 된다.** 궤적을 보면:

```
step   200    400    600    800   1000   1200   1400   1600   1800   2000
seq   .482   .707   .598   .732   .867   .875   .924   .940   .963   .965
loss  .445   .289   .378   .279   .147   .122   .062   .057   .021   .025
```

**2000스텝에서 여전히 단조 하강 중이었다.** 그리고 스케줄이 `CosineAnnealingLR(T_max=steps)`
이므로 `--steps 2000` 은 8000스텝 런의 앞부분이 아니라 **2000에서 lr 이 0 으로 죽는 다른
스케줄**이다. `RESULTS.md` 의 기본 예산은 8000스텝이다. **런을 덜 돌리고 판정했다.**

측정된 것은 "더 나쁜 해로 수렴" 이 아니라 **"같은 예산 안에서 더 느린 최적화"** 뿐이고,
점근값은 모른다. 시드도 1개다.

그리고 그 느림의 원인은 `[C]` 가 **이미 예측한 것**이다. `cos ψ<0` 단독은 `a_tt<0` 을
보장해 로지스틱 오버슈트를 산발적 사고에서 확실한 사건으로 만든다(×2.49 → ×9.77).
그래디언트 노름 궤적이 그 서명이다 — `66.9, 47.9, 69.0, 21.9, **162.7**, 82.5, 1.5, 22.5`
로 크고 불규칙하다. 반면 1+2 는 `36.2, 14.2, 3.2, 0.13, 0.08` 로 매끄럽다.

> **즉 개입 1 단독의 실패는 개입 1 의 비용이 아니라 개입 2 의 부재다.**
> 초판이 이것을 "ψ 제약이 주소지정을 해친다" 로 잘못 귀속했다.

남는 진짜 질문은 **1+2 가 400 vs 1400 스텝인 것**이고, 이것도 400스텝에 포화하는 태스크에서
2000스텝 예산으로는 판정할 수 없다. `#6` 의 처방(전 채널 부호 제약 vs 구획화)은
**아직 열려 있다.**

**④ §4.3 "두 개입은 반드시 함께" — 방향이 반대.** 개입 2 단독은 완전 무해(400스텝, 기준선과
동일), 개입 1 이 비용이다. **"함께 써야 한다" 가 아니라 "2 는 공짜, 1 은 손해" 다.**

### 8.3 살아남은 것

**R=4, `w=1.5` 에서 기준선은 step 1 에 `gnorm = 1.12e7` 을 찍는다.** 클리핑으로 살아나
800스텝에 수렴하지만, 개입 쪽은 `gnorm = 0.591` 로 시작해 400스텝에 수렴한다.
**첫 스텝 그래디언트 폭주는 실재하고 개입 2 가 그것을 제거한다.**

그리고 이것이 `CAPACITY.md §4.4` 가 기술한 현상과 같은 종류다 — 거기서는 d=512 가 첫
스텝에 발산해 **클리핑으로 못 살아나** `lr ∝ 1/√d` 가 필요했다. 즉:

> **개입 2 를 시험할 자리는 selective copy 가 아니라 LM d=512 다.**
> 남은 질문: 지수 적분이 `lr ∝ 1/√d` 스케일링을 대체하는가.

### 8.4 내가 반복한 실수

`CAPACITY.md §3.1` 이 이미 경고한 패턴이다.

> "복사 태스크는 원본을 그대로 가져오는 게 정답이라 `W_V` 가 필요 없다. 그래서 100% 가
> 나왔고, **그것이 LM 에서의 결함을 가렸다.**"

**같은 함정에 빠졌다** — 안정성 개입을 400스텝에 포화하는 태스크에서 쟀다. 여유가 큰
태스크에서는 개입의 이득도 손해도 안 보인다. 개입 1 의 손해만 보인 것은 그 개입이
**태스크를 직접 푸는 자원(주소 채널의 `ψ`)을 건드렸기 때문**이고, 개입 2 는 그 자원을
안 건드리므로 아무 신호가 없었다.

**교훈: 안정성 개입은 안정성이 병목인 설정에서 재야 한다.** 그게 LM d=512 다.

### 8.5 LM 재측정 — selective copy 가 두 판정을 **모두 반대로** 만들었다

§8.4 의 교훈("안정성은 안정성이 병목인 설정에서 재라")대로 `charlm.py` d=512 로 옮겼다.
`CAPACITY.md §4.4` 가 첫 스텝 발산을 보고한 셀이다. **`warmup=0`** 으로 두어 그 처방을
제거하고 개입만으로 살아남는지 본다. `p=256, T=128, 300스텝, seed 0`. 런당 ~105초.

**`lr=1e-3, warmup=0`**

| arm | step 1 (bpb / max logit) | step 300 bpb | 판정 |
|---|---|---:|---|
| 기준선 | 1628 / **2.0e6** | — | **발산** |
| **개입 1 단독** (`cos ψ<0`) | 7.51 / 3.75 | **4.4018** | **살아남음** |
| 개입 2 단독 (지수 적분) | **nan** | — | **발산** |
| 1+2 | 7.48 / 4.49 | **4.3960** | 살아남음 |

**`warmup=0` lr 스윕 (300스텝 생존 여부)**

| lr | 기준선 | 1+2 |
|---|---|---|
| 1e-3 | 발산 (logit 2.0e6) | **4.396** |
| 1.5e-3 | 발산 (logit 5.9e10) | **4.233** |
| 2e-3 | 발산 (logit 1.1e14) | step 100 발산 |
| 3e-3 | 발산 (logit 8.0e21) | step 100 nan |

### 8.6 그래서 최종 판정

**① 개입 1 은 유지한다 — 값진 쪽이 이것이었다.**

`warmup=0, lr=1e-3` 에서 기준선은 첫 스텝에 `max logit 2.0e6` 로 죽고, `cos ψ<0` 단독은
`3.75` 로 시작해 정상 학습한다. **파라미터 0개로 `CAPACITY.md §4.4` 의 warmup 300 을
대체한다.** 기준선은 warmup 없이는 lr 을 1e-3 으로 낮춰도 못 산다 — §4.4 의 "2e-3 발산,
1e-3 정상" 은 warmup 이 있을 때의 경계였다.

**§4 의 진단은 — 미결이다.** "d=512 발산의 경로가 3차 자기항의 부호 미보장" 이라고 썼다가
내린다. `ψ_j` 는 자기항에만 들어가지 않고 **커널 전 성분**에 들어간다:

```
a_tn = p^{-1/2} Σ_j γ_j |z_tj||z_nj| e^{-α_jΔ} cos( arg z_nj − arg z_tj − ψ_j − Δθ_j )
                                                                      ↑ 모든 (t,n) 에 있다
```

`f_t = Σ_n a_tn h_n` 에서 자기항은 T=128 항 중 하나이고 `a_tt/R = 0.058` 이다. 따라서
개입 1 의 구제가 **(a) 자기항 부호 보장** 때문인지 **(b) `ψ` 를 π 만큼 옮겨 커널 전체의
부호 구조가 바뀌어 `ρ_R` 이 떨어진** 것인지 갈리지 않았다.

**단 (a) 는 충분한 설명이다** (§8.6.1). `a_tt>0` 이면 자기항 단독으로 3차 양의 되먹임이라
폭주가 보장된다. 남은 질문은 (b) 가 *추가로* 기여했는지다. 가르는 측정: `ρ_R`(=`euler_gain`)과
비대각 행합을 `ψ` 자유 vs `cos ψ<0` 으로 비교.

### 8.6.1 왜 개입 1 만으로도 오일러가 안 터지는가 — 문턱 계산

자기항만 남기면 깊이 사상이 `h ← h(1 + a_tt/R)` 이다. `u ≡ |a_tt|/R` (개입 1 하에서 `a_tt<0`)
로 두면 게인이 `1−u` 이고, `a_tt ∝ ‖h‖²` 이므로 `u ∝ ‖h‖²`. 따라서 `u` 자체의 사상이

```
u_{r+1} = u_r · (1 − u_r)²
```

이다. 고정점은 `u(1−u)² = u` → **`u = 0` (안정) 과 `u = 2` (불안정)**.

| `u` 영역 | 게인 `1−u` | 거동 |
|---|---|---|
| `u < 1` | `(0,1)` | 수축 |
| `u = 1` | `0` | 전면 붕괴 (이전 `h` 소거) |
| `1 < u < 2` | `(−1,0)` | 부호 반전, 그래도 수축 |
| **`u > 2`** | `< −1` | **확대 + 부호 반전 → 폭주** |

**`u = 2` 가 분리선(separatrix)이다.** 실측 운용점은 `u ≤ 0.268` — 분리선의 **1/7.5** 이고,
사상이 그것을 더 내린다 (`0.268 → 0.268·0.536 = 0.144`). 즉 **오일러가 엇나갈 영역에
애초에 들어가지 않으며, 동역학이 그 반대 방향으로 밀어낸다.**

> **개입 2 는 호출되지 않은 보험이다.** 개입 1 단독과 1+2 가 거의 동일한 것은 우연이 아니라
> 귀결이다. 개입 2 의 값은 `u = 2` 분리선 자체를 제거하는 것이고, 그 분리선에 다가가는
> 설정에서만 발현된다.

**그리고 이것이 기준선이 왜 터지는지도 설명한다.** `ψ` 자유면 `a_tt > 0` 이 92.1% 이므로
게인이 `1 + |a_tt|/R > 1` 이고, 성장이 `a_tt ∝ ‖h‖²` 를 키워 게인을 더 키운다 —
**양의 되먹임이고 문턱이 없다.** 개입 1 은 이것을 음의 되먹임 + `u=2` 문턱으로 바꾼다.
`u < 2` 쪽에 있으면 안전하고, 실제로 1/7.5 지점에 있다.

### 8.6.2 그럼 기존 문서의 기본 구조는 어떻게 학습됐나 — 보호막 넷

`a_tt>0` 이 양의 되먹임이고 문턱이 없다면, `RESULTS.md`·`CAPACITY.md` 의 모든 런은 왜 됐나.
보호막이 넷 있었고, 넷째가 결정적이다.

**① `w_scale=0.5` 기본값이 임계 아래로 골라져 있었다.** `model0.py §1.3` 의 표가 정확히 그
목적으로 측정된 것이다 (0.5 → ×1.18, 1.5 → ×694,495). 주석에 "기본값은 임계점 아래(약한
소산~중립)에서 출발하도록 잡는다" 고 적혀 있다. §8.1 에서 `w=1.5, 2.0` 을 쓴 것은
**정상 영역을 일부러 벗어난 것**이었다.

**② `warmup 300` 이 첫 스텝 킥을 없앤다.** Adam 첫 스텝은 그래디언트와 무관하게 `±lr` 이므로
warmup 이 그것을 `±lr/300` 으로 줄인다. `CAPACITY.md §4.4` 가 이 목적으로 도입했고,
§8.5 는 `--warmup 0` 으로 **그 보호막을 제거해서** 발산을 재현한 것이다.

**③ `clip=1.0` + Adam 이 유한한 폭주를 흡수한다.** 실증 사례가 §8.1 에 있다 —
`base_R4_w15` 는 step 1 에 `gnorm 1.12e7`, `loss 475,464` 를 찍고도 step 800 에 1.000 에
도달했다. 클리핑이 1.0 으로 자르면 "쓰레기 방향으로 단위 크기 한 걸음" 이 되고, 그건
낭비된 한 스텝일 뿐이다.

여기서 개입 2 단독이 왜 **범주적으로** 나빴는지도 설명된다.

| | 결과 | 클리핑이 흡수하나 |
|---|---|---|
| 기준선 | logit 2e6 — **유한** | ○ (원리적으로) |
| 개입 2 단독 | **`nan`** | **✗ 불가능** |

`nan` 은 전 파라미터를 오염시켜 되돌릴 수 없다. **유한한 폭주와 `nan` 은 정도 차이가 아니라
종류 차이다.**

**④ 그리고 학습이 스스로 부호를 고친다.** `EXCITABILITY.md §7` 의 학습된 체크포인트:

```
a_tt :  blank +0.160    data -0.609    marker -0.135
cos ψ>0 (흥분성) 27/64  →  37/64 가 억제성
```

초기화에서는 대부분 양수인데(d=512 에서 92.1%) **학습이 지배적 토큰에 대해 음수로 옮겨간다.**

> **개입 1 은 새 능력을 주는 게 아니라, 학습이 어차피 찾아가는 것을 초기화에서 보장한다.**

이것으로 모든 관측이 하나로 정리된다.

| 상황 | 무슨 일이 일어나나 | 개입 1 |
|---|---|---|
| `w=0.5`, warmup 있음 | 부호를 고칠 시간이 충분 | 불필요 |
| selective copy `w=1.5` | 클리핑이 첫 킥 흡수 → 그 뒤 학습이 고침 | 불필요 (이득이 안 보인 이유) |
| LM d=512, warmup 0 | 첫 스텝에 `a_tt>0` 로 폭주 → **고칠 시간이 없다** | **결정적** |

**`warmup` 과 개입 1 은 같은 문제의 두 해법이다** — 전자는 "천천히 시작해 `ψ` 를 조정할
시간을 준다", 후자는 "처음부터 옳은 쪽에 둔다". 그래서 대체가 가능했다.

### 8.6.3 그래서 §4 의 주장을 격하한다

"① 포화항이 유계성을 보장하지 않는다" 는 **참이지만**, 결함의 성격이 다르다.

| | 초판 서술 | 정정 |
|---|---|---|
| 결함의 성격 | 영구적 구조 결함 | **초기화 시점의 경쟁 조건(race condition)** |
| 결과 | 발산 | 학습이 이기면 무해, 첫 스텝에 지면 죽음 |
| 기존 설계 | 그 결함을 안고 있었다 | **세 보호막으로 경쟁을 이기게 설계돼 있었다** |

즉 기존 구조는 "발산하지 않는" 것이 아니라 **"발산 직전 영역을 보호막으로 피하면서 학습이
부호를 고칠 시간을 버는"** 구조였다. 문서들이 `w_scale`·warmup 을 넣은 이유를 "3차 자기항"
이라고만 적고 **부호 문제로 특정하지 않았던 것**이 이 문서가 더한 부분이다.

**그리고 이것이 개입 3+4 에서 1+2 를 표준으로 깔아야 하는 이유다.** 개입 4 는 자기항을
`u=2` 분리선 쪽으로 키우는 방향이고, 새로 생기는 파라미터(`eps, b_v, c`)에는 **"학습이
고쳐줄 것" 이라는 보장이 없다** — `CAPACITY.md §4.2` 가 `τ` 에 대해 정확히 그것이 안 된다는
것을 보였다. 보호막을 학습 의존에서 구조적 보장으로 바꿔놓고 새 동역학을 얹는 순서다.

**미측정:** 위 `u ≤ 0.268` 은 **초기화 시점** 값이다. 300스텝 학습 중 `u` 궤적은 재지 않았다.
간접 증거는 있다 — max logit 이 `3.75 → 13.4 → 14.4 → 17.3` 으로 유계, gnorm 이
`63 → 12.7 → 1.98 → 1.93` 으로 감쇠, bpb 단조 하강. 직접 확인하려면 학습 중 `a_tt` 를 로깅한다.

**② 개입 2 는 기각한다.**

단독으로 `nan` 을 낸다 — `a_tt>0` 인 입력에서 `exp(+a_tt/R)` 가 `1+a_tt/R` 보다 세게
증폭하기 때문이다(§4.3 이 예측한 방향). 개입 1 위에서는 4.3960 대 4.4018, **차이 0.006 으로
시드 노이즈(0.015~0.03) 안쪽**이다. 이론적으로 더 나은 적분기(`O(1/R)` 정합성 확인)이지만
**실측 가치가 없다.** `self_exp=False` 를 기본으로 유지하고 플래그로만 남긴다.

**③ selective copy 는 두 판정을 모두 반대로 만들었다.**

| | selective copy 가 말한 것 | LM 이 말한 것 |
|---|---|---|
| 개입 1 | 해롭다 (0.965) | **구제하는 쪽** |
| 개입 2 | 공짜다 (동률) | **단독으로 nan** |
| 쌍으로 써야 하나 (§4.3) | 아니다 (2 는 단독 무해) | **§4.3 이 절반 옳다** — 2 는 1 을 필요로 하나 1 은 2 를 필요로 않는다 |

400스텝에 포화하는 태스크에서는 안정성 개입의 **이득도 손해도 부호가 뒤집힌다.**
`CAPACITY.md §3.1`(복사 태스크가 `W_V` 결함을 가렸다)과 같은 함정이며, 이번에는
**가린 것이 아니라 반전시켰다.** 더 강한 형태다.

### 8.7 남은 것

- **표준 레시피 비교 미실시.** 위 숫자는 300스텝·`warmup=0`·bpb 4.40 으로,
  `CAPACITY.md §2` 의 d=512 = **2.982 bpb**(6000스텝, warmup 300, lr 1e-3)와 비교할 수 없다.
  **개입 1 이 최종 성능을 바꾸는지는 모른다.** 지금 보인 것은 "warmup 을 대체한다" 뿐이다.
- 시드 1개.
- `psi_inhib` 는 초기 `ψ` 분포도 바꾼다(`U(-π,π)` → `(π/2,3π/2)`). "초기화 효과 vs 학습 중
  제약 효과" 가 안 갈렸다. 가르려면 `ψ` 를 `(π/2,3π/2)` 로 초기화만 하고 자유롭게 두면 된다.

### 8.8 무너지지 않은 것

`[A]`(안정 정지점 없음)는 **항등식이므로 이 실험과 무관하다.** §5 의 사슬 진단 — 문턱,
회복변수, 축 간 경로, 느린 저장소 — 은 아직 손대지 않았다. 이번에 시험한 것은 §4(포화항의
부호와 이산화)뿐이고, 그것은 사슬의 링크가 아니라 **위생(hygiene) 항목**이었다.

---

---

## 9. 갈래 C — t축 막전위 + 점별 문턱 + 깊이축 리셋

§5 의 사슬 첫 두 링크(문턱, 발화)를 구현한다. 재현: `python diag_membrane_c.py`
(학습 불필요). 개입 1+2 를 표준으로 깔고 시작한다(§8.6.3).

### 9.1 갈래 A(깊이축 FHN)를 버린 이유 — 깊이축의 총 시간은 1 이다

§6.3 에 쓴 깊이축 FHN 설계는 **틀렸다.** 이유:

```
h ← h + f(h)/R   을 R 번  =  dh/ds = f(h) 를 s ∈ [0,1] 에서 적분
```

**`R` 은 지속시간이 아니라 해상도다. 총 시간은 언제나 1 이다.** 실측이 확정한다 —
`R = 8/16/32` 에서 `‖h‖` 최종/초기가 `0.740/0.743/0.745` 로 **수렴**한다. 거동이
바뀌지 않고 같은 구간을 더 정밀하게 적분할 뿐이다.

`CAPACITY.md §5.1` 이 자랑한 성질("`1/R` 스케일 때문에 `R→∞` 극한이 연속 동역학이다")
그 자체이고, **그 극한이 `s=1` 에서의 값**이라는 것을 아무도 명시하지 않았다.

FHN 은 회복변수가 `1/ε` 시간척도로 움직이므로 여행 한 번에 `s ~ 1/ε ≈ 12` 가 필요하다.
`s ∈ [0,1]` 로는 **원리적으로 불가능**하다. 게다가 `v` 를 널클라인에서 시작하면
`v̇ = 0` 이라 `v` 가 사실상 정지하고 `−c⊙v` 는 고정 오프셋이 된다 — 동역학이 아예 없다.

지름길도 없다.

| | 고리 (T2) | 문턱 (T3) |
|---|---|---|
| `ε ≪ 1`, `t_end ~ 1` | ✗ `v` 정지 | ✗ |
| `ε ~ 1`, `t_end ~ 1` | ○ 감쇠진동 | ✗ **문턱은 시간척도 분리를 요구한다** |
| `ε ≪ 1`, `t_end ~ 1/ε` | ○ | ○ |

`t_end` 를 도입하면 기술적으로는 풀리지만(`Model1Config.t_end`, 기본 1.0 으로 무영향),
갈래 A 는 **더 근본적인 문제**가 있다. 깊이축은 "토큰 하나 내부의 시간" 이므로 거기서
스파이크가 완결되면 **토큰 사이로 막전위가 전달되지 않는다** — 생물학의 핵심 성질인
**문턱하 입력의 누적**이 빠진다.

### 9.2 설계 C — 깊이축은 시간이 아니라 되먹임 통로다

§6.1 에 써놓고 §6.3 설계에서 쓰지 않은 통찰이 답이었다.

> `r` 단계에서 모든 토큰의 `h^{(r-1)}` 이 동시에 존재하므로, 그것으로 계산한 무엇이든
> `r` 단계의 `t` 축 스캔에게는 **입력**이다.

```
a_t^(r) = μ⊙a_{t-1}^(r) + s_t^(r-1)                           발화 흔적 (SRM)
m_t^(r) = λ_m·e^{−ρ₁ a_{t-1}}·m_{t-1}^(r)
              + e^{−ρ₂ a_{t-1}}·w_m⊙f_t^(r)                   막전위, 게이트된 선형 스캔
s_t^(r) = sigmoid( (m_t^(r) − θ) / τ_s )                       점별 비선형
```

- **문턱하 누적이 `t` 축에서** 일어난다 (갈래 A 가 못 하는 것)
- 리셋(=상태 되먹임)이 한 깊이 단계 늦게 적용되어 **병렬 스캔이 보존된다**.
  `s^{(r-1)}` 이 상수이므로 `a` 도 미리 정해지고 계수가 전부 사전 결정된다
  (Mamba selectivity 와 같은 형태)
- `f` 를 **시냅스 전류**로 읽는다. `m` 은 그것을 누적하는 막전위다
- 파라미터 `7d` (d=128 에서 448). `gate_write=False` 이면 **순수 관측자**로
  `h` 동역학을 전혀 바꾸지 않는다 (실측 0.0e+00)

### 9.3 결과 — C0~C4 전부 통과

**[C0] 통제 환원.** `membrane` on/off 시 logits 차이 **0.0e+00**. 관측자가 아무것도 바꾸지 않는다.

**[C4] 완화 반복 수렴 — 설계의 사활 조건.** `R` 번의 (적분→검출→리셋→재적분) 이
자기정합적 스파이크열로 수렴하는가.

| R | `\|s^r − s^{r-1}\|` 궤적 | 판정 |
|---|---|---|
| 4 | 0.0231 → 0.0012 | 수렴 |
| 8 | 0.0385 → 0.0004 | 수렴 |
| 16 | 0.0674 → 0.0006 | 수렴 |

홀짝 진동이 감쇠하며 수렴한다(리셋 오버슈트 → 발화 감소 → 약한 리셋 → …).
**불응 흔적을 넣기 전에는 `R=4` 가 미수렴(끝 0.0519)이었는데, 입력 억제가 진동을 죽여
`R=4` 로도 수렴한다.** 태스크 기본값을 바꿀 필요가 없어졌다.

**[C1] 문턱하 누적.** 위치 5,10,15,20,25 에 같은 크기 자극을 반복 주입.

| A | Δm@5 | @10 | @15 | @20 | @25 | 첫 발화 |
|---:|---:|---:|---:|---:|---:|---:|
| 0.25 | −0.032 | −0.031 | −0.021 | −0.032 | −0.041 | **없음** |
| 0.50 | 0.033 | 0.089 | 0.088 | 0.115 | **0.144** | **없음** |
| 1.00 | 0.454 | 0.833 | 0.921 | 0.766 | 0.976 | **25 (5번째)** |
| 2.00 | 0.682 | 0.926 | 1.125 | 1.444 | 0.879 | **10 (2번째)** |
| 4.00 | 0.928 | 0.999 | 0.932 | 1.300 | 1.018 | **5 (첫번째)** |

**첫 발화가 5 → 10 → 25 → 없음 → 없음 으로 밀린다.** `A=1.0` 이 결정적 증거다 —
단일 자극으로는 문턱을 못 넘는데 **5번 누적해서 넘는다.** `A=0.5` 는 `Δm` 이 4.4배
누적되지만(0.033→0.144) 끝까지 못 넘는다. 누설 `λ_m=0.9`, 간격 5토큰이면 `0.9⁵=0.59`
가 남으므로 포화하는 것이 정상이다.

> **갈래 A 가 원리적으로 못 하는 성질을 갈래 C 가 갖는다. C 를 고른 이유가 확인됐다.**

**[C2] 전부아니면전무.** 자극 토큰(20)의 고정 채널에서 `s` 를 진폭에 대해 스윕.

| A | 0.10 | 0.54 | 0.95 | 1.67 | 2.93 | 5.14 | 9.03 | 15.8 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `s` | 0.000 | **0.000** | 0.188 | 0.500 | 0.976 | **1.000** | 0.487 | 0.000 |
| 기울기 | — | 0.000 | 0.334 | 0.554 | 0.845 | 0.043 | −0.911 | −0.865 |

**침묵(A≤0.54, 정확히 0) → 급전이(0.95~2.9) → 포화(5.1, 기울기 0.04).** 전부아니면전무의
정확한 서명이다.

`A ≥ 9` 에서 응답이 무너지는 것은 **개입 1 이 보장한 `a_tt<0`** 때문이다. 큰 진폭에서
3차 포화항이 강한 음수가 되어 `f` 를 뒤집고 `m` 을 끌어내린다. 즉 **3차항이 실제로
포화 역할을 하고 있다는 증거**이고, 생물학적 대응물도 있다(depolarization block).

**[C3] 불응기.** 펄스 두 개를 `Δt` 간격으로. 기준 = 펄스 하나만 줬을 때 `Δs = 0.836`.

| Δt | 1 | 2 | 3 | 4 | 5 | 8 | 12 | 20 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 2차/단독 | **0.000** | **0.000** | **0.000** | 0.001 | 0.728 | 0.733 | 1.037 | 1.194 |

**절대 불응기 3토큰(응답 정확히 0), 상대 불응기 4~11토큰, 12토큰에서 완전 회복.**
생물학의 절대/상대 불응기 구조가 그대로 나왔다. `μ=0.7` 이면 흔적 반감기가 1.94 토큰이고
`exp(−ρa)` 억제라 절대 불응 3토큰이 정합적이다.

### 9.4 C3 은 처음에 실패했다 — 원인 둘

초판 구현은 `m_t = λ_m(1 − ρ·s_{t-1})m_{t-1} + w_m⊙f_t` 였고 `2차/1차` 가
`1.26, 1.26, 1.11, …` 로 **억제가 아니라 증강**이었다. 원인이 둘이었다.

**(a) 리셋이 유입 `m_{t-1}` 만 게이트하고 입력 `w_m⊙f_t` 는 안 게이트했다.** 자극이
강하면 입력 단독으로 문턱을 넘으므로 리셋이 무의미하다. 생물의 불응기는 *새 입력에도
반응하지 않는* 상태인데 **누적분만 지우고 있었다.**

**(b) `s_{t-1}` 만 봤으므로 불응 창이 정확히 1토큰이었다.** `Δt ≥ 2` 에서 억제가 0.

수정은 발화 흔적 `a_t = μa_{t-1} + s_t` 를 두고 `exp(−ρa)` 로 **둘 다** 억제하는 것이고,
이는 SRM 의 곱셈형 불응 커널이다. 그리고 **`PLASTICITY.md §6.4` 의 (a) 기존 억제 +
(b) 쓰기 억제 와 정확히 같은 수학이다** — 그 절이 "(b) 가 STDP 의 LTD 에 가장 충실하면서
병렬 구조를 안 깬다" 고 결론냈는데, 여기서 **불응기가 같은 항을 요구한다.** 우연이 아니다.

### 9.5 측정 설계를 세 번 틀렸다

`EXCITABILITY.md` 가 "찾으려는 대상을 검사 설계가 미리 지워버린다" 는 함정을 문서로
남겼는데, 이 세션에서 세 번 밟았다. 기록해둔다.

| # | 무엇을 틀렸나 | 증상 | 왜 위험한가 |
|---|---|---|---|
| 1 | `R` 을 지속시간으로 착각 | T1~T3 전부 평평 | 틀린 축을 스윕하면 "구조가 없다" 는 오결론 |
| 2 | `ch = argmax(평균 발화율)` | 응답1 = 0.979 (항상 켜진 채널) | 문턱 시험인데 기저가 이미 문턱 위 |
| 3 | 채널을 조건마다 재선택 | 1차 응답이 0.50/0.55/0.84 로 흔들림 | 비율의 분모가 행마다 달라 비교 불가 |

`2` 는 θ 를 90분위로 잡아 기저 발화율이 10% 였던 것이 원인이다. **문턱 시험은 기저가
문턱 아래여야 성립한다** — 99.5분위로 고쳤다. `3` 은 조건 루프 밖에서 채널을 한 번
고정해서 고쳤다. 셋 다 "무엇에 대해 무엇을 재고 있는가" 를 확인하지 않은 것이다.

### 9.6 사슬 현황

| 링크 | 상태 | 근거 |
|---|---|---|
| 문턱 | **통과** | C2 — 침묵 → 급전이 → 포화 |
| 발화 | **통과** | C1(누적) + C2(전부아니면전무) + C3(불응기) |
| 공고화 촉발 | **미구현** | `gate_write=False`. 스파이크로 `S` 쓰기를 게이트하는 것이 다음 |
| 느린 저장소 이전 | **미구현** | §6.4 의 개입 6 |

### 9.7 정직하게 — 무엇을 얻고 무엇을 안 얻었나

**`h` 에 문턱을 만든 것이 아니다. 새 변수 `m` 을 추가해서 거기에 문턱을 만들었다.**

- `[A]`(`f(0)=0`, 안정 정지점 없음)는 **여전히 참**이고 `h` 동역학은 그대로다.
  `h` 는 아직 3차 동차 퇴화 계이고 원점에서 야코비안이 정확히 `I` 다.
- 얻는 것: 기존 결과 전부 유지, 막이 순수 관측자→게이트로만 작동, 병렬성 보존.
- 안 얻는 것: "막전위 동형" 이 `h` 자체의 성질이 아니라 **병렬로 붙인 부속**의 성질이다.
  §3 의 어긋남 `#3`(회복변수), `#6`(포화가 스칼라 하나)은 `h` 쪽에 그대로 남아 있다.

**개입 3(`tonic`)·4(`recovery`)는 갈래 A 소속이므로 폐기한다.** 코드는 남기되(플래그
기본 off) §6.3 의 설계는 §9.1 에 의해 무효다. `t_end` 도 남기되 갈래 C 에서는 안 쓴다.

**미검증:** 전부 초기화 시점 + 단일 시드 + 학습 없음이다. `θ` 를 무자극 분포의 99.5분위로
**손으로 보정**했고, 학습이 그 영역에 머무는지는 모른다 (`CAPACITY.md §4.2` 가 `τ` 에
대해 학습이 못 고친다는 것을 보였으므로 실제 위험이다). `gate_write=True` 로 켜서
`h` 동역학에 되먹임이 걸리면 C4 의 수렴이 유지되는지도 다시 확인해야 한다.

### 9.8 다음

1. **`gate_write=True`** — 스파이크로 `S` 쓰기를 게이트(링크 3). C4 수렴 재확인이 먼저다.
2. **학습** — selective copy + LM 에서 태스크가 깨지지 않는지, `θ`·`μ`·`ρ` 가 흥분성
   영역에 머무는지. 단 §8.4 의 교훈대로 **태스크 정확도로 흥분성을 판정하지 않는다.**
3. **개입 6** — 느린 저장소 `W` + 승격 게이트. 여기서 처음으로 장기기억을 잰다.

## 10. 한계

- `[C]` 는 **초기화 시점, 시드 1개, 미학습**이다. 강성(stiffness) 주장으로는 충분하고
  `model0.py §1.3` 표도 같은 층위지만, "그래도 학습이 되는가" 는 답할 수 없다.
- `[B]` 도 초기화 시점이다. 학습된 체크포인트에서의 `a_tt` 부호 분포는
  `EXCITABILITY.md §7` 의 값(blank +0.160, data −0.609, marker −0.135)이 유일한 실측이고,
  체크포인트가 `.gitignore` 되어 있어 재확인하려면 재학습이 필요하다.
- §6 의 개입 6개는 **전부 미검증 설계안**이다. `1+2` 만 측정 근거가 있다.
- §5.2 의 거듭제곱 망각 주장은 Fusi et al. 의 결과를 유추 적용한 것이다. 우리 구조에서
  실제로 거듭제곱이 나오는지는 계산도 측정도 하지 않았다.
- `#6`(구획화)은 진단만 했고 개입 설계가 없다. 멀티헤드
  (`CAPACITY.md §8.1` #2)와 같은 것인지 다른 것인지 정리되지 않았다.
- 이 문서는 `RESULTS.md §3.6`, `model0.py §1.3`, `CAPACITY.md §4.4` 의 **정정을 제안**
  하지만 아직 정정하지 않았다. `1+2` 결과를 기다린다.

---

## 부록 — 측정 재현

```bash
python diag_misalign.py           # [A]~[E] 전부, 학습 불필요, ~2초
python diag_misalign.py --only C  # 한 절만
```

| 절 | 재는 것 | 학습 필요 |
|---|---|---|
| `[A]` | `f(0)` — 안정 정지점의 존재 (플래그 8조합) | 불필요 |
| `[B]` | `a_tt` 부호 범위, `cos ψ<0` 제약 효과 (시드 6개) | 불필요 |
| `[C]` | 디리클레 에너지 증폭률 — 오일러 vs 자기항 지수 적분 | 불필요 |
| `[D]` | 흔적 커널의 피크 lag·부호 반전 횟수 | 불필요 |
| `[E]` | `S` 의 깊이축 지속성 (0 초기화 횟수) | 불필요 |
