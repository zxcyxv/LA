# 필즈상 연계 제안 검토 (개정판)

> `필즈상_연계_제안.txt` 의 검토. **원문은 `초기구상.txt` 를 대상으로 답변한 것이므로
> 현재 Model 1 기준으로 판정하면 부당하다.** 초판에서 그 실수를 했고 여기서 바로잡는다.
>
> **개정 결론:** 라그랑지안 주장은 **초기구상에서 실제로 성립한다.** 다만 올바른
> 불변량은 푸카야 범주가 아니라 **마슬로프 지수**이고, `Sp(2d,ℝ)` 제약의 이익 주장은
> 여전히 수학적으로 뒤집혀 있다.

---

## 0. 사실관계

2026 필즈상: **Yu Deng, John Pardon, Jacob Tsimerman, Hong Wang**
(2026-07-23, ICM 필라델피아). 원문의 세 이름은 정확하다.
원문 4차 응답이 "파든의 수상 업적을 직접 차용한 것이 아니다" 라고 스스로 정정한 것도 옳다.

---

## 1. 초판에서 내가 틀린 것 — 라그랑지안 주장은 성립한다

### 1.1 왜 성립하는가

초기구상의 사영은

```
q_t = (x_t W_Q) ⊙ e^{iφ_q},    W_Q ∈ ℝ^{d_model×d},  φ_q ∈ ℝ^d
```

이고 **`W_Q` 가 실수**이므로 진폭 `a_j = (x W_Q)_j` 가 실수다. 그러면 같은 위상을 갖는
두 벡터의 내적이

```
⟨q, q'⟩ = Σ_j conj(a_j e^{iφ_j}) · a'_j e^{iφ_j} = Σ_j a_j a'_j   (실수)
```

이므로 **`ω(q,q') = Im⟨q,q'⟩ ≡ 0`**. 즉 사영의 상

```
L_Q = { a ⊙ e^{iφ_q} : a ∈ ℝ^d } = e^{iφ_q} ℝ^d
```

가 `ℂ^d ≅ ℝ^{2d}` 의 **라그랑지안 부분공간**이다.

**수치 확인** (`d=16, d_model=32`, 모든 쌍의 최대 `|ω|`):

| 사영 | max `\|ω\|` |
|---|---:|
| 초기구상 (실수 `W_Q` × `e^{iφ}`) | **1.1e-14** |
| Model 1 (복소 `W_C`) | 2.5e+02 |

**Model 1 은 `W_C` 를 복소로 만들면서 이 구조를 버렸다.**
대화내역2 §9 의 지적("`φ` 에 `t` 인덱스가 없어 토큰 무관")을 고치려다 잃은 것이다.

### 1.2 어텐션은 라그랑지안 사이의 켈러 각을 잰다

`Λ = diag(e^{-iθ})` 는 유니터리이므로 라그랑지안을 라그랑지안으로 보낸다.
따라서 시각 `n` 의 키 라그랑지안이 시각 `t` 에서 `Λ^Δ L_K` 로 회전해 있고,
어텐션 점수는

```
Re⟨q_t, Λ^Δ k_n⟩ = Σ_j a_j b_j cos(φ_{q,j} − φ_{k,j} − Δθ_j)
```

이다. 각 채널에서 `cos(Δφ_j)` 는 **두 라그랑지안 직선 사이 각의 코사인**이다.

| `Δφ_j` | 기하 | 어텐션 |
|---|---|---|
| 0 (mod π) | 두 직선이 **일치** — 비횡단 | 최대 기여 |
| π/2 | 두 직선이 **직교** — 횡단 | 기여 0 |

**초판에서 "내적은 교차수가 아니다" 라고 일축한 것은 성급했다.**
교차수는 아니지만 **라그랑지안 사이의 각(Kähler angle)** 이며, 이는 정당한 사임플렉틱
기하량이다.

---

## 2. 그러나 올바른 불변량은 마슬로프 지수다 — 푸카야가 아니라

### 2.1 무엇이 실제로 이산 불변량인가

`Δ` 가 변할 때 `Λ^Δ L_K` 는 **라그랑지안 그라스만 다양체**

```
Λ(d) = U(d)/O(d)
```

위의 경로를 그린다. 이 다양체는 **컴팩트**하고 `π₁(Λ(d)) = ℤ` 이며, 경로의 불변량이
**마슬로프 지수**다. 그리고 마슬로프 지수는

> `L_Q` 와 **비횡단이 되는 사건의 부호 있는 개수**

다. 대각 전이에서는 채널 `j` 가 `π/θ_j` 마다 한 번씩 공진하므로

```
교차 수 ≈ Σ_j Δ·θ_j / π
```

**수치 확인** (`d=16`, 무작위 `θ_j ∈ [0,0.8)`):

| 창 길이 | 실측 교차 | 예측 `Σ_j Δθ_j/π` |
|---:|---:|---:|
| Δ ≤ 10 | 15 | 23.7 |
| Δ ≤ 20 | 38 | 47.5 |
| Δ ≤ 40 | 84 | 95.0 |

정수 절단 때문에 체계적으로 낮게 나오지만 스케일이 맞는다.

### 2.2 그래서 "위상적 강건성" 에는 실제 내용이 있다

마슬로프 지수는 **정수**다. 작은 섭동으로는 변하지 않는다. 원문이 말한
"노이즈에도 붕괴하지 않는 위상적 불변량" 은 **이 형태로는 옳다.**

다만 필요한 수학은 **학부 수준 대수위상**이다 — 라그랑지안 그라스만의 기본군이 `ℤ`
라는 사실. 푸카야 범주도 VFC 도 필요 없다.

### 2.3 우리가 잰 것과 연결된다

PLASTICITY.md §2 에서 학습된 가소성 창의 **부호 반전 3회** 를 관측했다.
그 부호 반전이 곧 `cos` 이 0 을 지나는 사건, 즉 **비횡단 교차 사건**이다.
마슬로프 지수는 그것을 세는 정수다.

즉 **"가소성 창의 부호 반전 횟수 = 마슬로프 지수"** 라는 대응이 성립하며,
이것은 측정 가능하고 이산이다.

---

## 3. 여전히 성립하지 않는 것

### 3.1 아놀드 추측 → 최소 인출 보장: **거짓**

아놀드 추측의 가설은 **닫힌(컴팩트)** 사임플렉틱 다양체 위의 **해밀토니안** 동형사상이다.

- 상태 공간 `ℂ^{d×d}` 는 **비컴팩트**
- 상태 갱신 `S_t = Λ S_{t-1} + k_t v_t†` 는 **아핀 사상**이다. 회전은 심플렉토모피즘이나
  `+ k v†` 는 평행이동이고, 전체가 컴팩트 다양체 위의 해밀토니안 흐름이 아니다

두 가설이 모두 불성립하므로 결론을 가져올 수 없다. "최소 인출 보증수표" 는 없다.

(선형 라그랑지안 부분공간 자체도 비컴팩트다. 그래서 §2 에서 컴팩트한 것은 부분공간이
아니라 **그라스만 다양체** 이고, 그것이 마슬로프 지수가 잘 정의되는 이유다.)

### 3.2 `Sp(2d,ℝ)` 제약의 이익: **뒤집혀 있다**

**수치 확인.** `W = exp(JA)`, `A` 대칭 (해밀토니안 지수사상), `d=8`, 20회 평균:

| scale | `WᵀJW−J` | `det` | `\|λ\|max` | `\|λ\|min` | `‖W‖₂` |
|---:|---:|---:|---:|---:|---:|
| 0.1 | 1.1e-15 | 1.0000 | 1.292 | 0.776 | 1.419 |
| 0.3 | 2.3e-15 | 1.0000 | 2.127 | 0.478 | 2.909 |
| 1.0 | 3.5e-14 | 1.0000 | **14.797** | 0.086 | **25.600** |

원문이 근거로 든 **켤레쌍 정리가 바로 반증이다.**

> `|λ| ≠ 1` 인 고유값이 하나라도 있으면 `1/λ` 도 존재한다.
> 즉 수축 방향이 있으면 **팽창 방향이 반드시 있다.**

`det = 1` 은 부피만 보존하고 형태를 보존하지 않는다. 한 축 15배·다른 축 1/15배면
부피는 같다. BPTT 에서 그 팽창 축을 따라 그래디언트가 커진다.

**막는 것은 유니터리다.** `Sp(2d,ℝ) ∩ O(2d) = U(d)` 이고 유니터리는 `|λ|=1`
(실측 1.0000). 즉 **유니터리가 사임플렉틱보다 강한 제약**이며, 초기구상의 전이
`e^{-iθ}` 는 이미 유니터리다. 제안은 이미 가진 것보다 **약하다.**

같은 이유로 **길이 외삽 보장**(체적 보존이 형태 왜곡을 못 막음)과
**립시츠 상계 통제**(`‖W‖₂` 무계, 실측 25.6)도 무너진다.

### 3.3 그리고 §3.2 는 §1 과 모순된다

더 근본적인 문제가 있다. **사영의 상이 라그랑지안이면 그 사영은 심플렉틱일 수 없다.**

- 라그랑지안: `ω` 가 상에서 **0 으로 당겨짐**
- 심플렉틱: `ω` 가 **보존됨**

원문 2차 응답(라그랑지안 그림)과 3차 응답(`Sp` 제약)은 **서로 배타적인 요구**다.
초기구상의 자연스러운 구조는 **"사영은 라그랑지안으로 보내고, 전이가 유니터리로
회전시킨다"** 이며, 이것이 마슬로프 지수가 사는 정확한 무대다.

덧: `Sp(2d,ℝ)` 는 정사각·짝수차원 군인데 `W_Q ∈ ℝ^{d_model×d}` 는 정사각이 아니다.
타입도 맞지 않는다.

---

## 4. Hong Wang / decoupling — 제안은 옳고 이론은 불필요

θ 스펙트럼을 dyadic 분할해 채널 그룹별로 다른 주파수 대역을 맡기는 것은 좋은 아이디어다.
다만 **이미 표준 관행**이다 — S4/HiPPO 의 주파수 초기화, RoPE 의 `10000^{-2i/N}`,
Mamba-3 의 `dt_min=0.001, dt_max=0.1` 로그 균등.

그리고 **우리는 이것이 중요함을 이미 실측했다** — `τ` 초기 범위가 **0.68 bpb** 를
결정하고 학습으로 교정되지 않는다 (CAPACITY.md §4).

decoupling 정리는 분리된 푸리에 지지집합을 가진 함수합의 `L^p` 노름에 대한 **날카로운
상수**를 다룬다. "서로 다른 대역이 섞이지 않는다" 는 정성적 진술은 정리 없이 자명하고,
정리가 주는 정량적 상수는 우리 설정에 대응물이 없다.

**다만 마슬로프 관점에서 보면 dyadic 분할에 새 의미가 생긴다** — 채널 `j` 의 교차
빈도가 `θ_j/π` 이므로, 주파수 대역 분할은 곧 **마슬로프 교차율의 다중 스케일 배분**이다.
이 각도는 실행 항목을 정당화하는 데 쓸 수 있다.

---

## 5. Tsimerman / o-최소성 — 전제가 성립하지 않는다

원문은 "코사인의 주기성 때문에 손실 지형이 초월적이라 SGD 가 헤맨다" 고 전제한다.

1. `sin`, `cos` 는 **해석적**이므로 Łojasiewicz 부등식이 **이미 적용된다**
2. 유계 구간에서 삼각함수는 `ℝ_an,exp` 에서 **이미 정의가능**하다
3. 주기적 파라미터는 딥러닝 표준이며(RoPE, Fourier features) 최적화가 잘 된다
4. `S^1 → ℂP^1` 재파라미터화는 경험적 선택지이지 정리의 귀결이 아니다

**우리가 실측한 진짜 최적화 문제는 다른 곳에 있다** — `α = exp(alpha_log)` 파라미터화
때문에 `τ` 가 학습되지 않는다 (CAPACITY.md §4.2). 위상이 아니라 **감쇠**의 문제이고
o-최소성과 무관하다. 재파라미터화가 필요한 자리는 여기다.

---

## 6. 개정 판정표

| # | 주장 | 초판 | **개정** |
|---|---|---|---|
| A | `ℂ^d` 가 사임플렉틱 위상공간 | 참 | 참 |
| B | `e^{-iθ}` 가 심플렉토모피즘 | 참 | 참 |
| C | `q`, `k` 의 상이 라그랑지안 | ~~거짓~~ | **참** (초기구상, 실측 1.1e-14) |
| D | 어텐션 = 라그랑지안 기하 | ~~거짓~~ | **부분 참** — 켈러 각. 교차수는 마슬로프 |
| E | 위상적 불변량 → 강건성 | ~~거짓~~ | **부분 참** — **마슬로프 지수**가 진짜 이산 불변량 |
| F | 아놀드 추측 → 인출 보장 | 거짓 | **거짓** (비컴팩트, 아핀) |
| G | `Sp` 제약 → 그래디언트 안정 | 뒤집힘 | **뒤집힘** + §3.3 의 자기모순 |
| H | 체적 보존 → 길이 외삽 | 거짓 | **거짓** |
| I | 사임플렉틱 → 립시츠 상계 | 거짓 | **거짓** (`‖W‖₂` 무계) |
| J | dyadic 주파수 분할 | 참, 이론 불필요 | **참** + 마슬로프 교차율 해석 |
| K | o-최소성 → 위상 최적화 | 장식적 | **장식적** |

**초판이 C·D·E 를 부당하게 기각했다.** Model 1 기준으로 판정했기 때문이다.

---

## 7. 설계 함의 — 잃은 것을 되찾을 수 있다

| | 초기구상 | Model 1 |
|---|---|---|
| 사영 상이 라그랑지안 | **○** | ✗ |
| 토큰별 위상 (내용 주소지정) | ✗ | **○** |

**둘 다 가질 수 있다.** 진폭을 실수로 두고 위상만 입력 의존으로 만들면 된다.

```
a_t = W_R h_t          (W_R ∈ ℝ^{p×d}, 실수 진폭)
φ_t = W_φ h_t          (W_φ ∈ ℝ^{p×d}, 입력 의존 위상)
z_t = a_t ⊙ e^{iφ_t}
```

그러면 **각 토큰이 자기 라그랑지안 `L(x_t) = e^{iφ(x_t)}ℝ^p` 를 갖고**, 어텐션이
`L(x_t)` 와 `Λ^Δ L(x_n)` 사이의 켈러 각을 잰다.

**파라미터 수가 복소 `W_C` 와 정확히 같다** (둘 다 `2pd`). 순수한 재파라미터화 비교다.

구현 검증 (`model1.py` 의 `polar=True`):

| | 파라미터 | 토큰별 라그랑지안 `ω` | `(q,k)` 그래프 `ω⊖ω` |
|---|---:|---:|---:|
| Model 1 (복소 `W_C`) | 6,305 | — (성립 안 함) | 0.0e+00 |
| **극좌표 (실수 진폭)** | **6,305** | **1.9e-17** | 5.6e-17 |

이것은 Model 1 과도 초기구상과도 다른 **제3의 구조**이며, 라그랑지안 구조와 내용
주소지정을 동시에 갖는다. 실험 결과는 §8 에 기록한다.

---

## 8. 실험 — 극좌표 변형

`model1.py` 의 `polar=True`. 두 조건 모두 **파라미터 수가 동일**하므로 차이는 순수한
구조 효과다.

| | Model 1 (복소 `W_C`) | **극좌표 (라그랑지안)** | 차이 |
|---|---:|---:|---:|
| 파라미터 (LM 설정) | 230,272 | **230,273** | +1 |
| **selective copy, R=1** | 1.000 / 1.000 | **1.000 / 1.000** | **동률** |
| **바이트 LM** (`d=256` + `W_V` + bias) | **2.002** | 2.070 | **+0.068** |

### 읽는 법

**구조적 태스크에서는 완전 동률이다.** 위상 주소지정 메커니즘이 진폭의 복소성에
의존하지 않는다는 뜻이다. `R=1` 에서 selective copy 를 100% 로 푸는 능력은
라그랑지안 사영으로도 그대로 유지된다.

**LM 은 0.068 손해다.** 시드 노이즈(0.015~0.03)의 2~4배이므로 실재하는 차이로 보이나,
이 세션에서 잰 다른 축들과 비교하면 작다:

| 축 | Δbpb |
|---|---:|
| value 재표현 `W_V` | −1.02 |
| 감쇠 유지 (vs 유니터리) | −0.82 |
| **극좌표 (라그랑지안 복원)** | **+0.068** |
| 커널 바이어스 | −0.035 |

즉 **라그랑지안 구조는 거의 공짜로 얻어진다.**

### 손해의 추정 원인 (미검증)

극좌표는 `|z| = |W_R h|` 와 `arg z = W_φ h` 가 **분리**되어 있다. 복소 `W_C` 는 둘이
결합돼 있어 더 유연하다. 특히 채널을 끄려면(`|z| ≈ 0`) 극좌표에서는 진폭이 정확히 0
이어야 하는데, 복소 사영에서는 실수부·허수부가 함께 작아지면 되므로 더 쉽다.
채널 게이팅 능력의 차이일 가능성이 있다.

### 얻는 것

| | 복소 `W_C` | 극좌표 |
|---|---|---|
| 사영 상이 라그랑지안 | ✗ | **○** (실측 1.9e-17) |
| `(q,k)` 그래프 라그랑지안 | ○ | ○ |
| 어텐션의 기하 해석 | 내적 | **두 라그랑지안 사이 켈러 각** |
| 이산 불변량 | 없음 | **마슬로프 지수** = 부호 반전 횟수 |
| 토큰별 위상 (내용 주소지정) | ○ | ○ |

**해석가능성 축에서는 명백한 이득이고, 성능 축에서는 0.068 의 대가다.**
어느 쪽을 택할지는 논문의 성격에 달렸다.

## 9. 논문 서술 권고

**쓸 수 있는 것**

> 커널은 계량 성분과 심플렉틱 성분의 `ψ`-혼합이다. 실수 진폭 사영에서 각 토큰의 상은
> 라그랑지안 부분공간을 이루며, 유니터리 전이가 이를 라그랑지안 그라스만 `U(d)/O(d)`
> 위에서 회전시킨다. 어텐션 점수는 두 라그랑지안 사이의 켈러 각이고, 가소성 창의
> **부호 반전 횟수는 그 경로의 마슬로프 지수**다.

전부 초등 사임플렉틱 선형대수와 `π₁(Λ(d)) = ℤ` 로 증명되며 수치로 검증된다.

**쓰면 안 되는 것**

- 아놀드 추측에 의한 인출 보장 (가설 불성립)
- 푸카야 범주 / VFC 의 직접 적용 (범주 오류)
- `Sp` 제약의 안정성·외삽·립시츠 이익 (**정반대**, 그리고 라그랑지안 그림과 모순)

**리뷰어가 사임플렉틱 기하를 알면 §3.2 의 오류는 즉시 잡힌다.** `λ, 1/λ` 켤레쌍은
교과서 첫 페이지이고, 그것이 안정성이 아니라 **불안정성**의 근거라는 것도 마찬가지다.

---

## 10. 반성 — 두 번의 같은 실수

원문의 오류는 "구조가 유사하다" 에서 "정리가 적용된다" 로 건너뛴 것이다.
컴팩트성·횡단성·정의가능성을 확인하지 않으면 결론을 가져올 수 없다.

**그리고 나도 같은 종류의 실수를 두 번 했다.**

1. `T=1` 에서 동차성을 재고 "선형항이 없다" 고 결론 (EXCITABILITY.md §3)
   — 측정 설계가 결론을 미리 지웠다
2. 이 문서 초판에서 **Model 1 기준으로 초기구상을 판정** — 대상을 잘못 잡았다

두 경우 모두 "무엇에 대해 무엇을 재고 있는가" 를 확인하지 않은 것이다.
정리의 가설을 확인하지 않는 것과 정확히 같은 오류다.
