# 유한 상태의 용량과 배분 — 언어모델링 벤치마크 기록

> RESULTS.md 이후의 실험 기록. selective copy 에서 언어모델링으로 넘어가면서
> 드러난 것들과, **비교 축을 잘못 잡았다가 고친 과정**을 남긴다.
> 결론부터: 지금 막힌 벽은 우리 설계의 결함이 아니라 `O(1)` 상태 클래스 전체의
> 정보이론적 한계다.

---

## 0. 한 장 요약

| 발견 | 상태 |
|---|---|
| Model 1 은 파라미터 효율 곡선에서 Mamba-3 에 **진다** | 확정 |
| 그런데 비교 축이 틀렸다 — Mamba-3 파라미터의 **82.7% 가 MLP** | 확정 |
| value 재표현(`W_V`) 부재가 최대 병목 — **−1.02 bpb** | 확정 |
| 커널 바이어스가 **파라미터 256개로 −0.035** | 확정 |
| 유니터리는 감쇠보다 **0.82 bpb 나쁘다** | 확정 (단 교란 있음) |
| **`τ` 는 학습되지 않는다** — 초기화가 0.68 bpb 를 결정 | 확정 |
| 위 전부는 "고정 용량을 어떻게 배분하는가"의 문제 | 해석 |

---

## 1. 실험 조건

Python 표준 라이브러리 소스 8MB, 바이트 단위(V=256), `T=128`, 6,000스텝, Adam.

**두 가지 레시피가 섞여 있으니 숫자를 함부로 비교하면 안 된다.**

| 레시피 | 사용 런 |
|---|---|
| A: `lr=2e-3` 고정, warmup 없음 | §5.1 (RESULTS.md) 의 model0/model1/attn 비교 |
| B: warmup 300 + `lr(d) = 2e-3·√(128/d)` | 이 문서의 모든 런 |

레시피 B 가 필요했던 이유는 §4 참조.

---

## 2. 파라미터 효율 곡선

| Model 1 (tied, MLP 없음) | 총계 | bpb | | Mamba-3 (SwiGLU+BCNorm) | 총계 | bpb |
|---|---:|---:|---|---|---:|---:|
| d=128 | 57,664 | 3.166 | | L=1 | 191,624 | **1.906** |
| d=256 | 164,480 | 3.057 | | L=2 | 350,352 | **1.643** |
| d=384 | 320,448 | 2.961 | | L=4 | 667,808 | **1.403** |
| d=512 | 525,568 | 2.982 | | L=4 d=192 | 1,471,712 | **1.312** |

**겹치는 구간에서 전부 진다.** 526k 를 써도 192k 짜리 Mamba-3 에 1.08 bpb 뒤진다.
그리고 Model 1 은 d=384 → 512 에서 **오히려 나빠진다** — 폭 확장이 384 쯤에서 포화한다.

### 2.1 그런데 비교 축이 틀렸다

Mamba-3 재현의 파라미터가 어디 있는지 세어보면:

| 경로 | 파라미터 (L=4) | |
|---|---:|---|
| `in_proj → z` (게이트) | 65,536 | MLP |
| `out_proj` | 65,536 | MLP |
| `SwiGLU` | 393,216 | MLP |
| **소계** | **524,288** | **82.7%** |
| `x, B, C, dt, A, angles` + 상태 | 109,600 | SSM/위상 17.3% |

Mamba 블록은 `expand → silu(z) 게이트 → out_proj` 구조 자체가 **게이트 MLP** 이고,
Mamba-3 는 거기에 **SwiGLU 블록을 교대로 얹는다**(논문 §3.4, Llama 식).
즉 비교가 "위상 vs 위상"이 아니라 **"우리 위상 기계 단독" vs "그들 위상 기계 + 4.8배의 MLP"**
였다.

### 2.2 SSM 축으로 봐도 우리가 진다

| 모델 | SSM/위상 | MLP성 | bpb |
|---|---:|---:|---:|
| Model 1 d=512 | **394,496** | 0 | **2.982** |
| Mamba-3 L=4 | **109,600** | 524,288 | **1.403** |

SSM 파라미터를 **3.6배** 쓰고 1.58 bpb 진다. **위상 채널을 늘려도 MLP 가 하는 일을
대신 못 한다**는 뜻이며, §3 이 그것을 확증한다.

---

## 3. 축별 단가

d=256 고정, 레시피 B, 6,000스텝.

| 축 | 추가 파라미터 | Δbpb | 파라미터당 효율 |
|---|---:|---:|---|
| **value 재표현 `W_V`** | +65,536 | **−1.02** | 기준 |
| **감쇠 유지 (vs 유니터리)** | **+0** | **−0.82** | **∞** |
| **커널 바이어스** | +256 | −0.035 | 기준의 9배 |
| 폭 `d` 128→512 | +369,600 | −0.18 | 기준의 1/32 |

누적 결과:

| 설정 | 총계 | bpb |
|---|---:|---:|
| Model 1 (d=256) | 164,480 | 3.057 |
| + `W_V` | 230,016 | **2.036** |
| + `W_V` + bias | 230,272 | **2.002** |
| *(참고) Mamba-3 L=1* | *191,624* | *1.906* |

### 3.1 `W_V` — span 제약이 병목이었다

`W_V` 가 없으면 `f_t = Σ_n a_tn h_n` 이므로 출력이 **`span{h_1,…,h_t}` 안에 갇힌다**
(수치 확인: 잔차 1e-15). 재귀를 R번 돌아도 못 벗어난다.

차원 제약은 `t ≥ d` 에서 사라지므로 그것만으로는 성능을 설명 못 한다.
**진짜 문제는 "얼마나 섞을까"만 되고 "무엇을 가져올까"가 안 된다는 것** — 원본 표현을
통째로 더할 수밖에 없고, 필요한 부분공간만 뽑는 재표현이 불가능하다.

selective copy 에서 이게 안 드러난 이유: **복사 태스크는 원본을 그대로 가져오는 게 정답**
이라 `W_V` 가 필요 없다. 그래서 100% 가 나왔고, 그것이 LM 에서의 결함을 가렸다.

### 3.2 `W_O` 는 단독으로는 무의미하다

`A` 가 value 경로에 의존하지 않으므로

```
f = (A (h W_V)) W_O = A h (W_V W_O)      ← 오차 1.7e-16
```

**두 행렬이 하나로 합쳐진다.** `2d²` 파라미터로 `d²` 자유도를 쓴다.
`W_O` 가 의미를 가지려면 **멀티헤드**(헤드 간 혼합) 또는 **사이의 비선형**(Mamba-3 의
`silu(z)` 게이트)이 필요하다. 단일 헤드 + 비선형 없음 = 순수 낭비.

### 3.3 바이어스 — 파라미터 256개

`z ← W_C h + b` (복소). 커널이 이렇게 늘어난다:

```
Re[e^{-iψ}(z̄_t + b̄)(z_n + b)]
  = Re[e^{-iψ}z̄_t z_n]  +  Re[e^{-iψ}z̄_t b]  +  Re[e^{-iψ}b̄ z_n]  +  |b|²cos ψ
    └─ 기존 2차 ─┘         └─ h_t 만 ─┘        └─ h_n 만 ─┘        └─ 상수 ─┘
```

**상수항이 균일 어텐션 기저선을 만들어 `f` 에 순수 선형 경로가 열린다.**
3차 동차성이 깨진다(c=4 에서 비 64 → 31.4). Mamba-3 §3.4 가 인용한
Yu & Erichson(2025) 의 보편근사 결과가 이 구조에 관한 것이다.

`z` 에 공통으로 걸었으므로 `k = e^{-iψ}q` 관계가 유지되어 **라그랑지안 그래프는 보존**
된다(측정 −1.7e-16). Mamba-3 처럼 `B`, `C` 에 따로 걸면 깨진다.

---

## 4. `τ` 는 학습되지 않는다

### 4.1 유니터리 실험

`α = 0` (`|U_t| = 1`) 으로 두고 읽기 시점에 `√(Σ_{n≤t}‖k_n‖²‖h_n‖²)` 로 정규화.
이 정규화기는 **입력에서 계산되는 스칼라**라 재귀식의 선형성을 안 깬다.

| 설정 | bpb |
|---|---:|
| `W_V` + bias, 감쇠 | **2.002** |
| `W_V` + bias, 유니터리 + read_norm | **2.825** |

파라미터 차이는 스칼라 1개. **0.82 bpb 를 `α` 한 축이 결정한다.**

먼저 확인한 것들 (T=512, 실측):

| | ‖S‖ t=512 | `∂/∂h_1` |
|---|---:|---:|
| 유니터리 | 512.9 (정확히 √t 성장, 배율 1.99 vs 이론 2.00) | **7.08** |
| 감쇠 (\|λ\|=0.95~0.99) | 106.5 | **2.07e-03** |

**부동소수점은 문제가 아니다** — `T=10⁶` 에서도 `‖S‖~10³`, float32 상한 3.4e38.
**역전파도 유니터리가 3400배 유리하다** — 노름 보존이라 vanishing 이 없다.
즉 감쇠를 넣은 이유로 흔히 드는 두 근거가 모두 성립하지 않는다.

### 4.2 그럼 감쇠가 귀납 편향인가 — 확인 실패

`α` 를 학습 가능하게 두고 `τ₀ = 9998` (사실상 유니터리) 에서 출발시켰다.
모델이 감쇠를 원하면 `τ` 를 줄일 것이다.

| | 초기 | 학습 후 |
|---|---:|---|
| `τ` 중앙값 | 9,998 | **10,536** |
| `τ < T=128` 채널 | — | **0 / 128** |
| `τ < 9000` | — | **7 / 128** |
| bpb | — | **2.679** |

**중앙값이 오히려 올라갔다.** 128채널 중 하나도 `T` 아래로 안 내려왔다.

`α = exp(alpha_log)` 파라미터화이므로 `τ` 를 9998 → 50 으로 줄이려면 `alpha_log` 가
**5.3 만큼** 이동해야 한다. 그 사이 `W_C`·`W_V`·`ψ` 가 나쁜 `τ` 에 맞춰 자리를 잡으면
`α` 를 움직일 유인도 사라진다.

### 4.3 그래서 결론은 "초기화"다

| 설정 | 실제로 쓴 τ | bpb |
|---|---|---:|
| 감쇠 초기화 (τ₀ 9.5~1000) | 초기값 근처 | **2.002** |
| α 학습 (τ₀ = 9998) | **9998 근처 — 못 움직임** | 2.679 |
| 유니터리 고정 | ∞ | 2.825 |

**`τ₀` 하나가 0.68 bpb 를 결정하고, 학습으로 교정되지 않는다.**

S4/Mamba 계열이 `dt_min`, `dt_max` 를 로그 균등으로 세심하게 잡는 이유가 여기 있다
(Mamba-3 공식 구현에도 `dt_min=0.001, dt_max=0.1` 하드코딩).
우리 `r_min=0.90, r_max=0.999` 는 우연히 잘 맞은 것이고 최적이라는 근거가 없다.

**미결:** 유니터리가 진 것이 (a) 유니터리성 자체, (b) read_norm 설계, (c) `τ` 초기화
문제의 극단값 중 무엇 때문인지 아직 안 갈렸다. `α` 에만 100배 학습률을 주면 갈린다.

### 4.4 폭 확장에는 lr 스케일링이 필요하다

`W_C` 초기 std 가 `w_scale/√(2d)` 인데 Adam 첫 스텝은 그래디언트와 무관하게 `±lr`.
따라서 상대 교란이 `lr·√(2d)/w_scale` 로 폭에 따라 커지고, 3차 자기항이 이를
세제곱으로, R=4 재귀가 복리로 증폭한다 → **d=512 에서 첫 스텝에 발산.**

```
lr(d) = lr₀ · √(d₀/d)
```

d=128 의 `2e-3` 에서 d=512 면 `1e-3` 을 예측하는데, 실측 경계가 정확히 거기였다
(`2e-3` 발산, `1e-3` 정상). warmup 300 과 함께 쓰면 안정된다.
정규화를 뺀 대가를 정량화한 값이다.

---

## 5. Mamba-3 와의 전체 대조

"다 벗기면 우리 모델로 수렴한다"고 썼다가 **철회**했다. 수렴하지 않는다.

### 5.1 깊이 구조 — 가장 큰 차이

| | Model 1 | Mamba-3 |
|---|---|---|
| 깊이 | **weight-tied 재귀 R회** | 독립 레이어 L개 |
| 갱신 | `h ← h + f(h)/R` — **Euler 적분** | `h ← h + layer(h)` |
| 파라미터 vs 깊이 | **불변** | 비례 |

`1/R` 스케일 때문에 `R→∞` 극한이 연속 동역학이다. Mamba-3 의 residual 에는 그런
극한이 없다. §3.6(RESULTS.md) 의 깊이↔위상 교환도 tied 라서 물을 수 있던 질문이다.

### 5.2 나머지

| | Model 1 | Mamba-3 |
|---|---|---|
| 주소 사영 | **`W_C` 하나 + `±ψ/2`** | `B`, `C` 독립 |
| `q,k` 관계 | `k = e^{-iψ}q` — **라그랑지안 그래프** | 무관 |
| `ψ` (흥분/억제 각) | **있음** | **없음** |
| 주소 정규화 | 없음 | `RMSNorm(B)`, `RMSNorm(C)` |
| 바이어스 | `z` 에 공통 (라그랑지안 보존) | `B`,`C` 따로 (대칭 파괴) |
| 회전 범위 | **전 채널** | `rope_fraction=0.5` — 절반 |
| 전이 크기 | **고정 α** | `exp(A_t Δ_t)` — 입력 의존 |
| 전이 위상 | `s·sigmoid` — **비음수 단조** | `tanh·Δ_t·π` — **부호 있음** |
| 이산화 | exp-Euler 등가 | exp-**trapezoidal** 3항 |
| value | `h` 또는 `h W_V` | `in_proj → x`, expand배 |
| 게이트 | 없음 | `RMSNormGated(y, z)` |
| 출력 사영 | 없음 | `out_proj` |
| MLP | **없음** | SwiGLU 교대 |
| 스킵 | residual 만 | `+ D·x` 추가 |
| 헤드 | 단일 (p채널 합산) | 4헤드, `ngroups=1` → **multi-query** |
| 상태 | `ℂ^{128×256}` → 실수 등가 **65,536** | `ℝ^{4×32×32}` = **4,096** |

**상태 크기가 16배 차이난다.** 우리는 `p×d` 라 `d²/2` 로 커지고, Mamba-3 는
`d_state` 고정이면 `d` 에 선형이다. 우리 폭 확장이 비싼 이유이기도 하다.

공통 조상은 **"복소 데이터 의존 회전 + 외적 상태 + RoPE trick"** 셋뿐이고,
그 위에서 두 계보가 갈린다.

### 5.3 재현의 충실도

원본 대비 `expand=1`(기본 2), `d_state=32`(기본 128), `headdim=32`(기본 64) 로 축소했고
exponential-trapezoidal 3항 재귀를 생략했다. **둘 다 원본보다 약하게 만드는 방향**인데도
Model 1 을 크게 이겼다. 따라서 "재현이 과대평가돼서 졌다"는 아니다.

---

## 6. 초기구상 채점

| 조항 | 판정 |
|---|---|
| §1 `q,k ∈ ℂ^d`, `v ∈ ℝ^d` | 타당 |
| §1 `φ_q, φ_k` = "토큰의 의미론적 위상" | **성립 안 함** — `W_Q` 실수, `x_t` 실수 → `x_tW_Q` 는 실수. 위상은 `φ_j` 하나뿐이고 `t` 인덱스가 없어 토큰 무관 |
| §2 상태 전이 | 타당 (⊙ 축이 모호) |
| §3 병렬 전개 / prefix scan | 타당 |
| §4 `o_t = Σ\|q\|\|k\|cos(…)v` | **부정확** — 채널별 합 필요, 부호 누락 |
| QM§1 `S_t` = 밀도 행렬 | **틀림** — 비에르미트 |
| QM§2 스톤 정리 | 참이나 무용. 정작 중요한 **대각성**(→ `U_t` 교환 → 구동 버전이 닫힌 형태) 미언급 |
| QM§2 유니터리·노름 보존 | 타당 (`γ=1` 조건부) |
| QM§3 무손실 중첩 | 타당 (동일 조건) |
| QM§4 호모다인 검출 | 타당 |
| QM§4 상쇄간섭 `Σ ≈ 0` | **틀림** — `O(√N)` |
| QM§4 정류위상 조건 | 타당 |

**12개 중 타당 7, 부정확 2, 틀림 3.**

### 6.1 그리고 Mamba-3 와 얼마나 겹치나

계산적 내용(§1~4)은 **사실상 전부 Mamba-3 에 있다** — 복소 q/k, 회전 상태 전이,
prefix scan, `Re(q†S)` 관측, 코사인 위상 간섭 커널("RoPE trick"이 정확히 그것).

겹치지 않는 것은 **QM 해석부의 세 항목뿐** — 유니터리성, 노름 보존, 무손실 중첩.
Mamba-3 는 `A_floor` 로 감쇠를 강제하므로 구조적으로 못 한다.

**그런데 그 셋을 우리가 먼저 포기했다.** `1/t` 를 버리고 `α` 를 넣은 순간 닫힌 계가
아니게 됐고, §4 의 실험은 되살리려는 시도가 0.82 bpb 손해였음을 보인다.

따라서 **초기구상 자체는 신규성 근거가 못 된다.** 살아 있는 차별점은 전부 이후에
만든 것들이다 — tied 재귀·오일러 적분, `ψ` 각과 라그랑지안 구조, `W_C` 통합 사영,
그리고 그 위에서 나온 측정 방법론.

---

## 7. 진짜 벽 — 정보이론적 한계

| | 뇌 | 현재 신경망 |
|---|---|---|
| 시냅스 | **추론 중에도 갱신** | 가중치 — **추론 중 동결** |
| 추론 중 새 문맥 | 시냅스에 **기록** | `S` 또는 KV 캐시 — 휘발성 |
| 누설 | 있어도 무관 (기억은 시냅스에) | 누설 = 곧 망각 |

핵심은 저장 장소가 아니라 **언제 쓰기가 가능한가**다. 가중치가 동결이면 추론 중
쓰기 가능한 곳은 `S` 뿐이고, 그건 고정 크기 휘발성이다.

| 접근 | 저장소 | 한계 |
|---|---|---|
| Transformer KV 캐시 | `O(T)` 증가 | 안 까먹음. 계산 `O(T²)` |
| 선형 어텐션 / SSM | 고정 `O(1)` | 싸지만 **반드시 까먹음** |
| fast weights (델타 규칙) | 고정, **덮어쓰기** | 용량이 랭크에 묶임 |
| test-time training | 추론 중 가중치 갱신 | 뇌에 가장 가까움 |

**`O(1)` 상태로 `O(T)` 정보를 담을 수 없다.** 아키텍처 튜닝 문제가 아니다.

그래서 감쇠 vs 유니터리 논쟁은 애초에 이길 수 없는 싸움이었다. 둘 다 "무엇을 먼저
버릴까"를 고를 뿐이고, 유니터리가 진 것은 "모든 것을 똑같이 흐리게" 하기 때문이지
유니터리성이 나빠서가 아닐 수 있다. **고정 용량의 패배**다.

### 7.1 발화-리셋은 대안이 되나

`‖S‖` 는 묶인다. 그러나 **리셋은 이산화된 감쇠**다 — 발화 전까지 완벽 보존,
발화 순간 통째로 소실. 총 정보량으로는 유한 용량에 유한 기억이고, 장거리 기억을
사주지 않는다.

의미를 가지려면 **버리는 게 아니라 옮겨야** 한다 → 빠른 상태(유니터리, 발화 시 리셋)
+ 느린 상태(누적, 시냅스/fast weights) 의 **두 시간척도**.

구현 장애물: 리셋 조건이 `‖S_t‖ > θ` 같은 **상태 의존**이면 재귀식이 `S` 에 비선형이
되어 **prefix scan 이 붕괴**한다. 모든 효율적 선형 모델이 게이트를 입력 의존으로만
두는 이유다.

우회로: **발화 시점을 입력에서 계산**하면 된다. `c_t = Σ_{n≤t}‖k_n‖²‖h_n‖²` 는
cumsum 이라 병렬 계산 가능하고, `c_t` 가 문턱의 배수를 넘을 때 리셋하면
**segmented scan** 이 된다. 구간 안에서는 완전 유니터리. 미분가능성도 유지된다.
이것이 지금 구조에서 실현 가능한 유일한 발화 형태로 보인다.

### 7.2 뇌 비유를 끝까지 밀면

뇌도 추론 중 시냅스 쓰기가 공짜가 아니다. 장기 강화(LTP)는 단백질 합성이 필요해
수 분~수 시간이 걸린다. **"30년 기억"은 대화 도중 즉석으로 만들어진 게 아니라
오래전에 공고화(consolidation)된 것**이다. 대화 중 작업기억은 뇌에서도 지속 발화·
단기 촉진에 의존하며, 용량이 작고 금방 사라진다.

| 뇌 | 모델 |
|---|---|
| 장기 시냅스 (공고화됨) | **학습된 가중치** — 대응됨 |
| 단기 촉진 / 지속 발화 | **`S` 상태** — 대응됨 |
| 그 사이의 **공고화 과정** | **없음** |

빠진 것은 저장소가 아니라 **공고화 메커니즘**이고, 그것이 test-time training 이
하려는 일이다. 뇌에서도 느리고 비싸다.

---

## 8. 그래서 우리 축은 무엇인가

**"용량을 늘린다"가 아니라 "같은 용량을 더 잘 조직한다".**

이번 세션에서 확인한 것이 전부 그 얘기였다.

- 위상 코드로 **순서 주소지정** — 시간 거리가 아니라 내용 좌표로 색인 (selective copy 100%)
- 채널별 `τ` — **다중 시간척도 배분**. 그런데 §4 에서 이 배분이 학습되지 않음이 드러났다
- `ψ` — 흥분/억제, 계량과 심플렉틱 성분의 혼합각
- 깊이 ↔ 위상 **교환 가능한 자원**

`τ` 초기화 문제가 정확히 이 축의 실패 사례다 — 배분이 나쁘면 0.68 bpb 를 잃는데
학습으로 못 고친다.

### 8.1 아직 안 해본 것

| # | 후보 | 동형성 비용 | 근거 |
|---|---|---|---|
| 1 | **`τ` 초기 범위 스윕** | 없음 | 파라미터 0개로 0.68 규모가 움직임 |
| 2 | **멀티헤드** | 낮음 (블록대각 보존) | `W_O` 를 비선형 없이 살림. 수상돌기 구획 해석 |
| 3 | `α` 전용 학습률 (×100) | 없음 | §4.3 의 미결을 가름 |
| 4 | Mamba-3 − SwiGLU / − 게이트 | — | MLP 사다리 분해 |
| 5 | 입력 계산 발화 시점 (segmented) | 중간 | §7.1. 모델에 처음으로 발화가 생김 |
| 6 | `SiLU + W_O` | 높음 | 분해 파괴. 마지막 수단 |

**선형·구조적 확장을 소진한 뒤에 점별 비선형에 손대는 것**이 원칙.
`W_V`(−1.02)와 바이어스(−0.035)가 그 범주였고 둘 다 분해를 보존했다.

---

## 9. 이번에 고친 버그

`use_wv` 를 병렬 경로에만 적용하고 **순차 경로에 안 넣었다.** 두 경로가 1.4 만큼
어긋나 있었다. 학습·평가는 전부 `parallel` 로 돌아 **결과에는 영향 없다**(순차 경로는
검증·추론용). 지금 3.8e-14 로 복구.

원인: `check_model1.py` 가 기본 설정만 검사하고 **플래그 조합별 등가성을 안 돈다.**
추가해야 한다.
