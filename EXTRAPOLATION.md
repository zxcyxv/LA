# 길이 외삽 — 진단과 부분 해결

selective copy 에서 학습 길이 밖 성능이 왜 무너지는가. `l_noise` 를 학습값의 4배까지
늘려가며 zero-shot 으로 측정했다. 지표는 seq_acc (한 시퀀스의 4개 토큰을 전부 맞춰야 1).

---

## 0. 출발 사실

어떤 개입을 해도 한계는 **학습 길이의 약 2배**에서 왔다. `l_noise=32` 학습 모델은 64 에서,
64 학습 모델은 128 에서 깨진다. 게이트 종류도 학습 데이터 길이 랜덤화도 이 배율을 못 움직였다.

---

## 1. 반증한 가설 넷

| 가설 | 개입 | `l_noise=128` seq_acc | 판정 |
|---|---|---|---|
| 빈칸 위상 잔차가 공진을 깬다 | — | — | **반증** |
| 잔차를 정확히 0 으로 만들면 산다 | `phase_gate="hard"` | 0.840 → **0.203** | **반증** (악화) |
| 학습 압력이 없어서다 | 길이 랜덤화 (`l_noise∈[16,64]`) | 0.840 → 0.809 | **반증** (무변화) |
| 감쇠가 짧아 신호가 죽는다 | `decay_init="logtau"` | 0.840 → 0.902 | **반증** (미미) |

### 1-1. 잔차 가설이 반증된 방식

`hard` 게이트는 `clamp(logit,0,1)` 이라 로짓 ≤0 에서 **정확한 0** 을 낼 수 있다. 실제로
`hard_rand` 의 최소 잔차는 0.0006 으로 전 모델 중 가장 작았다. 그런데 `l_noise=96` 에서
0.340 으로 가장 나빴다. **잔차와 외삽이 반대로 갔다.**

(당시 잰 것은 64채널 중 최솟값이었다. 뒤에서 보듯 봐야 할 것은 잔차의 *분포* 였고,
`hard` 가 실패한 진짜 이유는 클램프 구간 밖에서 그래디언트가 죽어 최적화가 나빠진 것이다
— 유일하게 학습 seq_acc 가 1.000 에 못 미쳤다: 0.994.)

### 1-2. 감쇠 가설이 반증된 방식 — SNR 은 결정 변수가 아니다

감쇠 초기화 `r ~ U(0.9, 0.999)` 는 **크기** 를 균등 추출하는데, 반감기 `ln2/(−ln r)` 는
`r→1` 에서 비선형 폭발한다. 명목 범위가 6.6~693 토큰이어도 실제 중앙값은 11.5 로 몰린다.
`decay_init="logtau"` 는 `log α` 를 균등하게 뿌려 중앙 반감기 94.9, 128 초과 채널 25개를
확보한다 (기존 3개). 학습 후에도 유지됐다 (중앙 110.5, 30/64).

읽기 시점 상태를 정확히 분해한 결과 (S 는 쓰기에 대해 선형이므로 근사가 아니다):

| 모델 | l_noise | ‖S_데이터‖ | ‖S_빈칸‖ | 신호/간섭 | seq_acc |
|---|---|---|---|---|---|
| sig_fix | 64 | 0.618 | 1.654 | **0.374** | **1.000** |
| sig_fix | 256 | 0.368 | 1.851 | 0.199 | 0.074 |
| logtau | 256 | 0.693 | 2.795 | **0.248** | **0.094** |

`logtau` 는 SNR 을 28% 올리고 정확도를 0.02 올렸다. 그리고 `sig_fix@64` 는 SNR 0.374 에
정확도 1.000, `logtau@256` 은 SNR 0.248 에 정확도 0.094 — **SNR 1.5배 차로 정확도 10배 차가
날 수 없다.**

> **결론: 신호 세기는 결정 변수가 아니다. 따라서 정규화로 고칠 수 있는 문제가 아니다.**

---

## 2. 진단 — 오류의 종류

틀린 예측이 (a) 같은 시퀀스의 *다른 슬롯* 정답인지, (b) 시퀀스에 아예 없는 토큰인지 갈랐다.
(a) 면 주소 실패, (b) 면 신호 실패다.

| 모델 | l_noise | 정답 | 순열 오류 | 집합 밖 |
|---|---|---|---|---|
| sig_fix | 128 | 0.958 | **0.038** | 0.004 |
| sig_fix | 192 | 0.749 | **0.204** | 0.047 |
| sig_fix | 256 | 0.564 | **0.328** | 0.107 |
| logtau | 128 | 0.977 | **0.023** | 0.000 |

오류의 90~100% 가 순열 오류다.

> **기억은 멀쩡히 남아 있다. 어느 것을 꺼낼지를 못 고른다.**

`logtau` 가 헛수고였던 이유가 여기 있다 — 안 망가진 것을 고쳤다.

---

## 3. 원인 — 학습된 주소는 위치 주소였다

위상 증분은 `ω = θ + s·gate(h)` 이고 `θ` 는 채널 상수라 **모든 토큰에** 붙는다. 따라서

```
Φ_t − Φ_n  =  (t−n)·θ   +   s·(사이의 데이터 토큰 개수)
                위치            내용
```

학습된 `θ` 중앙값은 **2.93 rad/스텝** (최대 6.16, `|θ|<0.01` 인 채널 0/64). 64 토큰만
진행해도 `θ` 만으로 30 바퀴가 돈다. 위치 항이 압도한다.

추론 시점에 `θ:=0` 으로 죽이면 **전 구간 0.000** 으로 붕괴한다. 모델이 위치 항에
전적으로 의존한다는 뜻이다.

즉 실제로 학습된 것은 **상대 위치 코드, 사실상 RoPE** 다. RoPE 계열이 학습 길이 밖에서
별칭(aliasing)을 일으키는 것은 알려진 현상이고, 순열 오류만 나오는 것이 정확히 그 그림이다.

### 초기구상과의 관계 (중요)

이것은 구현이 설계를 배신한 것이 **아니다.** 초기구상 §4 의 공진 조건은

```
ΔΦ_n = (φ_q − φ_k) − (t−n)θ ≈ 0
```

이고, `(t−n)θ` 는 제거 대상이 아니라 **조회 축 그 자체** 다. 의미 위상 `φ_q, φ_k` 는 토큰마다
고정된 값이지 누적값이 아니며, 읽기는 "의미가 거리를 고르는" 정류점 조회다.
누적 위상 `Φ_t = cumsum(ω)` 는 초기구상에 없고 대화내역의 driven Hamiltonian 쪽 개념이다.

> **모델은 초기구상이 설계한 대로 동작한다. 외삽이 안 되는 것은 설계 자체가 위치 기반
> 조회이기 때문이다.**

따라서 아래 §4 는 초기구상의 *검증* 이 아니라 *수정 제안* 이다.

---

## 4. 부분 해결 — `ordinal_only` (θ:=0 고정)

`θ` 를 0 에 고정하고 학습하면 `Φ_t` 는 "지금까지 센 데이터 토큰 수" 가 된다. 이 값은
`l_noise` 와 무관하게 항상 0~4 이므로 주소가 길이에 **원리적으로 불변** 이다.

| | 32 | 64 | 96 | 128 | 192 | 256 |
|---|---|---|---|---|---|---|
| sig_fix | 1.000 | 1.000 | 1.000 | 0.840 | 0.254 | 0.074 |
| logtau | 1.000 | 1.000 | 0.988 | 0.902 | 0.281 | 0.094 |
| **ordinal** | 0.996 | 1.000 | 0.973 | **0.973** | **0.613** | **0.285** |

192 에서 2.4배, 256 에서 3.9배. 학습 범위 내 성능 손실은 없다.

메커니즘도 함께 확인된다. 빈칸 잔차 최솟값이 **0.0000 / 0.0001 / 0.0003** 으로 사실상 정확한 0
이고, 128칸 드리프트가 π/2 미만인 채널이 1~2개에서 **7개** 로 늘었다. `hard` 게이트가 억지로
못 만들던 정확한 0 을, `θ` 를 치우니 sigmoid 가 그냥 만들어낸다 — `θ` 가 잔차의 원인이었지
게이트의 표현력이 원인이 아니었다.

### 4-1. 부작용 — 빈칸 간섭이 뭉친다

`θ=0` 이면 빈칸들이 전부 같은 위상을 갖는다. 예전엔 `θ` 가 빈칸을 위상 축에 흩뿌려 상쇄시켰다.

| 모델 | l_noise | ‖S_빈칸‖ | 빈칸 위상집중도 | 신호/간섭 | seq_acc |
|---|---|---|---|---|---|
| sig_fix | 64→256 | 1.65 → 1.85 | 0.073 → 0.047 | 0.374 → 0.199 | 1.000 → 0.074 |
| ordinal | 64→256 | 2.13 → 2.87 | 0.118 → 0.068 | 0.358 → **0.169** | 1.000 → **0.285** |

`ordinal` 은 SNR 이 더 나쁜데 정확도는 4배 좋다. **SNR 이 결정 변수가 아니라는 두 번째 독립
증거다.** 간섭 증가는 실재하지만 병목이 아니다.

---

## 5. 남은 격차

`ordinal` 도 평평하지 않다. 원인은 같고 정도만 덜하다.

**전 채널 잔차 분포** (`|ω(blank)|`, 64채널)

| 모델 | 10% | 25% | 50% | 75% | 90% | <0.01 |
|---|---|---|---|---|---|---|
| sig_fix | 1.485 | 2.029 | 3.039 | 5.137 | 5.841 | 0/64 |
| ordinal | 0.007 | 0.340 | **1.044** | 2.292 | 2.731 | **7/64** |

길이 불변 주소를 가진 채널은 64개 중 7개뿐이고, 중앙값 1.04 rad 인 나머지 57개는 여전히
드리프트한다. **오류 유형도 그대로 주소 실패다.**

| l_noise | 정답 | 순열 오류 | 집합 밖 |
|---|---|---|---|
| 128 | 0.996 | 0.004 | 0.000 |
| 192 | 0.916 | **0.082** | 0.002 |
| 256 | 0.792 | **0.198** | 0.010 |

256 에서도 오류의 95% 가 순열 오류다.

---

## 6. RoPE 와의 대조 — 왜 우리만 절벽인가

먼저 전제를 바로잡아야 한다. **RoPE Transformer 도 zero-shot 외삽은 사실상 안 된다.**
ALiBi 논문(Press et al., 2021)이 sinusoidal 과 rotary 가 모두 학습 길이 밖에서 무너지는 것을
보이며 나왔고, 실제 장문맥 모델은 Position Interpolation(Chen et al., 2023), NTK-aware,
YaRN(Peng et al., 2023) 같은 **개입 + 장문맥 추가학습** 으로 만든다.

다만 무너지는 *모양* 이 다르다. RoPE 는 완만하고 우리는 절벽이다. 원인은 주파수 스펙트럼이다.

| | 주기 중앙값 | 주기 > 학습길이(68) 채널 |
|---|---|---|
| 우리 `θ` (학습 후) | **2.11 토큰** | **1/64** |
| RoPE (base=10000, dim=64) | **471 토큰** | **23/32** |

RoPE 는 `θ_j = base^(-2j/d)` 로 주파수를 **로그 균등** 하게 깐다. 채널의 2/3 이상이 문맥보다
긴 주기를 가지므로 **한 바퀴도 안 돈다** — 감기지 않으니 단조롭고 모호하지 않은 거친 위치
좌표가 되고, 고주파 채널이 그 위에 미세 분해능을 얹는다. 학습 길이를 넘어도 저주파 채널은
여전히 단조라 순서가 뒤집히지 않는다.

우리는 `θ ~ U(0, 2π)` 로 **각속도를 균등** 하게 뽑는다 (`theta_max = 2π`). 주기로 환산하면
1~3 토큰에 몰린다. **모든 채널이 Nyquist 위** 라 두세 토큰마다 감기고, 저주파 좌표가 아예
없으므로 거친 위치 정보 자체가 존재하지 않는다. 학습 길이를 벗어나는 순간 전 채널이 동시에
별칭을 일으킨다.

> **RoPE 의 (제한된) 외삽은 "위치 인코딩이라서" 가 아니라 "로그 균등 스펙트럼이라서" 생긴다.
> 우리는 같은 위치 인코딩을 쓰면서 스펙트럼만 잘못 깔았다.**

### 6-1. 감쇠에서 겪은 것과 같은 형태의 버그

| | 균등하게 뽑은 것 | 붕괴한 변수 | 명목 범위 | 실제 중앙값 |
|---|---|---|---|---|
| 감쇠 | 크기 `r ~ U(0.9, 0.999)` | 반감기 | 6.6~693 | **11.5** |
| 위상 | 각속도 `θ ~ U(0, 2π)` | 주기 | 1~∞ | **2.1** |

둘 다 "비율 변수를 균등하게 뽑으면 시간척도 변수가 짧은 쪽으로 뭉친다" 는 같은 실수다.
감쇠 쪽은 `decay_init="logtau"` 로 고쳤고 (효과는 없었다 — 안 망가진 것을 고쳤으므로),
위상 쪽은 아직 고치지 않았다. **이쪽이 실제 병목이다.**

### 6-2. 문헌 대응물

Kazemnejad et al. (2023) 은 decoder-only 에서 위치 인코딩을 아예 빼는 편(NoPE)이 RoPE·ALiBi
보다 길이 일반화가 낫다는 것을 보였다. 인과 마스킹만으로 위치가 암묵적으로 복원되기 때문이다.
§4 의 `ordinal_only` (θ:=0) 결과가 같은 현상이다.

---

## 6-3. Mamba-3 대조 — 같은 동적 RoPE 인데 훨씬 못한다

Mamba-3 는 우리와 같은 입력 의존 RoPE 를 쓰므로 직접 대조가 된다. 그리고 §6 이 지목한
두 처방을 **설계에 이미 갖고 있다.**

| | 우리 (기본) | Mamba-3(ref) |
|---|---|---|
| 정적 위상 `θ` | `U(0,2π)`, 중앙 2.93 rad/스텝 | **없음.** `angle_inc = tanh(proj)·DT·π` 전부 입력 의존 |
| 안 감기는 채널 | 1/64 | **`rope_fraction=0.5`** → `d_state` 32 중 16 이 무회전 (주기 무한) |
| 파라미터 | 27k | 637k (23배) |

같은 `l_noise=64`, 8000 스텝으로 학습 (둘 다 학습 범위 내 1.000/1.000 포화).

| l_noise | 32 | 64 | 96 | 128 | 192 | 256 |
|---|---|---|---|---|---|---|
| Mamba-3(ref) 637k | 0.770 | 1.000 | 1.000 | **0.062** | **0.000** | **0.000** |
| sig_fix 27k | 1.000 | 1.000 | 1.000 | 0.840 | 0.254 | 0.074 |
| **ordinal 27k** | 0.996 | 1.000 | 0.973 | **0.973** | **0.613** | **0.285** |

### 빈칸 억제는 충분조건이 아니다

Mamba-3 의 빈칸 회전 억제는 거의 완벽하다.

| layer | 빈칸 \|ΔΦ\| | 데이터 \|ΔΦ\| | 비율 | 빈칸 증분 <0.01 인 각도채널 |
|---|---|---|---|---|
| 0 | 0.0144 | 0.0662 | 0.218 | 0/8 |
| **1** | **0.0052** | **2.054** | **0.0025** | **8/8** |
| 2 | 0.0016 | 0.0258 | 0.060 | 8/8 |
| 3 | 0.0101 | 0.0685 | 0.147 | 4/8 |

우리 `ordinal` 이 학습으로 겨우 7/64 도달한 성질을 Mamba-3 는 8/8 로 갖는다. 그런데도
128 에서 붕괴한다.

> **빈칸 억제는 외삽의 필요조건이지 충분조건이 아니다.** §5 가 지목한 잔여 격차(57/64 채널)를
> 메우는 것만으로는 부족하다는 반례다.

### 실패의 질이 다르다

| 모델 | l_noise | 정답 | 순열 오류 | 집합 밖 |
|---|---|---|---|---|
| Mamba-3 | 128 | 0.269 | **0.548** | 0.183 |
| Mamba-3 | 192 | 0.060 | 0.161 | **0.779** |
| Mamba-3 | 256 | 0.060 | 0.161 | **0.779** |
| ordinal | 256 | 0.792 | **0.198** | 0.010 |

Mamba-3 는 128 에서 주소 실패로 시작하지만 192 부터 **집합 밖 오류가 78% 로 뒤집힌다.**
정답률 0.060 은 무작위 수준(1/16 = 0.0625)이고 192 와 256 의 수치가 소수점까지 같다 —
입력과 무관하게 같은 것을 내는 완전 포화다.

우리는 256 에서도 오류의 95% 가 순열 오류다. **기억은 살아 있고 주소만 틀린다.**

> **우리는 우아하게 열화하고 Mamba-3 는 파국적으로 붕괴한다.**

또 하나. Mamba-3 는 학습 길이보다 **짧은** `l_noise=32` 에서도 0.770 이다. 위상 드리프트라면
긴 쪽만 나빠져야 하는데 양쪽이 나쁘다. `T=68` 에 특화된 것을 배웠다는 신호이며, 원인 후보는
입력 의존 감쇠의 포화, 레이어별 정규화, 4층 누적이다. (미확인.)

### 한계

`mamba3_ref.py` 는 공식 구현이 아니라 재현이다 (expand=1, exponential-Euler only, MIMO 없음).
파라미터도 23배 차이라 "구조 덕분"과 "용량 탓"을 이 실험만으로 가르지 못한다 — 다만 방향이
용량이 많은 쪽이 지는 쪽이므로, 용량으로 설명하려면 "용량이 클수록 길이 특화 과적합이 심하다"는
별도 주장이 필요하다. 층수를 줄인 파라미터 정합 대조가 남아 있다.

---

## 7. 다음 수

**(a) `θ` 를 로그 균등으로 깐다.** §6 이 직접 지시하는 수정이며, `ordinal_only` 와 달리
초기구상의 위치 조회 축을 **유지한 채** 고친다. 따라서 언어모델로도 그대로 이전된다
(언어에서는 위치가 실제 정보이므로 `θ=0` 은 쓸 수 없다). `ordinal` 과 배타적이지 않아
`log-θ + 서수 게이트` 조합이면 거친 위치와 내용 서수를 동시에 갖는다.

**(b) 모든 채널에서 `gate(blank)` 가 정확히 0 이 되게 한다.** sigmoid 는 로짓 −∞ 를 요구하므로
구조적으로 불가능하다. `hard` 는 정확한 0 을 내지만 그래디언트가 죽어 실패했다 — 다만 그
실험은 `θ` 가 살아 있는 교란 조건이었다. `θ` 를 정리한 지금 재시험할 가치가 있고,
straight-through (순방향 clamp, 역방향 sigmoid) 가 두 요구를 동시에 만족시킨다.

우선순위는 (a) 다. (b) 는 §5 의 잔여 격차(57/64 채널)를 겨냥하는데, §6-3 이 (b) 만으로는
부족하다는 반례를 준다 — Mamba-3 는 빈칸 억제가 8/8 인데도 붕괴한다. (a) 는 절벽 자체를 겨냥한다.

**(c) Mamba-3 의 붕괴 원인 규명.** 학습 길이보다 짧은 32 에서도 0.770 인 것, 192 부터 상수
출력으로 포화하는 것이 설명되지 않았다. 입력 의존 감쇠의 포화가 유력하다. 층수를 줄여
파라미터를 맞춘 대조도 함께 필요하다.

---

## 8. 한계

이 태스크는 출력 순서가 등장 순서라서 **"세기"가 곧 정답 주소** 다. `ordinal_only` 에
구조적으로 유리하다. 따라서 여기서 얻은 결론은 "이 태스크에서 내용 주소로 충분하다" 까지이며,
언어모델에서도 `θ` 를 버릴 수 있다는 뜻이 아니다. 언어에서는 위치 자체가 정보이므로
별도 측정이 필요하다.

---

## 부록 — 추가된 설정 플래그

| 플래그 | 위치 | 뜻 |
|---|---|---|
| `decay_init="logtau"` | `Model1Config` | 감쇠 시간척도를 로그 균등으로 초기화 |
| `ordinal_only=True` | `Model1Config` | `θ:=0` 고정 (`requires_grad=False`). 위치 위상 제거 |
| `phase_gate="hard"` | `Model1Config` | `clamp(logit,0,1)`. 정확한 0 가능, 그래디언트 소실 |
| `--l-noise-min` | `train_selective_copy.py` | 샘플마다 `l_noise` 랜덤화 |

평가 스크립트: `eval_extrap.py` (외삽 격자 + 잔차), `snr_len.py` (신호/간섭 분해).
