# 흥분성과 FHN 정합 — 그리고 쉽게 빠지는 두 함정

> **이 문서의 용도.** 이 구조가 FitzHugh–Nagumo 흥분계와 어떻게 정합하는지를
> 정리한다. 동시에, 분석하다가 **정반대 결론에 도달하게 만드는 두 가지 측정 함정**을
> 기록한다. 실제로 이 세션에서 두 함정에 다 빠져 "흥분 요소가 없다"는 틀린 결론을
> 냈다가 철회했다. 사람이든 AI든 같은 자리에서 넘어지기 쉬우니 남겨둔다.

---

## 1. 결론부터

**FHN 흥분계의 네 요소가 모두 존재하며, 학습된 모델에서 실제로 작동한다.**

| # | FHN 요소 | 이 구조에서 | 실측 |
|---|---|---|---|
| 1 | 3차항 `−u³/3` (포화) | `a_tt · h_t`, `a_tt ∝ Σ_j γ_j cos ψ_j \|W_C h_t\|²_j` | `a_tt(data) = −0.61` — **포화 부호** |
| 2 | 선형항 `+u` (불안정화) | `M(S_{t-1}) · h_t` | `max Re λ(M) > 0`, **96~100%** |
| 3 | 느린 회복변수 `v` | `S_t` — 발화가 `S`에 기록되고 `S`가 다음 이득을 바꿈 | 구조적으로 존재 |
| 4 | 시간척도 분리 `ε ≪ 1` | 채널별 `τ = 1/α` | 2.4 ~ 2286 |

그리고 ②가 **상수가 아니라 기억의 함수**라는 점에서 FHN보다 풍부하다.
FHN 의 `+u` 는 고정 계수지만, 여기서는 이득 자체가 `S` 에 의해 조절된다.

---

## 2. 정확한 분해

상태 갱신을 한 스텝 펼치면

```
S_t = Λ S_{t-1} + k_t h_tᵀ
```

이므로 출력이 두 항으로 정확히 갈린다.

```
f_t = p^{-1/2} · Re[ q_t† S_t ]

    = p^{-1/2} · Re[ q_t† Λ S_{t-1} ]   +   p^{-1/2} · Re[ q_t† k_t h_tᵀ ]
      └────────── M(S_{t-1}) h_t ──────┘     └────────── a_tt h_t ────────┘
              h_t 에 **선형**                        h_t 에 **3차**
```

`q_t` 는 `h_t` 에 선형이고 `S_{t-1}` 은 과거만의 함수다. 따라서 첫 항은
`h_t` 에 대해 **정확히 선형**이며, 그 계수 행렬이

```
M(S_{t-1})[d,e] = p^{-1/2} · Re[ Σ_j conj(rot_j) · conj(W_C[j,e]) · λ_j · S_{t-1}[j,d] ]
                  rot_j = e^{iψ_j/2}  (gamma_split 이면 ×√γ_j)
```

이다. 이것이 대화내역2 가 유도한 `λ(S_{t-1})` 의 행렬 버전이다.

```
z_{t+1} = λ(S_{t-1}) z_t + κ z_t³        ← 대화내역2 §3
h_t 갱신 = M(S_{t-1}) h_t + a_tt h_t     ← 같은 구조
```

`analyze_excitability.py` 가 이 `M` 을 해석식으로 만들고 autograd 야코비안과
대조한다 (오차 ~4e-16).

---

## 3. 함정 ① — 동차성 검사를 `T=1` 에서 하기

**증상.** `f(c·h) = c³ f(h)` 가 정확히 성립한다 (오차 1e-16). "순수 3차 동차이므로
원점 야코비안이 0, 즉 선형항이 구조적으로 없다"고 결론짓게 된다.

**왜 틀렸나.** `T=1` 이면 `S_0 = 0` 이다. 그러면 위 분해의 **첫 항이 통째로 사라지고**
자기항만 남는다. 자기항은 실제로 3차이므로 검사는 "정확히 3차"를 보고한다.

> **찾으려는 대상을 검사 설계가 미리 지워버린다.**

`check_model0.py` 의 3차 동차성 테스트는 *자기항이 3차임*을 확인하는 용도로는
올바르다. 그것을 *전체 `f` 에 선형항이 없다*는 근거로 쓰면 안 된다.

**올바른 확인.** `T > 1` 에서 과거를 고정한 채 `h_t` 만 스케일한다. 또는
`M(S_{t-1})` 을 직접 만들어 본다.

---

## 4. 함정 ② — `d_t = Σ_n a_tn` 을 "이득"으로 읽기

**증상.** 토큰 종류별로 `d_t` 를 재면 **어디서나 음수**다
(blank −1.02, data −1.52, marker −3.88). "국소 이득이 음수이므로 자가증폭이 없다,
즉 흥분성이 없다"고 결론짓게 된다.

**왜 틀렸나.** `d_t` 는 어텐션 행의 **스칼라 합**이다. 행렬 `M` 의 스펙트럼이 아니다.
행합이 음수여도 특정 고유방향의 고유값은 양수일 수 있다.

실제로 같은 체크포인트에서:

```
d_t < 0  (모든 토큰 종류)         ←  평균적으로는 수축
max Re λ(M) > 0  (96~100%)        ←  특정 방향은 팽창
```

둘은 모순이 아니다. **대부분의 방향은 감쇠하고 소수의 방향만 증폭**한다는 뜻이며,
이는 대화내역2 §6 이 `ρ_R = ‖H A H‖₂` 를 "최악 모드 이득"으로 잡은 것과 같은 논리다.
토큰 그래프 층위에서는 그 논리를 적용해놓고 자기항 층위에서 빠뜨리기 쉽다.

**올바른 확인.** `M` 의 고유값을 보라. 스칼라 요약량으로 대체하지 말 것.

---

## 5. 실측

`python analyze_excitability.py --ckpt ckpt_model1_R4.pt --verify`
(selective copy 100%/100% 체크포인트, `d=128, p=64, R=4, T=36`)

```
[검증] M 해석식 vs autograd 야코비안: 4.441e-16

     토큰    max Re λ(M)      >0 비율
    blank         0.2155       96.88%
     data         0.0894       96.88%
   marker         0.1272      100.00%

  ‖M‖₂ 평균 4.6223   >1 비율 94.79%
```

작용소 노름 `‖M‖₂` 도 94.8% 의 시점에서 1 을 넘는다. 대화내역2 §3 의
"`|λ_t|<1` 이면 소산, `>1` 이면 작은 자극도 커진다" 에서 후자 쪽에 대체로 머문다.

`max Re λ(M) > 0` 은 **원점이 불안정한 방향이 존재한다**는 뜻이다. 그 방향의
섭동은 외부 입력 없이도 스스로 커진다. FHN 의 `+u` 가 하는 일이 바로 이것이다.

---

## 6. 그래도 남아 있는 것 — 이산 발화는 관측되지 않았다

흥분 **기질**이 있다는 것과 스파이크가 **일어난다**는 것은 다르다. 아직 관측되지
않은 것:

- 문턱(threshold) 통과 이벤트
- 전부아니면전무(all-or-none) 응답
- 유한한 피크와 그 이후 반전
- 명시적 리셋 / 불응기

다만 이것을 "흥분계가 아니다"로 읽으면 안 된다. **selective copy 는 이산 발화를
요구하지 않는 태스크**다 — 부드러운 위상 정합만으로 100% 가 나오므로 문턱을 만들
이유가 없다. 구조는 있고 태스크가 안 시켰다고 보는 편이 정합적이다.

반증 가능한 형태로 바꾸면: **출력 대역폭을 좁히거나 상태 전달에 노이즈를 넣어
인공적으로 "누설 케이블"을 만들면** 전부아니면전무 코드가 나와야 한다.

---

## 7. 함께 정정되는 것 — 3차항의 부호

대화내역1 은 *"스파이킹의 양의 피드백 폭발을 유발하는 수학적 본질은 3차항"*
이라고 했다. **반대다.** FHN 에서

```
u̇ = u − u³/3 − v
     ↑    ↑
     │    └─ 음의 피드백: 큰 진폭을 꺾음 (스파이크 상한)
     └────── 양의 피드백: 원점 불안정화 (스파이크 상승)
```

폭발은 **1차항**이 만들고 3차항이 **멈춘다**. 대화내역2 §6 이 지적한 대로다.

그리고 학습된 모델은 FHN 이 맞는 부호를 골랐다. `a_tt(data) = −0.61` 로 **음수**,
즉 포화항이다. 흥미롭게도 부호가 토큰 내용에 따라 바뀐다:

| 토큰 | `a_tt` | `\|z\|²` 가중 `cos ψ` |
|---|---:|---:|
| blank | +0.160 | −0.042 |
| data | **−0.609** | −0.234 |
| marker | −0.135 | +0.060 |

채널의 절반이 억제성인 게 아니라 (`cos ψ > 0` 이 27/64), **데이터 토큰이 억제성
채널을 선택적으로 켠다.** `a_tt` 의 부호는 `ψ` 가 결정하고, 가중치 `|z_{t,j}|²` 가
내용 의존이라 부호가 입력에 따라 뒤집힌다.

---

## 8. 왜 `ψ` 만 부호를 결정하는가 — 심플렉틱 관점

`ℂ^p` 의 에르미트 내적은 계량과 심플렉틱 형식으로 갈린다.

```
⟨u,v⟩ = Re⟨u,v⟩ + i·Im⟨u,v⟩ = g(u,v) + i·ω(u,v)
```

우리 커널을 전개하면

```
Re[ conj(q_t) k_n ] = γ [ cos ψ · g(z_t,z_n) + sin ψ · ω(z_t,z_n) ]
```

(실측 오차 2e-17). 즉 **`ψ` 는 계량 성분과 심플렉틱 성분을 섞는 각도**다.

| ψ | 커널 | 성질 |
|---|---|---|
| 0 | 순수 `g` | 대칭 — 무방향, 확산 |
| π/2 | 순수 `ω` | 반대칭 — 회전, 방향성 |

심플렉틱 형식은 교대형식이라 `ω(z,z) = 0` 이다. 따라서 **자기항에는 `cos ψ` 만
남는다** — 이것이 `a_tt ∝ cos ψ` 의 이유다.

부수적으로: `q = e^{+iψ/2}z`, `k = e^{-iψ/2}z` 는 유니터리 `e^{-iψ}` 의 그래프이므로
`(ℂ^p × ℂ^p, ω ⊖ ω)` 에서 **라그랑지안 부분다양체**다 (`ψ` 와 무관하게).
`q=k` 는 그중 대각선인 특수 경우일 뿐 새로 얻는 게 없고, `cos ψ = 1` 이 되어
**억제 채널을 구조적으로 금지**하게 된다 — 학습된 모델이 실제로 쓴 해를 막는 셈이다.

단, `γ` 를 `k` 에만 걸면 `(ω⊖ω) = (1−γ²)ω ≠ 0` 으로 라그랑지안성이 깨진다.
`√γ` 씩 양쪽에 배분하면 복구되며, 커널은 곱 `q̄k` 만 보므로 **함수는 불변**이다
(실측 1.7e-16). `Model1Config.gamma_split=True` 가 기본값이다.

---

## 9. 요약 — 무엇을 믿고 무엇을 의심할 것인가

**믿어도 되는 것**
- 분해 `f_t = M(S_{t-1})h_t + a_tt h_t` 는 항등식이다
- `M` 은 autograd 로 검증됐다 (4e-16)
- `max Re λ(M) > 0` 이 96~100% 에서 성립한다
- `a_tt` 는 포화 부호이며 내용 의존으로 뒤집힌다

**의심해야 하는 것**
- `T=1` 에서 잰 어떤 결론도 과거항을 지운 상태의 결론이다
- 스칼라 요약량(`d_t`, 행합, 평균)으로 안정성을 판단하지 말 것 — 스펙트럼을 볼 것
- "흥분 기질이 있다" ≠ "스파이크가 일어난다". 후자는 아직 관측되지 않았다
- 이 측정은 selective copy 체크포인트 하나에서 나온 것이다
